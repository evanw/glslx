(function(){var Xh=Object.create||function(a){return{__proto__:a}};function li(a,b){a.prototype=Xh(b.prototype),a.prototype.constructor=a}var mi=Math.imul||function(a,b){return (a*(b>>>16)<<16)+a*(b&65535)|0};var If;function ni(a){return typeof a==='string'}function oi(a){return a===null?a:a+''}function Ff(c,a,b){return c.a=a,c.b=b,c.c=a.length,c}function Gf(c){if(c.b>=c.c)return-1;var a=c.a.charCodeAt((c.b=c.b+1|0)-1|0);if(a&64512^55296)return a;if(c.b>=c.c)return-1;var b=c.a.charCodeAt((c.b=c.b+1|0)-1|0);return ((a<<10)+b|0)-56613888|0}function Nc(a){return a.c=a.c+1|0,a.c}function td(p,a){switch(a){case 0:var b={};return Array.from(p.b.keys()).forEach(function(c){b[c]=p.b.get(c)}),JSON.stringify({shaders:p.a?p.a.map(function(d){var e={name:d.a,contents:d.b};switch(d.c){case 1:e.type='vertex';break;case 2:e.type='fragment';break}return e}):null,renaming:b},null,2)+'\n';case 1:if(p.a){for(var f='',J=0,x=p.a,Q=x.length;J<Q;J=J+1|0){var h=x[J];f+='export const GLSLX_SOURCE_'+h.a.replace(new RegExp('([a-z0-9])([A-Z])',
'g'),'$1_$2').toUpperCase()+' = '+JSON.stringify(h.b)+'\n'}if(p.b&&Array.from(p.b.keys()).length){f+='\n';for(var T=0,fa=Array.from(p.b.keys()),ha=fa.length;T<ha;T=T+1|0){var m=fa[T];f+='export const GLSLX_NAME_'+m.replace(new RegExp('([a-z0-9])([A-Z])','g'),'$1_$2').toUpperCase()+' = '+JSON.stringify(p.b.get(m))+'\n'}}return f}break;case 2:if(p.a){var l='';l+='#ifndef GLSLX_STRINGS_H\n',l+='#define GLSLX_STRINGS_H\n',l+='\n';for(var X=0,ca=p.a,aa=ca.length;X<aa;X=X+1|0){var g=ca[X];l+='static const char *GLSLX_SOURCE_'+g.a.replace(new RegExp('([a-z0-9])([A-Z])','g'),'$1_$2').toUpperCase()+' = '+JSON.stringify(g.b)+';\n'}if(l+='\n',p.b){for(var ia=0,ma=Array.from(p.b.keys()),za=ma.length;ia<za;ia=ia+1|0){var i=ma[ia];l+='static const char *GLSLX_NAME_'+i.replace(new RegExp('([a-z0-9])([A-Z])','g'),'$1_$2').toUpperCase()+' = '+JSON.stringify(p.b.get(i))+';\n'}l+='\n'}return l+='#endif\n',l}break;case 3:if(p.a){for(var o='',ga=0,la=p.a,kb=la.length;ga<kb;ga=ga+1|0){var k=la[ga];o+='const GLSLX_SOURCE_'+k.a.replace(new RegExp('([a-z0-9])([A-Z])',
'g'),'$1_$2').toUpperCase()+' = '+JSON.stringify(k.b)+'\n'}if(p.b&&Array.from(p.b.keys()).length){o+='\n';for(var ra=0,Za=Array.from(p.b.keys()),Xa=Za.length;ra<Xa;ra=ra+1|0){var q=Za[ra];o+='const GLSLX_NAME_'+q.replace(new RegExp('([a-z0-9])([A-Z])','g'),'$1_$2').toUpperCase()+' = '+JSON.stringify(p.b.get(q))+'\n'}}return o}break;case 4:if(p.a){for(var y='',Na=0,lb=p.a,Ya=lb.length;Na<Ya;Na=Na+1|0){var s=lb[Na];y+='pub static GLSLX_SOURCE_'+s.a.replace(new RegExp('([a-z0-9])([A-Z])','g'),'$1_$2').toUpperCase()+': &str = '+JSON.stringify(s.b)+';\n'}if(p.b&&Array.from(p.b.keys()).length){y+='\n';for(var Aa=0,mb=Array.from(p.b.keys()),Fa=mb.length;Aa<Fa;Aa=Aa+1|0){var z=mb[Aa];y+='pub static GLSLX_NAME_'+z.replace(new RegExp('([a-z0-9])([A-Z])','g'),'$1_$2').toUpperCase()+': &str = '+JSON.stringify(p.b.get(z))+';\n'}}return y}break}return null}function ie(a,b){for(var c=null,h=0,m=b.length;h<m;h=h+1|0){var d=b[h];if(d.c.b==1){c=d;break}}var e=null;if(c){e=new Vi('<api>',"\n#version 300 es\nimport {\n  in highp int gl_VertexID;\n  in highp int gl_InstanceID;\n  out highp vec4 gl_Position;\n  out highp float gl_PointSize;\n\n  in highp vec4 gl_FragCoord;\n  in bool gl_FrontFacing;\n  out highp float gl_FragDepth;\n  in mediump vec2 gl_PointCoord;\n\n  const mediump int gl_MaxVertexAttribs;\n  const mediump int gl_MaxVertexUniformVectors;\n  const mediump int gl_MaxVertexOutputVectors;\n  const mediump int gl_MaxFragmentInputVectors;\n  const mediump int gl_MaxVertexTextureImageUnits;\n  const mediump int gl_MaxCombinedTextureImageUnits;\n  const mediump int gl_MaxTextureImageUnits;\n  const mediump int gl_MaxFragmentUniformVectors;\n  const mediump int gl_MaxDrawBuffers;\n  const mediump int gl_MinProgramTexelOffset;\n  const mediump int gl_MaxProgramTexelOffset;\n\n  struct gl_DepthRangeParameters {\n    highp float near; // n\n    highp float far; // f\n    highp float diff; // f - n\n  };\n  uniform gl_DepthRangeParameters gl_DepthRange;\n\n  ////////////////////////////////////////////////////////////////////////////////\n  // Angle and Trigonometry Functions\n\n  // Converts `degrees` to radians, i.e. `π / 180 * degrees`\n  float radians(float degrees);\n  // Converts `degrees` to radians, i.e. `π / 180 * degrees`\n  vec2 radians(vec2 degrees);\n  // Converts `degrees` to radians, i.e. `π / 180 * degrees`\n  vec3 radians(vec3 degrees);\n  // Converts `degrees` to radians, i.e. `π / 180 * degrees`\n  vec4 radians(vec4 degrees);\n\n  // Converts `radians` to degrees, i.e. `180 / π * radians`\n  float degrees(float radians);\n  // Converts `radians` to degrees, i.e. `180 / π * radians`\n  vec2 degrees(vec2 radians);\n  // Converts `radians` to degrees, i.e. `180 / π * radians`\n  vec3 degrees(vec3 radians);\n  // Converts `radians` to degrees, i.e. `180 / π * radians`\n  vec4 degrees(vec4 radians);\n\n  // The standard trigonometric sine function.\n  float sin(float angle);\n  // The standard trigonometric sine function.\n  vec2 sin(vec2 angle);\n  // The standard trigonometric sine function.\n  vec3 sin(vec3 angle);\n  // The standard trigonometric sine function.\n  vec4 sin(vec4 angle);\n\n  // The standard trigonometric cosine function.\n  float cos(float angle);\n  // The standard trigonometric cosine function.\n  vec2 cos(vec2 angle);\n  // The standard trigonometric cosine function.\n  vec3 cos(vec3 angle);\n  // The standard trigonometric cosine function.\n  vec4 cos(vec4 angle);\n\n  // The standard trigonometric tangent.\n  float tan(float angle);\n  // The standard trigonometric tangent.\n  vec2 tan(vec2 angle);\n  // The standard trigonometric tangent.\n  vec3 tan(vec3 angle);\n  // The standard trigonometric tangent.\n  vec4 tan(vec4 angle);\n\n  // Arc sine. Returns an angle whose sine is `x`. The range of values returned by this function is `[-π/2, π/2]`. Results are undefined if `∣x∣>1`.\n  float asin(float x);\n  // Arc sine. Returns an angle whose sine is `x`. The range of values returned by this function is `[-π/2, π/2]`. Results are undefined if `∣x∣>1`.\n  vec2 asin(vec2 x);\n  // Arc sine. Returns an angle whose sine is `x`. The range of values returned by this function is `[-π/2, π/2]`. Results are undefined if `∣x∣>1`.\n  vec3 asin(vec3 x);\n  // Arc sine. Returns an angle whose sine is `x`. The range of values returned by this function is `[-π/2, π/2]`. Results are undefined if `∣x∣>1`.\n  vec4 asin(vec4 x);\n\n  // Arc cosine. Returns an angle whose cosine is `x`. The range of values returned by this function is `[0, π]`. Results are undefined if `∣x∣>1`.\n  float acos(float x);\n  // Arc cosine. Returns an angle whose cosine is `x`. The range of values returned by this function is `[0, π]`. Results are undefined if `∣x∣>1`.\n  vec2 acos(vec2 x);\n  // Arc cosine. Returns an angle whose cosine is `x`. The range of values returned by this function is `[0, π]`. Results are undefined if `∣x∣>1`.\n  vec3 acos(vec3 x);\n  // Arc cosine. Returns an angle whose cosine is `x`. The range of values returned by this function is `[0, π]`. Results are undefined if `∣x∣>1`.\n  vec4 acos(vec4 x);\n\n  // Arc tangent. Returns an angle whose tangent is `y/x`. The signs of `x` and `y` are used to determine what quadrant the\n  // angle is in. The range of values returned by this function is `[−π,π]`. Results are undefined if `x` and `y` are both 0.\n  float atan(float y, float x);\n  // Arc tangent. Returns an angle whose tangent is `y/x`. The signs of `x` and `y` are used to determine what quadrant the\n  // angle is in. The range of values returned by this function is `[−π,π]`. Results are undefined if `x` and `y` are both 0.\n  vec2 atan(vec2 y, vec2 x);\n  // Arc tangent. Returns an angle whose tangent is `y/x`. The signs of `x` and `y` are used to determine what quadrant the\n  // angle is in. The range of values returned by this function is `[−π,π]`. Results are undefined if `x` and `y` are both 0.\n  vec3 atan(vec3 y, vec3 x);\n  // Arc tangent. Returns an angle whose tangent is `y/x`. The signs of `x` and `y` are used to determine what quadrant the\n  // angle is in. The range of values returned by this function is `[−π,π]`. Results are undefined if `x` and `y` are both 0.\n  vec4 atan(vec4 y, vec4 x);\n\n  // Arc tangent. Returns an angle whose tangent is `y_over_x`. The range of values returned by this function is `[-π/2, π/2]`.\n  float atan(float y_over_x);\n  // Arc tangent. Returns an angle whose tangent is `y_over_x`. The range of values returned by this function is `[-π/2, π/2]`.\n  vec2 atan(vec2 y_over_x);\n  // Arc tangent. Returns an angle whose tangent is `y_over_x`. The range of values returned by this function is `[-π/2, π/2]`.\n  vec3 atan(vec3 y_over_x);\n  // Arc tangent. Returns an angle whose tangent is `y_over_x`. The range of values returned by this function is `[-π/2, π/2]`.\n  vec4 atan(vec4 y_over_x);\n\n  // Returns the hyperbolic sine function `(eˣ - e⁻ˣ) / 2`\n  float sinh(float x);\n  // Returns the hyperbolic sine function `(eˣ - e⁻ˣ) / 2`\n  vec2 sinh(vec2 x);\n  // Returns the hyperbolic sine function `(eˣ - e⁻ˣ) / 2`\n  vec3 sinh(vec3 x);\n  // Returns the hyperbolic sine function `(eˣ - e⁻ˣ) / 2`\n  vec4 sinh(vec4 x);\n\n  // Returns the hyperbolic cosine function `(eˣ + e⁻ˣ) / 2`\n  float cosh(float x);\n  // Returns the hyperbolic cosine function `(eˣ + e⁻ˣ) / 2`\n  vec2 cosh(vec2 x);\n  // Returns the hyperbolic cosine function `(eˣ + e⁻ˣ) / 2`\n  vec3 cosh(vec3 x);\n  // Returns the hyperbolic cosine function `(eˣ + e⁻ˣ) / 2`\n  vec4 cosh(vec4 x);\n\n  // Returns the hyperbolic tangent function `sinh(x) / cosh(x)`\n  float tanh(float x);\n  // Returns the hyperbolic tangent function `sinh(x) / cosh(x)`\n  vec2 tanh(vec2 x);\n  // Returns the hyperbolic tangent function `sinh(x) / cosh(x)`\n  vec3 tanh(vec3 x);\n  // Returns the hyperbolic tangent function `sinh(x) / cosh(x)`\n  vec4 tanh(vec4 x);\n\n  // Arc hyperbolic sine; returns the inverse of `sinh`.\n  float asinh(float x);\n  // Arc hyperbolic sine; returns the inverse of `sinh`.\n  vec2 asinh(vec2 x);\n  // Arc hyperbolic sine; returns the inverse of `sinh`.\n  vec3 asinh(vec3 x);\n  // Arc hyperbolic sine; returns the inverse of `sinh`.\n  vec4 asinh(vec4 x);\n\n  // Arc hyperbolic cosine; returns the non-negative inverse of `cosh`. Results are undefined if `x < 1`.\n  float acosh(float x);\n  // Arc hyperbolic cosine; returns the non-negative inverse of `cosh`. Results are undefined if `x < 1`.\n  vec2 acosh(vec2 x);\n  // Arc hyperbolic cosine; returns the non-negative inverse of `cosh`. Results are undefined if `x < 1`.\n  vec3 acosh(vec3 x);\n  // Arc hyperbolic cosine; returns the non-negative inverse of `cosh`. Results are undefined if `x < 1`.\n  vec4 acosh(vec4 x);\n\n  // Arc hyperbolic tangent; returns the inverse of `tanh`. Results are undefined if `∣x∣≥1`.\n  float atanh(float x);\n  // Arc hyperbolic tangent; returns the inverse of `tanh`. Results are undefined if `∣x∣≥1`.\n  vec2 atanh(vec2 x);\n  // Arc hyperbolic tangent; returns the inverse of `tanh`. Results are undefined if `∣x∣≥1`.\n  vec3 atanh(vec3 x);\n  // Arc hyperbolic tangent; returns the inverse of `tanh`. Results are undefined if `∣x∣≥1`.\n  vec4 atanh(vec4 x);\n\n  ////////////////////////////////////////////////////////////////////////////////\n  // Exponential Functions\n\n  // Returns `x` raised to the `y` power, i.e., `xʸ`. Results are undefined if `x < 0`. Results are undefined if `x = 0` and `y <= 0`.\n  float pow(float x, float y);\n  // Returns `x` raised to the `y` power, i.e., `xʸ`. Results are undefined if `x < 0`. Results are undefined if `x = 0` and `y <= 0`.\n  vec2 pow(vec2 x, vec2 y);\n  // Returns `x` raised to the `y` power, i.e., `xʸ`. Results are undefined if `x < 0`. Results are undefined if `x = 0` and `y <= 0`.\n  vec3 pow(vec3 x, vec3 y);\n  // Returns `x` raised to the `y` power, i.e., `xʸ`. Results are undefined if `x < 0`. Results are undefined if `x = 0` and `y <= 0`.\n  vec4 pow(vec4 x, vec4 y);\n\n  // Returns the natural exponentiation of `x`, i.e., `eˣ`\n  float exp(float x);\n  // Returns the natural exponentiation of `x`, i.e., `eˣ`\n  vec2 exp(vec2 x);\n  // Returns the natural exponentiation of `x`, i.e., `eˣ`\n  vec3 exp(vec3 x);\n  // Returns the natural exponentiation of `x`, i.e., `eˣ`\n  vec4 exp(vec4 x);\n\n  // Returns the natural logarithm of `x`, i.e., returns the value `y` which satisfies the equation `x = eʸ`. Results are undefined if `x <= 0`.\n  float log(float x);\n  // Returns the natural logarithm of `x`, i.e., returns the value `y` which satisfies the equation `x = eʸ`. Results are undefined if `x <= 0`.\n  vec2 log(vec2 x);\n  // Returns the natural logarithm of `x`, i.e., returns the value `y` which satisfies the equation `x = eʸ`. Results are undefined if `x <= 0`.\n  vec3 log(vec3 x);\n  // Returns the natural logarithm of `x`, i.e., returns the value `y` which satisfies the equation `x = eʸ`. Results are undefined if `x <= 0`.\n  vec4 log(vec4 x);\n\n  // Returns 2 raised to the `x` power, i.e., `2ˣ`.\n  float exp2(float x);\n  // Returns 2 raised to the `x` power, i.e., `2ˣ`.\n  vec2 exp2(vec2 x);\n  // Returns 2 raised to the `x` power, i.e., `2ˣ`.\n  vec3 exp2(vec3 x);\n  // Returns 2 raised to the `x` power, i.e., `2ˣ`.\n  vec4 exp2(vec4 x);\n\n  // Returns the base 2 logarithm of `x`, i.e., returns the value `y` which satisfies the equation `x = 2ʸ`. Results are undefined if `x <= 0`.\n  float log2(float x);\n  // Returns the base 2 logarithm of `x`, i.e., returns the value `y` which satisfies the equation `x = 2ʸ`. Results are undefined if `x <= 0`.\n  vec2 log2(vec2 x);\n  // Returns the base 2 logarithm of `x`, i.e., returns the value `y` which satisfies the equation `x = 2ʸ`. Results are undefined if `x <= 0`.\n  vec3 log2(vec3 x);\n  // Returns the base 2 logarithm of `x`, i.e., returns the value `y` which satisfies the equation `x = 2ʸ`. Results are undefined if `x <= 0`.\n  vec4 log2(vec4 x);\n\n  // Returns `√x`. Results are undefined if `x < 0`.\n  float sqrt(float x);\n  // Returns `√x`. Results are undefined if `x < 0`.\n  vec2 sqrt(vec2 x);\n  // Returns `√x`. Results are undefined if `x < 0`.\n  vec3 sqrt(vec3 x);\n  // Returns `√x`. Results are undefined if `x < 0`.\n  vec4 sqrt(vec4 x);\n\n  // Returns `1 / √x`. Results are undefined if `x <= 0`.\n  float inversesqrt(float x);\n  // Returns `1 / √x`. Results are undefined if `x <= 0`.\n  vec2 inversesqrt(vec2 x);\n  // Returns `1 / √x`. Results are undefined if `x <= 0`.\n  vec3 inversesqrt(vec3 x);\n  // Returns `1 / √x`. Results are undefined if `x <= 0`.\n  vec4 inversesqrt(vec4 x);\n\n  ////////////////////////////////////////////////////////////////////////////////\n  // Common Functions\n\n  // Returns `x` if `x >= 0`, otherwise it returns `-x`.\n  float abs(float x);\n  // Returns `x` if `x >= 0`, otherwise it returns `-x`.\n  vec2 abs(vec2 x);\n  // Returns `x` if `x >= 0`, otherwise it returns `-x`.\n  vec3 abs(vec3 x);\n  // Returns `x` if `x >= 0`, otherwise it returns `-x`.\n  vec4 abs(vec4 x);\n\n  // Returns `x` if `x >= 0`, otherwise it returns `-x`.\n  int abs(int x);\n  // Returns `x` if `x >= 0`, otherwise it returns `-x`.\n  ivec2 abs(ivec2 x);\n  // Returns `x` if `x >= 0`, otherwise it returns `-x`.\n  ivec3 abs(ivec3 x);\n  // Returns `x` if `x >= 0`, otherwise it returns `-x`.\n  ivec4 abs(ivec4 x);\n\n  // Returns `1.0` if `x > 0`, `0.0` if `x = 0`, or `-1.0` if `x < 0`.\n  float sign(float x);\n  // Returns `1.0` if `x > 0`, `0.0` if `x = 0`, or `-1.0` if `x < 0`.\n  vec2 sign(vec2 x);\n  // Returns `1.0` if `x > 0`, `0.0` if `x = 0`, or `-1.0` if `x < 0`.\n  vec3 sign(vec3 x);\n  // Returns `1.0` if `x > 0`, `0.0` if `x = 0`, or `-1.0` if `x < 0`.\n  vec4 sign(vec4 x);\n\n  // Returns `1` if `x > 0`, `0` if `x = 0`, or `-1` if `x < 0`.\n  int sign(int x);\n  // Returns `1` if `x > 0`, `0` if `x = 0`, or `-1` if `x < 0`.\n  ivec2 sign(ivec2 x);\n  // Returns `1` if `x > 0`, `0` if `x = 0`, or `-1` if `x < 0`.\n  ivec3 sign(ivec3 x);\n  // Returns `1` if `x > 0`, `0` if `x = 0`, or `-1` if `x < 0`.\n  ivec4 sign(ivec4 x);\n\n  // Returns a value equal to the nearest integer that is less than or equal to `x`\n  float floor(float x);\n  // Returns a value equal to the nearest integer that is less than or equal to `x`\n  vec2 floor(vec2 x);\n  // Returns a value equal to the nearest integer that is less than or equal to `x`\n  vec3 floor(vec3 x);\n  // Returns a value equal to the nearest integer that is less than or equal to `x`\n  vec4 floor(vec4 x);\n\n  // Returns a value equal to the nearest integer to `x` whose absolute value is not larger than the absolute value of `x`.\n  float trunc(float x);\n  // Returns a value equal to the nearest integer to `x` whose absolute value is not larger than the absolute value of `x`.\n  vec2 trunc(vec2 x);\n  // Returns a value equal to the nearest integer to `x` whose absolute value is not larger than the absolute value of `x`.\n  vec3 trunc(vec3 x);\n  // Returns a value equal to the nearest integer to `x` whose absolute value is not larger than the absolute value of `x`.\n  vec4 trunc(vec4 x);\n\n  // Returns a value equal to the nearest integer to `x`. The fraction `0.5` will round in a direction chosen by the implementation.\n  float round(float x);\n  // Returns a value equal to the nearest integer to `x`. The fraction `0.5` will round in a direction chosen by the implementation.\n  vec2 round(vec2 x);\n  // Returns a value equal to the nearest integer to `x`. The fraction `0.5` will round in a direction chosen by the implementation.\n  vec3 round(vec3 x);\n  // Returns a value equal to the nearest integer to `x`. The fraction `0.5` will round in a direction chosen by the implementation.\n  vec4 round(vec4 x);\n\n  // Returns a value equal to the nearest integer to `x`. A fractional part of `0.5` will round toward the nearest even integer.\n  float roundEven(float x);\n  // Returns a value equal to the nearest integer to `x`. A fractional part of `0.5` will round toward the nearest even integer.\n  vec2 roundEven(vec2 x);\n  // Returns a value equal to the nearest integer to `x`. A fractional part of `0.5` will round toward the nearest even integer.\n  vec3 roundEven(vec3 x);\n  // Returns a value equal to the nearest integer to `x`. A fractional part of `0.5` will round toward the nearest even integer.\n  vec4 roundEven(vec4 x);\n\n  // Returns a value equal to the nearest integer that is greater than or equal to `x`\n  float ceil(float x);\n  // Returns a value equal to the nearest integer that is greater than or equal to `x`\n  vec2 ceil(vec2 x);\n  // Returns a value equal to the nearest integer that is greater than or equal to `x`\n  vec3 ceil(vec3 x);\n  // Returns a value equal to the nearest integer that is greater than or equal to `x`\n  vec4 ceil(vec4 x);\n\n  // Returns `x - floor(x)`\n  float fract(float x);\n  // Returns `x - floor(x)`\n  vec2 fract(vec2 x);\n  // Returns `x - floor(x)`\n  vec3 fract(vec3 x);\n  // Returns `x - floor(x)`\n  vec4 fract(vec4 x);\n\n  // Modulus. Returns `x - y * floor(x/y)`\n  float mod(float x, float y);\n  // Modulus. Returns `x - y * floor(x/y)`\n  vec2 mod(vec2 x, float y);\n  // Modulus. Returns `x - y * floor(x/y)`\n  vec3 mod(vec3 x, float y);\n  // Modulus. Returns `x - y * floor(x/y)`\n  vec4 mod(vec4 x, float y);\n\n  // Modulus. Returns `x - y * floor(x/y)`\n  vec2 mod(vec2 x, vec2 y);\n  // Modulus. Returns `x - y * floor(x/y)`\n  vec3 mod(vec3 x, vec3 y);\n  // Modulus. Returns `x - y * floor(x/y)`\n  vec4 mod(vec4 x, vec4 y);\n\n  // Returns the fractional part of `x` and sets `i` to the integer part (as a whole number floating point value).\n  // Both the return value and the output parameter will have the same sign as `x`.\n  // If `x` has the value `+/- INF`, the return value should be `NaN` and must be either `NaN` or `0.0`.\n  float modf(float x, out float i);\n  // Returns the fractional part of `x` and sets `i` to the integer part (as a whole number floating point value).\n  // Both the return value and the output parameter will have the same sign as `x`.\n  // If `x` has the value `+/- INF`, the return value should be `NaN` and must be either `NaN` or `0.0`.\n  vec2 modf(vec2 x, out vec2 i);\n  // Returns the fractional part of `x` and sets `i` to the integer part (as a whole number floating point value).\n  // Both the return value and the output parameter will have the same sign as `x`.\n  // If `x` has the value `+/- INF`, the return value should be `NaN` and must be either `NaN` or `0.0`.\n  vec3 modf(vec3 x, out vec3 i);\n  // Returns the fractional part of `x` and sets `i` to the integer part (as a whole number floating point value).\n  // Both the return value and the output parameter will have the same sign as `x`.\n  // If `x` has the value `+/- INF`, the return value should be `NaN` and must be either `NaN` or `0.0`.\n  vec4 modf(vec4 x, out vec4 i);\n\n  // Returns `y` if `y < x`, otherwise it returns `x`.\n  float min(float x, float y);\n  // Returns `y` if `y < x`, otherwise it returns `x`.\n  vec2 min(vec2 x, float y);\n  // Returns `y` if `y < x`, otherwise it returns `x`.\n  vec2 min(vec2 x, vec2 y);\n  // Returns `y` if `y < x`, otherwise it returns `x`.\n  vec3 min(vec3 x, float y);\n  // Returns `y` if `y < x`, otherwise it returns `x`.\n  vec3 min(vec3 x, vec3 y);\n  // Returns `y` if `y < x`, otherwise it returns `x`.\n  vec4 min(vec4 x, float y);\n  // Returns `y` if `y < x`, otherwise it returns `x`.\n  vec4 min(vec4 x, vec4 y);\n\n  // Returns `y` if `y < x`, otherwise it returns `x`.\n  int min(int x, int y);\n  // Returns `y` if `y < x`, otherwise it returns `x`.\n  ivec2 min(ivec2 x, int y);\n  // Returns `y` if `y < x`, otherwise it returns `x`.\n  ivec2 min(ivec2 x, ivec2 y);\n  // Returns `y` if `y < x`, otherwise it returns `x`.\n  ivec3 min(ivec3 x, int y);\n  // Returns `y` if `y < x`, otherwise it returns `x`.\n  ivec3 min(ivec3 x, ivec3 y);\n  // Returns `y` if `y < x`, otherwise it returns `x`.\n  ivec4 min(ivec4 x, int y);\n  // Returns `y` if `y < x`, otherwise it returns `x`.\n  ivec4 min(ivec4 x, ivec4 y);\n\n  // Returns `y` if `y < x`, otherwise it returns `x`.\n  uint min(uint x, uint y);\n  // Returns `y` if `y < x`, otherwise it returns `x`.\n  uvec2 min(uvec2 x, uint y);\n  // Returns `y` if `y < x`, otherwise it returns `x`.\n  uvec2 min(uvec2 x, uvec2 y);\n  // Returns `y` if `y < x`, otherwise it returns `x`.\n  uvec3 min(uvec3 x, uint y);\n  // Returns `y` if `y < x`, otherwise it returns `x`.\n  uvec3 min(uvec3 x, uvec3 y);\n  // Returns `y` if `y < x`, otherwise it returns `x`.\n  uvec4 min(uvec4 x, uint y);\n  // Returns `y` if `y < x`, otherwise it returns `x`.\n  uvec4 min(uvec4 x, uvec4 y);\n\n  // Returns `y` if `x < y`, otherwise it returns `x`.\n  float max(float x, float y);\n  // Returns `y` if `x < y`, otherwise it returns `x`.\n  vec2 max(vec2 x, float y);\n  // Returns `y` if `x < y`, otherwise it returns `x`.\n  vec2 max(vec2 x, vec2 y);\n  // Returns `y` if `x < y`, otherwise it returns `x`.\n  vec3 max(vec3 x, float y);\n  // Returns `y` if `x < y`, otherwise it returns `x`.\n  vec3 max(vec3 x, vec3 y);\n  // Returns `y` if `x < y`, otherwise it returns `x`.\n  vec4 max(vec4 x, float y);\n  // Returns `y` if `x < y`, otherwise it returns `x`.\n  vec4 max(vec4 x, vec4 y);\n\n  // Returns `y` if `x < y`, otherwise it returns `x`.\n  int max(int x, int y);\n  // Returns `y` if `x < y`, otherwise it returns `x`.\n  ivec2 max(ivec2 x, int y);\n  // Returns `y` if `x < y`, otherwise it returns `x`.\n  ivec2 max(ivec2 x, ivec2 y);\n  // Returns `y` if `x < y`, otherwise it returns `x`.\n  ivec3 max(ivec3 x, int y);\n  // Returns `y` if `x < y`, otherwise it returns `x`.\n  ivec3 max(ivec3 x, ivec3 y);\n  // Returns `y` if `x < y`, otherwise it returns `x`.\n  ivec4 max(ivec4 x, int y);\n  // Returns `y` if `x < y`, otherwise it returns `x`.\n  ivec4 max(ivec4 x, ivec4 y);\n\n  // Returns `y` if `x < y`, otherwise it returns `x`.\n  uint max(uint x, uint y);\n  // Returns `y` if `x < y`, otherwise it returns `x`.\n  uvec2 max(uvec2 x, uint y);\n  // Returns `y` if `x < y`, otherwise it returns `x`.\n  uvec2 max(uvec2 x, uvec2 y);\n  // Returns `y` if `x < y`, otherwise it returns `x`.\n  uvec3 max(uvec3 x, uint y);\n  // Returns `y` if `x < y`, otherwise it returns `x`.\n  uvec3 max(uvec3 x, uvec3 y);\n  // Returns `y` if `x < y`, otherwise it returns `x`.\n  uvec4 max(uvec4 x, uint y);\n  // Returns `y` if `x < y`, otherwise it returns `x`.\n  uvec4 max(uvec4 x, uvec4 y);\n\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  float clamp(float x, float minVal, float maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  vec2 clamp(vec2 x, float minVal, float maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  vec2 clamp(vec2 x, vec2 minVal, vec2 maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  vec3 clamp(vec3 x, float minVal, float maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  vec3 clamp(vec3 x, vec3 minVal, vec3 maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  vec4 clamp(vec4 x, float minVal, float maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  vec4 clamp(vec4 x, vec4 minVal, vec4 maxVal);\n\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  int clamp(int x, int minVal, int maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  ivec2 clamp(ivec2 x, int minVal, int maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  ivec2 clamp(ivec2 x, ivec2 minVal, ivec2 maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  ivec3 clamp(ivec3 x, int minVal, int maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  ivec3 clamp(ivec3 x, ivec3 minVal, ivec3 maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  ivec4 clamp(ivec4 x, int minVal, int maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  ivec4 clamp(ivec4 x, ivec4 minVal, ivec4 maxVal);\n\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  uint clamp(uint x, uint minVal, uint maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  uvec2 clamp(uvec2 x, uint minVal, uint maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  uvec2 clamp(uvec2 x, uvec2 minVal, uvec2 maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  uvec3 clamp(uvec3 x, uint minVal, uint maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  uvec3 clamp(uvec3 x, uvec3 minVal, uvec3 maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  uvec4 clamp(uvec4 x, uint minVal, uint maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  uvec4 clamp(uvec4 x, uvec4 minVal, uvec4 maxVal);\n\n  // Returns the linear blend of `x` and `y`, i.e. `x * (1-a) + y * a`\n  float mix(float x, float y, float a);\n  // Returns the linear blend of `x` and `y`, i.e. `x * (1-a) + y * a`\n  vec2 mix(vec2 x, vec2 y, float a);\n  // Returns the linear blend of `x` and `y`, i.e. `x * (1-a) + y * a`\n  vec2 mix(vec2 x, vec2 y, vec2 a);\n  // Returns the linear blend of `x` and `y`, i.e. `x * (1-a) + y * a`\n  vec3 mix(vec3 x, vec3 y, float a);\n  // Returns the linear blend of `x` and `y`, i.e. `x * (1-a) + y * a`\n  vec3 mix(vec3 x, vec3 y, vec3 a);\n  // Returns the linear blend of `x` and `y`, i.e. `x * (1-a) + y * a`\n  vec4 mix(vec4 x, vec4 y, float a);\n  // Returns the linear blend of `x` and `y`, i.e. `x * (1-a) + y * a`\n  vec4 mix(vec4 x, vec4 y, vec4 a);\n\n  // Selects which vector each returned component comes from. For a component of `a` that is\n  // false, the corresponding component of `x` is returned. For a component of `a` that is true,\n  // the corresponding component of `y` is returned.\n  float mix(float x, float y, bool a);\n  // Selects which vector each returned component comes from. For a component of `a` that is\n  // false, the corresponding component of `x` is returned. For a component of `a` that is true,\n  // the corresponding component of `y` is returned.\n  vec2 mix(vec2 x, vec2 y, bvec2 a);\n  // Selects which vector each returned component comes from. For a component of `a` that is\n  // false, the corresponding component of `x` is returned. For a component of `a` that is true,\n  // the corresponding component of `y` is returned.\n  vec3 mix(vec3 x, vec3 y, bvec3 a);\n  // Selects which vector each returned component comes from. For a component of `a` that is\n  // false, the corresponding component of `x` is returned. For a component of `a` that is true,\n  // the corresponding component of `y` is returned.\n  vec4 mix(vec4 x, vec4 y, bvec4 a);\n\n  // Returns `0.0` if `x < edge`, otherwise it returns `1.0`\n  float step(float edge, float x);\n  // Returns `0.0` if `x < edge`, otherwise it returns `1.0`\n  vec2 step(float edge, vec2 x);\n  // Returns `0.0` if `x < edge`, otherwise it returns `1.0`\n  vec2 step(vec2 edge, vec2 x);\n  // Returns `0.0` if `x < edge`, otherwise it returns `1.0`\n  vec3 step(float edge, vec3 x);\n  // Returns `0.0` if `x < edge`, otherwise it returns `1.0`\n  vec3 step(vec3 edge, vec3 x);\n  // Returns `0.0` if `x < edge`, otherwise it returns `1.0`\n  vec4 step(float edge, vec4 x);\n  // Returns `0.0` if `x < edge`, otherwise it returns `1.0`\n  vec4 step(vec4 edge, vec4 x);\n\n  // Returns `0.0` if `x <= edge0` and `1.0` if `x >= edge1` and performs smooth Hermite interpolation between 0 and 1 when `edge0 < x < edge1`.\n  // This is useful in cases where you would want a threshold function with a smooth transition. This is equivalent to:\n  //\n  // ```glslx\n  // float t = clamp((x - edge0) / (edge1 - edge0), 0.0, 1.0);\n  // return t * t * (3.0 - 2.0 * t);\n  // ```\n  //\n  // Results are undefined if `edge0 >= edge1`.\n  float smoothstep(float edge0, float edge1, float x);\n  // Returns `0.0` if `x <= edge0` and `1.0` if `x >= edge1` and performs smooth Hermite interpolation between 0 and 1 when `edge0 < x < edge1`.\n  // This is useful in cases where you would want a threshold function with a smooth transition. This is equivalent to:\n  //\n  // ```glslx\n  // vec2 t = clamp((x - edge0) / (edge1 - edge0), 0.0, 1.0);\n  // return t * t * (3.0 - 2.0 * t);\n  // ```\n  //\n  // Results are undefined if `edge0 >= edge1`.\n  vec2 smoothstep(float edge0, float edge1, vec2 x);\n  // Returns `0.0` if `x <= edge0` and `1.0` if `x >= edge1` and performs smooth Hermite interpolation between 0 and 1 when `edge0 < x < edge1`.\n  // This is useful in cases where you would want a threshold function with a smooth transition. This is equivalent to:\n  //\n  // ```glslx\n  // vec2 t = clamp((x - edge0) / (edge1 - edge0), 0.0, 1.0);\n  // return t * t * (3.0 - 2.0 * t);\n  // ```\n  //\n  // Results are undefined if `edge0 >= edge1`.\n  vec2 smoothstep(vec2 edge0, vec2 edge1, vec2 x);\n  // Returns `0.0` if `x <= edge0` and `1.0` if `x >= edge1` and performs smooth Hermite interpolation between 0 and 1 when `edge0 < x < edge1`.\n  // This is useful in cases where you would want a threshold function with a smooth transition. This is equivalent to:\n  //\n  // ```glslx\n  // vec3 t = clamp((x - edge0) / (edge1 - edge0), 0.0, 1.0);\n  // return t * t * (3.0 - 2.0 * t);\n  // ```\n  //\n  // Results are undefined if `edge0 >= edge1`.\n  vec3 smoothstep(float edge0, float edge1, vec3 x);\n  // Returns `0.0` if `x <= edge0` and `1.0` if `x >= edge1` and performs smooth Hermite interpolation between 0 and 1 when `edge0 < x < edge1`.\n  // This is useful in cases where you would want a threshold function with a smooth transition. This is equivalent to:\n  //\n  // ```glslx\n  // vec3 t = clamp((x - edge0) / (edge1 - edge0), 0.0, 1.0);\n  // return t * t * (3.0 - 2.0 * t);\n  // ```\n  //\n  // Results are undefined if `edge0 >= edge1`.\n  vec3 smoothstep(vec3 edge0, vec3 edge1, vec3 x);\n  // Returns `0.0` if `x <= edge0` and `1.0` if `x >= edge1` and performs smooth Hermite interpolation between 0 and 1 when `edge0 < x < edge1`.\n  // This is useful in cases where you would want a threshold function with a smooth transition. This is equivalent to:\n  //\n  // ```glslx\n  // vec4 t = clamp((x - edge0) / (edge1 - edge0), 0.0, 1.0);\n  // return t * t * (3.0 - 2.0 * t);\n  // ```\n  //\n  // Results are undefined if `edge0 >= edge1`.\n  vec4 smoothstep(float edge0, float edge1, vec4 x);\n  // Returns `0.0` if `x <= edge0` and `1.0` if `x >= edge1` and performs smooth Hermite interpolation between 0 and 1 when `edge0 < x < edge1`.\n  // This is useful in cases where you would want a threshold function with a smooth transition. This is equivalent to:\n  //\n  // ```glslx\n  // vec4 t = clamp((x - edge0) / (edge1 - edge0), 0.0, 1.0);\n  // return t * t * (3.0 - 2.0 * t);\n  // ```\n  //\n  // Results are undefined if `edge0 >= edge1`.\n  vec4 smoothstep(vec4 edge0, vec4 edge1, vec4 x);\n\n  // Returns `true` if `x` holds a NaN. Returns `false` otherwise.\n  bool isnan(float x);\n  // Returns `true` if `x` holds a NaN. Returns `false` otherwise.\n  bvec2 isnan(vec2 x);\n  // Returns `true` if `x` holds a NaN. Returns `false` otherwise.\n  bvec3 isnan(vec3 x);\n  // Returns `true` if `x` holds a NaN. Returns `false` otherwise.\n  bvec4 isnan(vec4 x);\n\n  // Returns `true` if `x` holds a positive infinity or negative infinity. Returns `false` otherwise.\n  bool isinf(float x);\n  // Returns `true` if `x` holds a positive infinity or negative infinity. Returns `false` otherwise.\n  bvec2 isinf(vec2 x);\n  // Returns `true` if `x` holds a positive infinity or negative infinity. Returns `false` otherwise.\n  bvec3 isinf(vec3 x);\n  // Returns `true` if `x` holds a positive infinity or negative infinity. Returns `false` otherwise.\n  bvec4 isinf(vec4 x);\n\n  // Returns a signed or unsigned `highp` integer value representing the encoding of a floating-point value.\n  // For `highp` floating point, the value's bit level representation is preserved.\n  // For `mediump` and `lowp`, the value is first converted to `highp` floating point and the encoding of that value is returned.\n  int floatBitsToInt(float value);\n  // Returns a signed or unsigned `highp` integer value representing the encoding of a floating-point value.\n  // For `highp` floating point, the value's bit level representation is preserved.\n  // For `mediump` and `lowp`, the value is first converted to `highp` floating point and the encoding of that value is returned.\n  ivec2 floatBitsToInt(vec2 value);\n  // Returns a signed or unsigned `highp` integer value representing the encoding of a floating-point value.\n  // For `highp` floating point, the value's bit level representation is preserved.\n  // For `mediump` and `lowp`, the value is first converted to `highp` floating point and the encoding of that value is returned.\n  ivec3 floatBitsToInt(vec3 value);\n  // Returns a signed or unsigned `highp` integer value representing the encoding of a floating-point value.\n  // For `highp` floating point, the value's bit level representation is preserved.\n  // For `mediump` and `lowp`, the value is first converted to `highp` floating point and the encoding of that value is returned.\n  ivec4 floatBitsToInt(vec4 value);\n\n  // Returns a signed or unsigned `highp` integer value representing the encoding of a floating-point value.\n  // For `highp` floating point, the value's bit level representation is preserved.\n  // For `mediump` and `lowp`, the value is first converted to `highp` floating point and the encoding of that value is returned.\n  uint floatBitsToUint(float value);\n  // Returns a signed or unsigned `highp` integer value representing the encoding of a floating-point value.\n  // For `highp` floating point, the value's bit level representation is preserved.\n  // For `mediump` and `lowp`, the value is first converted to `highp` floating point and the encoding of that value is returned.\n  uvec2 floatBitsToUint(vec2 value);\n  // Returns a signed or unsigned `highp` integer value representing the encoding of a floating-point value.\n  // For `highp` floating point, the value's bit level representation is preserved.\n  // For `mediump` and `lowp`, the value is first converted to `highp` floating point and the encoding of that value is returned.\n  uvec3 floatBitsToUint(vec3 value);\n  // Returns a signed or unsigned `highp` integer value representing the encoding of a floating-point value.\n  // For `highp` floating point, the value's bit level representation is preserved.\n  // For `mediump` and `lowp`, the value is first converted to `highp` floating point and the encoding of that value is returned.\n  uvec4 floatBitsToUint(vec4 value);\n\n  // Returns a `highp` floating-point value corresponding to a signed or unsigned integer encoding of a floating-point\n  // value. If an `inf` or `NaN` is passed in, it will not signal, and the resulting floating point value is unspecified.\n  // Otherwise, the bit-level representation is preserved. For `lowp` and `mediump`, the value is first converted to the\n  // corresponding signed or unsigned `highp` integer and then reinterpreted as a `highp` floating point value as before.\n  float intBitsToFloat(int value);\n  // Returns a `highp` floating-point value corresponding to a signed or unsigned integer encoding of a floating-point\n  // value. If an `inf` or `NaN` is passed in, it will not signal, and the resulting floating point value is unspecified.\n  // Otherwise, the bit-level representation is preserved. For `lowp` and `mediump`, the value is first converted to the\n  // corresponding signed or unsigned `highp` integer and then reinterpreted as a `highp` floating point value as before.\n  vec2 intBitsToFloat(ivec2 value);\n  // Returns a `highp` floating-point value corresponding to a signed or unsigned integer encoding of a floating-point\n  // value. If an `inf` or `NaN` is passed in, it will not signal, and the resulting floating point value is unspecified.\n  // Otherwise, the bit-level representation is preserved. For `lowp` and `mediump`, the value is first converted to the\n  // corresponding signed or unsigned `highp` integer and then reinterpreted as a `highp` floating point value as before.\n  vec3 intBitsToFloat(ivec3 value);\n  // Returns a `highp` floating-point value corresponding to a signed or unsigned integer encoding of a floating-point\n  // value. If an `inf` or `NaN` is passed in, it will not signal, and the resulting floating point value is unspecified.\n  // Otherwise, the bit-level representation is preserved. For `lowp` and `mediump`, the value is first converted to the\n  // corresponding signed or unsigned `highp` integer and then reinterpreted as a `highp` floating point value as before.\n  vec4 intBitsToFloat(ivec4 value);\n\n  // Returns a `highp` floating-point value corresponding to a signed or unsigned integer encoding of a floating-point\n  // value. If an `inf` or `NaN` is passed in, it will not signal, and the resulting floating point value is unspecified.\n  // Otherwise, the bit-level representation is preserved. For `lowp` and `mediump`, the value is first converted to the\n  // corresponding signed or unsigned `highp` integer and then reinterpreted as a `highp` floating point value as before.\n  float intBitsToFloat(uint value);\n  // Returns a `highp` floating-point value corresponding to a signed or unsigned integer encoding of a floating-point\n  // value. If an `inf` or `NaN` is passed in, it will not signal, and the resulting floating point value is unspecified.\n  // Otherwise, the bit-level representation is preserved. For `lowp` and `mediump`, the value is first converted to the\n  // corresponding signed or unsigned `highp` integer and then reinterpreted as a `highp` floating point value as before.\n  vec2 intBitsToFloat(uvec2 value);\n  // Returns a `highp` floating-point value corresponding to a signed or unsigned integer encoding of a floating-point\n  // value. If an `inf` or `NaN` is passed in, it will not signal, and the resulting floating point value is unspecified.\n  // Otherwise, the bit-level representation is preserved. For `lowp` and `mediump`, the value is first converted to the\n  // corresponding signed or unsigned `highp` integer and then reinterpreted as a `highp` floating point value as before.\n  vec3 intBitsToFloat(uvec3 value);\n  // Returns a `highp` floating-point value corresponding to a signed or unsigned integer encoding of a floating-point\n  // value. If an `inf` or `NaN` is passed in, it will not signal, and the resulting floating point value is unspecified.\n  // Otherwise, the bit-level representation is preserved. For `lowp` and `mediump`, the value is first converted to the\n  // corresponding signed or unsigned `highp` integer and then reinterpreted as a `highp` floating point value as before.\n  vec4 intBitsToFloat(uvec4 value);\n\n  ////////////////////////////////////////////////////////////////////////////////\n  // Floating-Point Pack and Unpack Functions\n\n  // First, converts each component of the normalized floating-point value `v` into 16-bit integer values. Then,\n  // the results are packed into the returned 32-bit unsigned integer. The conversion for component `c` of `v` to\n  // fixed point is done as follows:\n  //\n  // ```glslx\n  // packSnorm2x16: round(clamp(c, -1, +1) * 32767.0)\n  // ```\n  //\n  // The first component of the vector will be written to the least significant bits of the output; the last component\n  // will be written to the most significant bits.\n  highp uint packSnorm2x16(vec2 v);\n  // First, unpacks a single 32-bit unsigned integer `p` into a pair of 16-bit signed integers. Then, each component is\n  // converted to a normalized floating-point value to generate the returned two-component vector. The conversion for\n  // unpacked fixed-point value `f` to floating point is done as follows:\n  //\n  // ```glslx\n  // unpackSnorm2x16: clamp(f / 32767.0, -1, +1)\n  //\n  // The first component of the returned vector will be extracted from the least significant bits of the input; the\n  // last component will be extracted from the most significant bits.\n  highp vec2 unpackSnorm2x16(highp uint p);\n\n  // First, converts each component of the normalized floating-point value `v` into 16-bit integer values. Then,\n  // the results are packed into the returned 32-bit unsigned integer. The conversion for component `c` of `v` to\n  // fixed point is done as follows:\n  //\n  // ```glslx\n  // packUnorm2x16: round(clamp(c, 0, +1) * 65535.0)\n  // ```\n  //\n  // The first component of the vector will be written to the least significant bits of the output; the last component\n  // will be written to the most significant bits.\n  highp uint packUnorm2x16(vec2 v);\n  // First, unpacks a single 32-bit unsigned integer `p` into a pair of 16-bit unsigned integers. Then, each component\n  // is converted to a normalized floating-point value to generate the returned two-component vector. The conversion for\n  // unpacked fixed-point value `f` to floating point is done as follows:\n  //\n  // ```glslx\n  // unpackUnorm2x16: f / 65535.0\n  // ```\n  //\n  // The first component of the returned vector will be extracted from the least significant bits of the input; the\n  // last component will be extracted from the most significant bits.\n  highp vec2 unpackUnorm2x16(highp uint p);\n\n  // Returns an unsigned integer obtained by converting the components of a two-component floating-point vector to\n  // the 16-bit floating-point representation found in the OpenGL ES Specification, and then packing these two\n  // 16-bit integers into a 32-bit unsigned integer.\n  //\n  // The first vector component specifies the 16 least-significant bits of the result; the second component\n  // specifies the 16 most-significant bits.\n  highp uint packHalf2x16(mediump vec2 v);\n  // Returns a two-component floating-point vector with components obtained by unpacking a 32-bit unsigned\n  // integer into a pair of 16-bit values, interpreting those values as 16-bit floating-point numbers according to the\n  // OpenGL ES Specification, and converting them to 32-bit floating-point values.\n  //\n  // The first component of the vector is obtained from the 16 least-significant bits of `v`; the second component is\n  // obtained from the 16 most-significant bits of `v`.\n  mediump vec2 unpackHalf2x16(highp uint v);\n\n  ////////////////////////////////////////////////////////////////////////////////\n  // Geometric Functions\n\n  // Returns the length of vector `x`, i.e. `√x²`\n  float length(float x);\n  // Returns the length of vector `x`, i.e. `√x[0]² + x[1]²`\n  float length(vec2 x);\n  // Returns the length of vector `x`, i.e. `√x[0]² + x[1]² + x[2]²`\n  float length(vec3 x);\n  // Returns the length of vector `x`, i.e. `√x[0]² + x[1]² + x[2]² + x[3]²`\n  float length(vec4 x);\n\n  // Returns the distance between `p0` and `p1`, i.e. `length(p0 - p1)`\n  float distance(float p0, float p1);\n  // Returns the distance between `p0` and `p1`, i.e. `length(p0 - p1)`\n  float distance(vec2 p0, vec2 p1);\n  // Returns the distance between `p0` and `p1`, i.e. `length(p0 - p1)`\n  float distance(vec3 p0, vec3 p1);\n  // Returns the distance between `p0` and `p1`, i.e. `length(p0 - p1)`\n  float distance(vec4 p0, vec4 p1);\n\n  // Returns the dot product of `x` and `y`, i.e. `x*y`\n  float dot(float x, float y);\n  // Returns the dot product of `x` and `y`, i.e. `x[0]*y[0] + x[1]*y[1]`\n  float dot(vec2 x, vec2 y);\n  // Returns the dot product of `x` and `y`, i.e. `x[0]*y[0] + x[1]*y[1] + x[2]*y[2]`\n  float dot(vec3 x, vec3 y);\n  // Returns the dot product of `x` and `y`, i.e. `x[0]*y[0] + x[1]*y[1] + x[2]*y[2] + x[3]*y[3]`\n  float dot(vec4 x, vec4 y);\n\n  // Returns the cross product of `x` and `y`, i.e.\n  //\n  // ```glslx\n  // vec3(\n  //   x[1]*y[2] - y[1]*x[2],\n  //   x[2]*y[0] - y[2]*x[0],\n  //   x[0]*y[1] - y[0]*x[1])\n  // ```\n  vec3 cross(vec3 x, vec3 y);\n\n  // Returns a vector in the same direction as `x` but with a length of 1 i.e. `x / length(x)`\n  float normalize(float x);\n  // Returns a vector in the same direction as `x` but with a length of 1 i.e. `x / length(x)`\n  vec2 normalize(vec2 x);\n  // Returns a vector in the same direction as `x` but with a length of 1 i.e. `x / length(x)`\n  vec3 normalize(vec3 x);\n  // Returns a vector in the same direction as `x` but with a length of 1 i.e. `x / length(x)`\n  vec4 normalize(vec4 x);\n\n  // If `dot(Nref, I) < 0` return `N`, otherwise return `-N`\n  float faceforward(float N, float I, float Nref);\n  // If `dot(Nref, I) < 0` return `N`, otherwise return `-N`\n  vec2 faceforward(vec2 N, vec2 I, vec2 Nref);\n  // If `dot(Nref, I) < 0` return `N`, otherwise return `-N`\n  vec3 faceforward(vec3 N, vec3 I, vec3 Nref);\n  // If `dot(Nref, I) < 0` return `N`, otherwise return `-N`\n  vec4 faceforward(vec4 N, vec4 I, vec4 Nref);\n\n  // For the incident vector `I` and surface orientation `N`, returns the reflection direction: `I - 2 * dot(N, I) * N`.\n  // `N` must already be normalized in order to achieve the desired result.\n  float reflect(float I, float N);\n  // For the incident vector `I` and surface orientation `N`, returns the reflection direction: `I - 2 * dot(N, I) * N`.\n  // `N` must already be normalized in order to achieve the desired result.\n  vec2 reflect(vec2 I, vec2 N);\n  // For the incident vector `I` and surface orientation `N`, returns the reflection direction: `I - 2 * dot(N, I) * N`.\n  // `N` must already be normalized in order to achieve the desired result.\n  vec3 reflect(vec3 I, vec3 N);\n  // For the incident vector `I` and surface orientation `N`, returns the reflection direction: `I - 2 * dot(N, I) * N`.\n  // `N` must already be normalized in order to achieve the desired result.\n  vec4 reflect(vec4 I, vec4 N);\n\n  // For the incident vector `I` and surface normal `N`, and the ratio of indices of refraction `eta`, return the refraction vector.\n  // The result is computed by:\n  //\n  // ```glslx\n  // float k = 1.0 - eta * eta * (1.0 - dot(N, I) * dot(N, I));\n  // if (k < 0.0) return float(0.0);\n  // else return eta * I - (eta * dot(N, I) + sqrt(k)) * N;\n  // ```\n  //\n  // The input parameters for the incident vector `I` and the surface normal `N`.\n  float refract(float I, float N, float eta);\n  // For the incident vector `I` and surface normal `N`, and the ratio of indices of refraction `eta`, return the refraction vector.\n  // The result is computed by:\n  //\n  // ```glslx\n  // float k = 1.0 - eta * eta * (1.0 - dot(N, I) * dot(N, I));\n  // if (k < 0.0) return vec2(0.0);\n  // else return eta * I - (eta * dot(N, I) + sqrt(k)) * N;\n  // ```\n  //\n  // The input parameters for the incident vector `I` and the surface normal `N`.\n  vec2 refract(vec2 I, vec2 N, float eta);\n  // For the incident vector `I` and surface normal `N`, and the ratio of indices of refraction `eta`, return the refraction vector.\n  // The result is computed by:\n  //\n  // ```glslx\n  // float k = 1.0 - eta * eta * (1.0 - dot(N, I) * dot(N, I));\n  // if (k < 0.0) return vec3(0.0);\n  // else return eta * I - (eta * dot(N, I) + sqrt(k)) * N;\n  // ```\n  //\n  // The input parameters for the incident vector `I` and the surface normal `N`.\n  vec3 refract(vec3 I, vec3 N, float eta);\n  // For the incident vector `I` and surface normal `N`, and the ratio of indices of refraction `eta`, return the refraction vector.\n  // The result is computed by:\n  //\n  // ```glslx\n  // float k = 1.0 - eta * eta * (1.0 - dot(N, I) * dot(N, I));\n  // if (k < 0.0) return vec4(0.0);\n  // else return eta * I - (eta * dot(N, I) + sqrt(k)) * N;\n  // ```\n  //\n  // The input parameters for the incident vector `I` and the surface normal `N`.\n  vec4 refract(vec4 I, vec4 N, float eta);\n\n  ////////////////////////////////////////////////////////////////////////////////\n  // Matrix Functions\n\n  // Multiply matrix `x` by matrix `y` component-wise, i.e., `result[i][j]` is the scalar product of `x[i][j]` and `y[i][j]`.\n  // Note: to get linear algebraic matrix multiplication, use the multiply operator (`*`).\n  mat2 matrixCompMult(mat2 x, mat2 y);\n  // Multiply matrix `x` by matrix `y` component-wise, i.e., `result[i][j]` is the scalar product of `x[i][j]` and `y[i][j]`.\n  // Note: to get linear algebraic matrix multiplication, use the multiply operator (`*`).\n  mat3 matrixCompMult(mat3 x, mat3 y);\n  // Multiply matrix `x` by matrix `y` component-wise, i.e., `result[i][j]` is the scalar product of `x[i][j]` and `y[i][j]`.\n  // Note: to get linear algebraic matrix multiplication, use the multiply operator (`*`).\n  mat4 matrixCompMult(mat4 x, mat4 y);\n\n  // Treats the first parameter `c` as a column vector (matrix with one column) and the second parameter `r` as a row\n  // vector (matrix with one row) and does a linear algebraic matrix multiply `c * r`, yielding a matrix whose number of\n  // rows is the number of components in `c` and whose number of columns is the number of components in `r`.\n  mat2 outerProduct(vec2 c, vec2 r);\n  // Treats the first parameter `c` as a column vector (matrix with one column) and the second parameter `r` as a row\n  // vector (matrix with one row) and does a linear algebraic matrix multiply `c * r`, yielding a matrix whose number of\n  // rows is the number of components in `c` and whose number of columns is the number of components in `r`.\n  mat3 outerProduct(vec3 c, vec3 r);\n  // Treats the first parameter `c` as a column vector (matrix with one column) and the second parameter `r` as a row\n  // vector (matrix with one row) and does a linear algebraic matrix multiply `c * r`, yielding a matrix whose number of\n  // rows is the number of components in `c` and whose number of columns is the number of components in `r`.\n  mat4 outerProduct(vec4 c, vec4 r);\n  // Treats the first parameter `c` as a column vector (matrix with one column) and the second parameter `r` as a row\n  // vector (matrix with one row) and does a linear algebraic matrix multiply `c * r`, yielding a matrix whose number of\n  // rows is the number of components in `c` and whose number of columns is the number of components in `r`.\n  mat2x3 outerProduct(vec3 c, vec2 r);\n  // Treats the first parameter `c` as a column vector (matrix with one column) and the second parameter `r` as a row\n  // vector (matrix with one row) and does a linear algebraic matrix multiply `c * r`, yielding a matrix whose number of\n  // rows is the number of components in `c` and whose number of columns is the number of components in `r`.\n  mat3x2 outerProduct(vec2 c, vec3 r);\n  // Treats the first parameter `c` as a column vector (matrix with one column) and the second parameter `r` as a row\n  // vector (matrix with one row) and does a linear algebraic matrix multiply `c * r`, yielding a matrix whose number of\n  // rows is the number of components in `c` and whose number of columns is the number of components in `r`.\n  mat2x4 outerProduct(vec4 c, vec2 r);\n  // Treats the first parameter `c` as a column vector (matrix with one column) and the second parameter `r` as a row\n  // vector (matrix with one row) and does a linear algebraic matrix multiply `c * r`, yielding a matrix whose number of\n  // rows is the number of components in `c` and whose number of columns is the number of components in `r`.\n  mat4x2 outerProduct(vec2 c, vec4 r);\n  // Treats the first parameter `c` as a column vector (matrix with one column) and the second parameter `r` as a row\n  // vector (matrix with one row) and does a linear algebraic matrix multiply `c * r`, yielding a matrix whose number of\n  // rows is the number of components in `c` and whose number of columns is the number of components in `r`.\n  mat3x4 outerProduct(vec4 c, vec3 r);\n  // Treats the first parameter `c` as a column vector (matrix with one column) and the second parameter `r` as a row\n  // vector (matrix with one row) and does a linear algebraic matrix multiply `c * r`, yielding a matrix whose number of\n  // rows is the number of components in `c` and whose number of columns is the number of components in `r`.\n  mat4x3 outerProduct(vec3 c, vec4 r);\n\n  // Returns a matrix that is the transpose of `m`. The input matrix `m` is not modified.\n  mat2 transpose(mat2 m);\n  // Returns a matrix that is the transpose of `m`. The input matrix `m` is not modified.\n  mat3 transpose(mat3 m);\n  // Returns a matrix that is the transpose of `m`. The input matrix `m` is not modified.\n  mat4 transpose(mat4 m);\n  // Returns a matrix that is the transpose of `m`. The input matrix `m` is not modified.\n  mat2x3 transpose(mat3x2 m);\n  // Returns a matrix that is the transpose of `m`. The input matrix `m` is not modified.\n  mat3x2 transpose(mat2x3 m);\n  // Returns a matrix that is the transpose of `m`. The input matrix `m` is not modified.\n  mat2x4 transpose(mat4x2 m);\n  // Returns a matrix that is the transpose of `m`. The input matrix `m` is not modified.\n  mat4x2 transpose(mat2x4 m);\n  // Returns a matrix that is the transpose of `m`. The input matrix `m` is not modified.\n  mat3x4 transpose(mat4x3 m);\n  // Returns a matrix that is the transpose of `m`. The input matrix `m` is not modified.\n  mat4x3 transpose(mat3x4 m);\n\n  // Returns the determinant of `m`.\n  float determinant(mat2 m);\n  // Returns the determinant of `m`.\n  float determinant(mat3 m);\n  // Returns the determinant of `m`.\n  float determinant(mat4 m);\n\n  // Returns a matrix that is the inverse of `m`. The input matrix `m` is not modified. The values in\n  // the returned matrix are undefined if `m` is singular or poorly-conditioned (nearly singular).\n  mat2 inverse(mat2 m);\n  // Returns a matrix that is the inverse of `m`. The input matrix `m` is not modified. The values in\n  // the returned matrix are undefined if `m` is singular or poorly-conditioned (nearly singular).\n  mat3 inverse(mat3 m);\n  // Returns a matrix that is the inverse of `m`. The input matrix `m` is not modified. The values in\n  // the returned matrix are undefined if `m` is singular or poorly-conditioned (nearly singular).\n  mat4 inverse(mat4 m);\n\n  ////////////////////////////////////////////////////////////////////////////////\n  // Vector Relational Functions\n\n  // Returns the component-wise compare of `x < y`.\n  bvec2 lessThan(vec2 x, vec2 y);\n  // Returns the component-wise compare of `x < y`.\n  bvec2 lessThan(ivec2 x, ivec2 y);\n  // Returns the component-wise compare of `x < y`.\n  bvec2 lessThan(uvec2 x, uvec2 y);\n  // Returns the component-wise compare of `x < y`.\n  bvec3 lessThan(vec3 x, vec3 y);\n  // Returns the component-wise compare of `x < y`.\n  bvec3 lessThan(ivec3 x, ivec3 y);\n  // Returns the component-wise compare of `x < y`.\n  bvec3 lessThan(uvec3 x, uvec3 y);\n  // Returns the component-wise compare of `x < y`.\n  bvec4 lessThan(vec4 x, vec4 y);\n  // Returns the component-wise compare of `x < y`.\n  bvec4 lessThan(ivec4 x, ivec4 y);\n  // Returns the component-wise compare of `x < y`.\n  bvec4 lessThan(uvec4 x, uvec4 y);\n\n  // Returns the component-wise compare of `x <= y`.\n  bvec2 lessThanEqual(vec2 x, vec2 y);\n  // Returns the component-wise compare of `x <= y`.\n  bvec2 lessThanEqual(ivec2 x, ivec2 y);\n  // Returns the component-wise compare of `x <= y`.\n  bvec2 lessThanEqual(uvec2 x, uvec2 y);\n  // Returns the component-wise compare of `x <= y`.\n  bvec3 lessThanEqual(vec3 x, vec3 y);\n  // Returns the component-wise compare of `x <= y`.\n  bvec3 lessThanEqual(ivec3 x, ivec3 y);\n  // Returns the component-wise compare of `x <= y`.\n  bvec3 lessThanEqual(uvec3 x, uvec3 y);\n  // Returns the component-wise compare of `x <= y`.\n  bvec4 lessThanEqual(vec4 x, vec4 y);\n  // Returns the component-wise compare of `x <= y`.\n  bvec4 lessThanEqual(ivec4 x, ivec4 y);\n  // Returns the component-wise compare of `x <= y`.\n  bvec4 lessThanEqual(uvec4 x, uvec4 y);\n\n  // Returns the component-wise compare of `x > y`.\n  bvec2 greaterThan(vec2 x, vec2 y);\n  // Returns the component-wise compare of `x > y`.\n  bvec2 greaterThan(ivec2 x, ivec2 y);\n  // Returns the component-wise compare of `x > y`.\n  bvec2 greaterThan(uvec2 x, uvec2 y);\n  // Returns the component-wise compare of `x > y`.\n  bvec3 greaterThan(vec3 x, vec3 y);\n  // Returns the component-wise compare of `x > y`.\n  bvec3 greaterThan(ivec3 x, ivec3 y);\n  // Returns the component-wise compare of `x > y`.\n  bvec3 greaterThan(uvec3 x, uvec3 y);\n  // Returns the component-wise compare of `x > y`.\n  bvec4 greaterThan(vec4 x, vec4 y);\n  // Returns the component-wise compare of `x > y`.\n  bvec4 greaterThan(ivec4 x, ivec4 y);\n  // Returns the component-wise compare of `x > y`.\n  bvec4 greaterThan(uvec4 x, uvec4 y);\n\n  // Returns the component-wise compare of `x >= y`.\n  bvec2 greaterThanEqual(vec2 x, vec2 y);\n  // Returns the component-wise compare of `x >= y`.\n  bvec2 greaterThanEqual(ivec2 x, ivec2 y);\n  // Returns the component-wise compare of `x >= y`.\n  bvec2 greaterThanEqual(uvec2 x, uvec2 y);\n  // Returns the component-wise compare of `x >= y`.\n  bvec3 greaterThanEqual(vec3 x, vec3 y);\n  // Returns the component-wise compare of `x >= y`.\n  bvec3 greaterThanEqual(ivec3 x, ivec3 y);\n  // Returns the component-wise compare of `x >= y`.\n  bvec3 greaterThanEqual(uvec3 x, uvec3 y);\n  // Returns the component-wise compare of `x >= y`.\n  bvec4 greaterThanEqual(vec4 x, vec4 y);\n  // Returns the component-wise compare of `x >= y`.\n  bvec4 greaterThanEqual(ivec4 x, ivec4 y);\n  // Returns the component-wise compare of `x >= y`.\n  bvec4 greaterThanEqual(uvec4 x, uvec4 y);\n\n  // Returns the component-wise compare of `x == y`.\n  bvec2 equal(vec2 x, vec2 y);\n  // Returns the component-wise compare of `x == y`.\n  bvec2 equal(ivec2 x, ivec2 y);\n  // Returns the component-wise compare of `x == y`.\n  bvec2 equal(uvec2 x, uvec2 y);\n  // Returns the component-wise compare of `x == y`.\n  bvec2 equal(bvec2 x, bvec2 y);\n  // Returns the component-wise compare of `x == y`.\n  bvec3 equal(vec3 x, vec3 y);\n  // Returns the component-wise compare of `x == y`.\n  bvec3 equal(ivec3 x, ivec3 y);\n  // Returns the component-wise compare of `x == y`.\n  bvec3 equal(uvec3 x, uvec3 y);\n  // Returns the component-wise compare of `x == y`.\n  bvec3 equal(bvec3 x, bvec3 y);\n  // Returns the component-wise compare of `x == y`.\n  bvec4 equal(vec4 x, vec4 y);\n  // Returns the component-wise compare of `x == y`.\n  bvec4 equal(ivec4 x, ivec4 y);\n  // Returns the component-wise compare of `x == y`.\n  bvec4 equal(uvec4 x, uvec4 y);\n  // Returns the component-wise compare of `x == y`.\n  bvec4 equal(bvec4 x, bvec4 y);\n\n  // Returns the component-wise compare of `x != y`.\n  bvec2 notEqual(vec2 x, vec2 y);\n  // Returns the component-wise compare of `x != y`.\n  bvec2 notEqual(ivec2 x, ivec2 y);\n  // Returns the component-wise compare of `x != y`.\n  bvec2 notEqual(uvec2 x, uvec2 y);\n  // Returns the component-wise compare of `x != y`.\n  bvec2 notEqual(bvec2 x, bvec2 y);\n  // Returns the component-wise compare of `x != y`.\n  bvec3 notEqual(vec3 x, vec3 y);\n  // Returns the component-wise compare of `x != y`.\n  bvec3 notEqual(ivec3 x, ivec3 y);\n  // Returns the component-wise compare of `x != y`.\n  bvec3 notEqual(uvec3 x, uvec3 y);\n  // Returns the component-wise compare of `x != y`.\n  bvec3 notEqual(bvec3 x, bvec3 y);\n  // Returns the component-wise compare of `x != y`.\n  bvec4 notEqual(vec4 x, vec4 y);\n  // Returns the component-wise compare of `x != y`.\n  bvec4 notEqual(ivec4 x, ivec4 y);\n  // Returns the component-wise compare of `x != y`.\n  bvec4 notEqual(uvec4 x, uvec4 y);\n  // Returns the component-wise compare of `x != y`.\n  bvec4 notEqual(bvec4 x, bvec4 y);\n\n  // Returns true if any component of `x` is `true`.\n  bool any(bvec2 x);\n  // Returns true if any component of `x` is `true`.\n  bool any(bvec3 x);\n  // Returns true if any component of `x` is `true`.\n  bool any(bvec4 x);\n\n  // Returns true only if all components of `x` are `true`.\n  bool all(bvec2 x);\n  // Returns true only if all components of `x` are `true`.\n  bool all(bvec3 x);\n  // Returns true only if all components of `x` are `true`.\n  bool all(bvec4 x);\n\n  // Returns the component-wise logical complement of `x`.\n  bvec2 not(bvec2 x);\n  // Returns the component-wise logical complement of `x`.\n  bvec3 not(bvec3 x);\n  // Returns the component-wise logical complement of `x`.\n  bvec4 not(bvec4 x);\n\n  ////////////////////////////////////////////////////////////////////////////////\n  // Texture Lookup Functions\n\n  // Returns the dimensions of level `lod` for the texture bound to `sampler`.\n  highp ivec2 textureSize(sampler2D sampler, int lod);\n  // Returns the dimensions of level `lod` for the texture bound to `sampler`.\n  highp ivec2 textureSize(isampler2D sampler, int lod);\n  // Returns the dimensions of level `lod` for the texture bound to `sampler`.\n  highp ivec2 textureSize(usampler2D sampler, int lod);\n  // Returns the dimensions of level `lod` for the texture bound to `sampler`.\n  highp ivec3 textureSize(sampler3D sampler, int lod);\n  // Returns the dimensions of level `lod` for the texture bound to `sampler`.\n  highp ivec3 textureSize(isampler3D sampler, int lod);\n  // Returns the dimensions of level `lod` for the texture bound to `sampler`.\n  highp ivec3 textureSize(usampler3D sampler, int lod);\n  // Returns the dimensions of level `lod` for the texture bound to `sampler`.\n  highp ivec2 textureSize(samplerCube sampler, int lod);\n  // Returns the dimensions of level `lod` for the texture bound to `sampler`.\n  highp ivec2 textureSize(isamplerCube sampler, int lod);\n  // Returns the dimensions of level `lod` for the texture bound to `sampler`.\n  highp ivec2 textureSize(usamplerCube sampler, int lod);\n  // Returns the dimensions of level `lod` for the texture bound to `sampler`.\n  highp ivec2 textureSize(sampler2DShadow sampler, int lod);\n  // Returns the dimensions of level `lod` for the texture bound to `sampler`.\n  highp ivec2 textureSize(samplerCubeShadow sampler, int lod);\n  // Returns the dimensions of level `lod` for the texture bound to `sampler`.\n  highp ivec3 textureSize(sampler2DArray sampler, int lod);\n  // Returns the dimensions of level `lod` for the texture bound to `sampler`.\n  highp ivec3 textureSize(isampler2DArray sampler, int lod);\n  // Returns the dimensions of level `lod` for the texture bound to `sampler`.\n  highp ivec3 textureSize(usampler2DArray sampler, int lod);\n  // Returns the dimensions of level `lod` for the texture bound to `sampler`.\n  highp ivec3 textureSize(sampler2DArrayShadow sampler, int lod);\n\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  vec4 texture(sampler2D sampler, vec2 P);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  ivec4 texture(isampler2D sampler, vec2 P);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  uvec4 texture(usampler2D sampler, vec2 P);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  vec4 texture(sampler2D sampler, vec2 P, float bias);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  ivec4 texture(isampler2D sampler, vec2 P, float bias);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  uvec4 texture(usampler2D sampler, vec2 P, float bias);\n\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  vec4 texture(sampler3D sampler, vec3 P);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  ivec4 texture(isampler3D sampler, vec3 P);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  uvec4 texture(usampler3D sampler, vec3 P);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  vec4 texture(sampler3D sampler, vec3 P, float bias);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  ivec4 texture(isampler3D sampler, vec3 P, float bias);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  uvec4 texture(usampler3D sampler, vec3 P, float bias);\n\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  vec4 texture(samplerCube sampler, vec3 P);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  ivec4 texture(isamplerCube sampler, vec3 P);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  uvec4 texture(usamplerCube sampler, vec3 P);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  vec4 texture(samplerCube sampler, vec3 P, float bias);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  ivec4 texture(isamplerCube sampler, vec3 P, float bias);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  uvec4 texture(usamplerCube sampler, vec3 P, float bias);\n\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  float texture(sampler2DShadow sampler, vec3 P);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  float texture(sampler2DShadow sampler, vec3 P, float bias);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  float texture(samplerCubeShadow sampler, vec4 P);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  float texture(samplerCubeShadow sampler, vec4 P, float bias);\n\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  vec4 texture(sampler2DArray sampler, vec3 P);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  ivec4 texture(isampler2DArray sampler, vec3 P);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  uvec4 texture(usampler2DArray sampler, vec3 P);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  vec4 texture(sampler2DArray sampler, vec3 P, float bias);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  ivec4 texture(isampler2DArray sampler, vec3 P, float bias);\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  uvec4 texture(usampler2DArray sampler, vec3 P, float bias);\n\n  // Use the texture coordinate `P` to do a texture lookup in the texture currently bound to `sampler`.\n  float texture(sampler2DArrayShadow sampler, vec4 P);\n\n  // Do a texture lookup with projection. The texture coordinates consumed from `P`, not including the last\n  // component of `P`, are divided by the last component of `P` to form projected coordinates `P'`.\n  vec4 textureProj(sampler2D sampler, vec3 P);\n  // Do a texture lookup with projection. The texture coordinates consumed from `P`, not including the last\n  // component of `P`, are divided by the last component of `P` to form projected coordinates `P'`.\n  ivec4 textureProj(isampler2D sampler, vec3 P);\n  // Do a texture lookup with projection. The texture coordinates consumed from `P`, not including the last\n  // component of `P`, are divided by the last component of `P` to form projected coordinates `P'`.\n  uvec4 textureProj(usampler2D sampler, vec3 P);\n  // Do a texture lookup with projection. The texture coordinates consumed from `P`, not including the last\n  // component of `P`, are divided by the last component of `P` to form projected coordinates `P'`.\n  vec4 textureProj(sampler2D sampler, vec3 P, float bias);\n  // Do a texture lookup with projection. The texture coordinates consumed from `P`, not including the last\n  // component of `P`, are divided by the last component of `P` to form projected coordinates `P'`.\n  ivec4 textureProj(isampler2D sampler, vec3 P, float bias);\n  // Do a texture lookup with projection. The texture coordinates consumed from `P`, not including the last\n  // component of `P`, are divided by the last component of `P` to form projected coordinates `P'`.\n  uvec4 textureProj(usampler2D sampler, vec3 P, float bias);\n\n  // Do a texture lookup with projection. The texture coordinates consumed from `P`, not including the last\n  // component of `P`, are divided by the last component of `P` to form projected coordinates `P'`.\n  vec4 textureProj(sampler2D sampler, vec4 P);\n  // Do a texture lookup with projection. The texture coordinates consumed from `P`, not including the last\n  // component of `P`, are divided by the last component of `P` to form projected coordinates `P'`.\n  ivec4 textureProj(isampler2D sampler, vec4 P);\n  // Do a texture lookup with projection. The texture coordinates consumed from `P`, not including the last\n  // component of `P`, are divided by the last component of `P` to form projected coordinates `P'`.\n  uvec4 textureProj(usampler2D sampler, vec4 P);\n  // Do a texture lookup with projection. The texture coordinates consumed from `P`, not including the last\n  // component of `P`, are divided by the last component of `P` to form projected coordinates `P'`.\n  vec4 textureProj(sampler2D sampler, vec4 P, float bias);\n  // Do a texture lookup with projection. The texture coordinates consumed from `P`, not including the last\n  // component of `P`, are divided by the last component of `P` to form projected coordinates `P'`.\n  ivec4 textureProj(isampler2D sampler, vec4 P, float bias);\n  // Do a texture lookup with projection. The texture coordinates consumed from `P`, not including the last\n  // component of `P`, are divided by the last component of `P` to form projected coordinates `P'`.\n  uvec4 textureProj(usampler2D sampler, vec4 P, float bias);\n\n  // Do a texture lookup with projection. The texture coordinates consumed from `P`, not including the last\n  // component of `P`, are divided by the last component of `P` to form projected coordinates `P'`.\n  vec4 textureProj(sampler3D sampler, vec4 P);\n  // Do a texture lookup with projection. The texture coordinates consumed from `P`, not including the last\n  // component of `P`, are divided by the last component of `P` to form projected coordinates `P'`.\n  ivec4 textureProj(isampler3D sampler, vec4 P);\n  // Do a texture lookup with projection. The texture coordinates consumed from `P`, not including the last\n  // component of `P`, are divided by the last component of `P` to form projected coordinates `P'`.\n  uvec4 textureProj(usampler3D sampler, vec4 P);\n  // Do a texture lookup with projection. The texture coordinates consumed from `P`, not including the last\n  // component of `P`, are divided by the last component of `P` to form projected coordinates `P'`.\n  vec4 textureProj(sampler3D sampler, vec4 P, float bias);\n  // Do a texture lookup with projection. The texture coordinates consumed from `P`, not including the last\n  // component of `P`, are divided by the last component of `P` to form projected coordinates `P'`.\n  ivec4 textureProj(isampler3D sampler, vec4 P, float bias);\n  // Do a texture lookup with projection. The texture coordinates consumed from `P`, not including the last\n  // component of `P`, are divided by the last component of `P` to form projected coordinates `P'`.\n  uvec4 textureProj(usampler3D sampler, vec4 P, float bias);\n\n  // Do a texture lookup with projection. The texture coordinates consumed from `P`, not including the last\n  // component of `P`, are divided by the last component of `P` to form projected coordinates `P'`.\n  float textureProj(sampler2DShadow sampler, vec4 P);\n  // Do a texture lookup with projection. The texture coordinates consumed from `P`, not including the last\n  // component of `P`, are divided by the last component of `P` to form projected coordinates `P'`.\n  float textureProj(sampler2DShadow sampler, vec4 P, float bias);\n\n  // Do a texture lookup as in `texture` but with explicit LOD; `lod` specifies `λbase` and sets the partial derivatives.\n  vec4 textureLod(sampler2D sampler, vec2 P, float lod);\n  // Do a texture lookup as in `texture` but with explicit LOD; `lod` specifies `λbase` and sets the partial derivatives.\n  ivec4 textureLod(isampler2D sampler, vec2 P, float lod);\n  // Do a texture lookup as in `texture` but with explicit LOD; `lod` specifies `λbase` and sets the partial derivatives.\n  uvec4 textureLod(usampler2D sampler, vec2 P, float lod);\n  // Do a texture lookup as in `texture` but with explicit LOD; `lod` specifies `λbase` and sets the partial derivatives.\n  vec4 textureLod(sampler3D sampler, vec3 P, float lod);\n  // Do a texture lookup as in `texture` but with explicit LOD; `lod` specifies `λbase` and sets the partial derivatives.\n  ivec4 textureLod(isampler3D sampler, vec3 P, float lod);\n  // Do a texture lookup as in `texture` but with explicit LOD; `lod` specifies `λbase` and sets the partial derivatives.\n  uvec4 textureLod(usampler3D sampler, vec3 P, float lod);\n  // Do a texture lookup as in `texture` but with explicit LOD; `lod` specifies `λbase` and sets the partial derivatives.\n  vec4 textureLod(samplerCube sampler, vec3 P, float lod);\n  // Do a texture lookup as in `texture` but with explicit LOD; `lod` specifies `λbase` and sets the partial derivatives.\n  ivec4 textureLod(isamplerCube sampler, vec3 P, float lod);\n  // Do a texture lookup as in `texture` but with explicit LOD; `lod` specifies `λbase` and sets the partial derivatives.\n  uvec4 textureLod(usamplerCube sampler, vec3 P, float lod);\n  // Do a texture lookup as in `texture` but with explicit LOD; `lod` specifies `λbase` and sets the partial derivatives.\n  float textureLod(sampler2DShadow sampler, vec3 P, float lod);\n  // Do a texture lookup as in `texture` but with explicit LOD; `lod` specifies `λbase` and sets the partial derivatives.\n  vec4 textureLod(sampler2DArray sampler, vec3 P, float lod);\n  // Do a texture lookup as in `texture` but with explicit LOD; `lod` specifies `λbase` and sets the partial derivatives.\n  ivec4 textureLod(isampler2DArray sampler, vec3 P, float lod);\n  // Do a texture lookup as in `texture` but with explicit LOD; `lod` specifies `λbase` and sets the partial derivatives.\n  uvec4 textureLod(usampler2DArray sampler, vec3 P, float lod);\n\n  // Do a texture lookup as in texture but with `offset` added to the `(u,v)` texel coordinates\n  // before looking up each texel. The offset value must be a constant expression.\n  vec4 textureOffset(sampler2D sampler, vec2 P, ivec2 offset);\n  // Do a texture lookup as in texture but with `offset` added to the `(u,v)` texel coordinates\n  // before looking up each texel. The offset value must be a constant expression.\n  ivec4 textureOffset(isampler2D sampler, vec2 P, ivec2 offset);\n  // Do a texture lookup as in texture but with `offset` added to the `(u,v)` texel coordinates\n  // before looking up each texel. The offset value must be a constant expression.\n  uvec4 textureOffset(usampler2D sampler, vec2 P, ivec2 offset);\n  // Do a texture lookup as in texture but with `offset` added to the `(u,v)` texel coordinates\n  // before looking up each texel. The offset value must be a constant expression.\n  vec4 textureOffset(sampler2D sampler, vec2 P, ivec2 offset, float bias);\n  // Do a texture lookup as in texture but with `offset` added to the `(u,v)` texel coordinates\n  // before looking up each texel. The offset value must be a constant expression.\n  ivec4 textureOffset(isampler2D sampler, vec2 P, ivec2 offset, float bias);\n  // Do a texture lookup as in texture but with `offset` added to the `(u,v)` texel coordinates\n  // before looking up each texel. The offset value must be a constant expression.\n  uvec4 textureOffset(usampler2D sampler, vec2 P, ivec2 offset, float bias);\n\n  // Do a texture lookup as in texture but with `offset` added to the `(u,v,w)` texel coordinates\n  // before looking up each texel. The offset value must be a constant expression.\n  vec4 textureOffset(sampler3D sampler, vec3 P, ivec3 offset);\n  // Do a texture lookup as in texture but with `offset` added to the `(u,v,w)` texel coordinates\n  // before looking up each texel. The offset value must be a constant expression.\n  ivec4 textureOffset(isampler3D sampler, vec3 P, ivec3 offset);\n  // Do a texture lookup as in texture but with `offset` added to the `(u,v,w)` texel coordinates\n  // before looking up each texel. The offset value must be a constant expression.\n  uvec4 textureOffset(usampler3D sampler, vec3 P, ivec3 offset);\n  // Do a texture lookup as in texture but with `offset` added to the `(u,v,w)` texel coordinates\n  // before looking up each texel. The offset value must be a constant expression.\n  vec4 textureOffset(sampler3D sampler, vec3 P, ivec3 offset, float bias);\n  // Do a texture lookup as in texture but with `offset` added to the `(u,v,w)` texel coordinates\n  // before looking up each texel. The offset value must be a constant expression.\n  ivec4 textureOffset(isampler3D sampler, vec3 P, ivec3 offset, float bias);\n  // Do a texture lookup as in texture but with `offset` added to the `(u,v,w)` texel coordinates\n  // before looking up each texel. The offset value must be a constant expression.\n  uvec4 textureOffset(usampler3D sampler, vec3 P, ivec3 offset, float bias);\n\n  // Do a texture lookup as in texture but with `offset` added to the `(u,v,w)` texel coordinates\n  // before looking up each texel. The offset value must be a constant expression.\n  float textureOffset(sampler2DShadow sampler, vec3 P, ivec2 offset);\n  // Do a texture lookup as in texture but with `offset` added to the `(u,v,w)` texel coordinates\n  // before looking up each texel. The offset value must be a constant expression.\n  float textureOffset(sampler2DShadow sampler, vec3 P, ivec2 offset, float bias);\n\n  // Do a texture lookup as in texture but with `offset` added to the `(u,v,w)` texel coordinates\n  // before looking up each texel. The offset value must be a constant expression. Note that\n  // `offset` does not apply to the layer coordinate for texture arrays.\n  vec4 textureOffset(sampler2DArray sampler, vec3 P, ivec2 offset);\n  // Do a texture lookup as in texture but with `offset` added to the `(u,v,w)` texel coordinates\n  // before looking up each texel. The offset value must be a constant expression. Note that\n  // `offset` does not apply to the layer coordinate for texture arrays.\n  ivec4 textureOffset(isampler2DArray sampler, vec3 P, ivec2 offset);\n  // Do a texture lookup as in texture but with `offset` added to the `(u,v,w)` texel coordinates\n  // before looking up each texel. The offset value must be a constant expression. Note that\n  // `offset` does not apply to the layer coordinate for texture arrays.\n  uvec4 textureOffset(usampler2DArray sampler, vec3 P, ivec2 offset);\n  // Do a texture lookup as in texture but with `offset` added to the `(u,v,w)` texel coordinates\n  // before looking up each texel. The offset value must be a constant expression. Note that\n  // `offset` does not apply to the layer coordinate for texture arrays.\n  vec4 textureOffset(sampler2DArray sampler, vec3 P, ivec2 offset, float bias);\n  // Do a texture lookup as in texture but with `offset` added to the `(u,v,w)` texel coordinates\n  // before looking up each texel. The offset value must be a constant expression. Note that\n  // `offset` does not apply to the layer coordinate for texture arrays.\n  ivec4 textureOffset(isampler2DArray sampler, vec3 P, ivec2 offset, float bias);\n  // Do a texture lookup as in texture but with `offset` added to the `(u,v,w)` texel coordinates\n  // before looking up each texel. The offset value must be a constant expression. Note that\n  // `offset` does not apply to the layer coordinate for texture arrays.\n  uvec4 textureOffset(usampler2DArray sampler, vec3 P, ivec2 offset, float bias);\n\n  // Use integer texture coordinate `P` to lookup a single texel from sampler.\n  vec4 texelFetch(sampler2D sampler, ivec2 P, int lod);\n  // Use integer texture coordinate `P` to lookup a single texel from sampler.\n  ivec4 texelFetch(isampler2D sampler, ivec2 P, int lod);\n  // Use integer texture coordinate `P` to lookup a single texel from sampler.\n  uvec4 texelFetch(usampler2D sampler, ivec2 P, int lod);\n  // Use integer texture coordinate `P` to lookup a single texel from sampler.\n  vec4 texelFetch(sampler3D sampler, ivec3 P, int lod);\n  // Use integer texture coordinate `P` to lookup a single texel from sampler.\n  ivec4 texelFetch(isampler3D sampler, ivec3 P, int lod);\n  // Use integer texture coordinate `P` to lookup a single texel from sampler.\n  uvec4 texelFetch(usampler3D sampler, ivec3 P, int lod);\n  // Use integer texture coordinate `P` to lookup a single texel from sampler.\n  vec4 texelFetch(sampler2DArray sampler, ivec3 P, int lod);\n  // Use integer texture coordinate `P` to lookup a single texel from sampler.\n  ivec4 texelFetch(isampler2DArray sampler, ivec3 P, int lod);\n  // Use integer texture coordinate `P` to lookup a single texel from sampler.\n  uvec4 texelFetch(usampler2DArray sampler, ivec3 P, int lod);\n\n  // Fetch a single texel as in `texelFetch` offset by `offset` as described in `textureOffset`.\n  vec4 texelFetchOffset(sampler2D sampler, ivec2 P, int lod, ivec2 offset);\n  // Fetch a single texel as in `texelFetch` offset by `offset` as described in `textureOffset`.\n  ivec4 texelFetchOffset(isampler2D sampler, ivec2 P, int lod, ivec2 offset);\n  // Fetch a single texel as in `texelFetch` offset by `offset` as described in `textureOffset`.\n  uvec4 texelFetchOffset(usampler2D sampler, ivec2 P, int lod, ivec2 offset);\n  // Fetch a single texel as in `texelFetch` offset by `offset` as described in `textureOffset`.\n  vec4 texelFetchOffset(sampler3D sampler, ivec3 P, int lod, ivec3 offset);\n  // Fetch a single texel as in `texelFetch` offset by `offset` as described in `textureOffset`.\n  ivec4 texelFetchOffset(isampler3D sampler, ivec3 P, int lod, ivec3 offset);\n  // Fetch a single texel as in `texelFetch` offset by `offset` as described in `textureOffset`.\n  uvec4 texelFetchOffset(usampler3D sampler, ivec3 P, int lod, ivec3 offset);\n  // Fetch a single texel as in `texelFetch` offset by `offset` as described in `textureOffset`.\n  vec4 texelFetchOffset(sampler2DArray sampler, ivec3 P, int lod, ivec2 offset);\n  // Fetch a single texel as in `texelFetch` offset by `offset` as described in `textureOffset`.\n  ivec4 texelFetchOffset(isampler2DArray sampler, ivec3 P, int lod, ivec2 offset);\n  // Fetch a single texel as in `texelFetch` offset by `offset` as described in `textureOffset`.\n  uvec4 texelFetchOffset(usampler2DArray sampler, ivec3 P, int lod, ivec2 offset);\n\n// Do a projective texture lookup as described in `textureProj` offset by `offset` as described in `textureOffset`.\n  vec4 textureProjOffset(sampler2D sampler, vec3 P, ivec2 offset);\n  // Do a projective texture lookup as described in `textureProj` offset by `offset` as described in `textureOffset`.\n  ivec4 textureProjOffset(isampler2D sampler, vec3 P, ivec2 offset);\n  // Do a projective texture lookup as described in `textureProj` offset by `offset` as described in `textureOffset`.\n  uvec4 textureProjOffset(usampler2D sampler, vec3 P, ivec2 offset);\n  // Do a projective texture lookup as described in `textureProj` offset by `offset` as described in `textureOffset`.\n  vec4 textureProjOffset(sampler2D sampler, vec3 P, ivec2 offset, float bias);\n  // Do a projective texture lookup as described in `textureProj` offset by `offset` as described in `textureOffset`.\n  ivec4 textureProjOffset(isampler2D sampler, vec3 P, ivec2 offset, float bias);\n  // Do a projective texture lookup as described in `textureProj` offset by `offset` as described in `textureOffset`.\n  uvec4 textureProjOffset(usampler2D sampler, vec3 P, ivec2 offset, float bias);\n  // Do a projective texture lookup as described in `textureProj` offset by `offset` as described in `textureOffset`.\n  vec4 textureProjOffset(sampler2D sampler, vec4 P, ivec2 offset);\n  // Do a projective texture lookup as described in `textureProj` offset by `offset` as described in `textureOffset`.\n  ivec4 textureProjOffset(isampler2D sampler, vec4 P, ivec2 offset);\n  // Do a projective texture lookup as described in `textureProj` offset by `offset` as described in `textureOffset`.\n  uvec4 textureProjOffset(usampler2D sampler, vec4 P, ivec2 offset);\n  // Do a projective texture lookup as described in `textureProj` offset by `offset` as described in `textureOffset`.\n  vec4 textureProjOffset(sampler2D sampler, vec4 P, ivec2 offset, float bias);\n  // Do a projective texture lookup as described in `textureProj` offset by `offset` as described in `textureOffset`.\n  ivec4 textureProjOffset(isampler2D sampler, vec4 P, ivec2 offset, float bias);\n  // Do a projective texture lookup as described in `textureProj` offset by `offset` as described in `textureOffset`.\n  uvec4 textureProjOffset(usampler2D sampler, vec4 P, ivec2 offset, float bias);\n  // Do a projective texture lookup as described in `textureProj` offset by `offset` as described in `textureOffset`.\n  vec4 textureProjOffset(sampler3D sampler, vec4 P, ivec3 offset);\n  // Do a projective texture lookup as described in `textureProj` offset by `offset` as described in `textureOffset`.\n  ivec4 textureProjOffset(isampler3D sampler, vec4 P, ivec3 offset);\n  // Do a projective texture lookup as described in `textureProj` offset by `offset` as described in `textureOffset`.\n  uvec4 textureProjOffset(usampler3D sampler, vec4 P, ivec3 offset);\n  // Do a projective texture lookup as described in `textureProj` offset by `offset` as described in `textureOffset`.\n  vec4 textureProjOffset(sampler3D sampler, vec4 P, ivec3 offset, float bias);\n  // Do a projective texture lookup as described in `textureProj` offset by `offset` as described in `textureOffset`.\n  ivec4 textureProjOffset(isampler3D sampler, vec4 P, ivec3 offset, float bias);\n  // Do a projective texture lookup as described in `textureProj` offset by `offset` as described in `textureOffset`.\n  uvec4 textureProjOffset(usampler3D sampler, vec4 P, ivec3 offset, float bias);\n  // Do a projective texture lookup as described in `textureProj` offset by `offset` as described in `textureOffset`.\n  float textureProjOffset(sampler2DShadow sampler, vec4 P, ivec2 offset);\n  // Do a projective texture lookup as described in `textureProj` offset by `offset` as described in `textureOffset`.\n  float textureProjOffset(sampler2DShadow sampler, vec4 P, ivec2 offset, float bias);\n\n  // Do an offset texture lookup with explicit LOD. See `textureLod` and `textureOffset`.\n  vec4 textureLodOffset(sampler2D sampler, vec2 P, float lod, ivec2 offset);\n  // Do an offset texture lookup with explicit LOD. See `textureLod` and `textureOffset`.\n  ivec4 textureLodOffset(isampler2D sampler, vec2 P, float lod, ivec2 offset);\n  // Do an offset texture lookup with explicit LOD. See `textureLod` and `textureOffset`.\n  uvec4 textureLodOffset(usampler2D sampler, vec2 P, float lod, ivec2 offset);\n  // Do an offset texture lookup with explicit LOD. See `textureLod` and `textureOffset`.\n  vec4 textureLodOffset(sampler3D sampler, vec3 P, float lod, ivec3 offset);\n  // Do an offset texture lookup with explicit LOD. See `textureLod` and `textureOffset`.\n  ivec4 textureLodOffset(isampler3D sampler, vec3 P, float lod, ivec3 offset);\n  // Do an offset texture lookup with explicit LOD. See `textureLod` and `textureOffset`.\n  uvec4 textureLodOffset(usampler3D sampler, vec3 P, float lod, ivec3 offset);\n  // Do an offset texture lookup with explicit LOD. See `textureLod` and `textureOffset`.\n  float textureLodOffset(sampler2DShadow sampler, vec3 P, float lod, ivec2 offset);\n  // Do an offset texture lookup with explicit LOD. See `textureLod` and `textureOffset`.\n  vec4 textureLodOffset(sampler2DArray sampler, vec3 P, float lod, ivec2 offset);\n  // Do an offset texture lookup with explicit LOD. See `textureLod` and `textureOffset`.\n  ivec4 textureLodOffset(isampler2DArray sampler, vec3 P, float lod, ivec2 offset);\n  // Do an offset texture lookup with explicit LOD. See `textureLod` and `textureOffset`.\n  uvec4 textureLodOffset(usampler2DArray sampler, vec3 P, float lod, ivec2 offset);\n\n  // Do a projective texture lookup with explicit LOD. See `textureProj` and `textureLod`.\n  vec4 textureProjLod(sampler2D sampler, vec3 P, float lod);\n  // Do a projective texture lookup with explicit LOD. See `textureProj` and `textureLod`.\n  ivec4 textureProjLod(isampler2D sampler, vec3 P, float lod);\n  // Do a projective texture lookup with explicit LOD. See `textureProj` and `textureLod`.\n  uvec4 textureProjLod(usampler2D sampler, vec3 P, float lod);\n  // Do a projective texture lookup with explicit LOD. See `textureProj` and `textureLod`.\n  vec4 textureProjLod(sampler2D sampler, vec4 P, float lod);\n  // Do a projective texture lookup with explicit LOD. See `textureProj` and `textureLod`.\n  ivec4 textureProjLod(isampler2D sampler, vec4 P, float lod);\n  // Do a projective texture lookup with explicit LOD. See `textureProj` and `textureLod`.\n  uvec4 textureProjLod(usampler2D sampler, vec4 P, float lod);\n  // Do a projective texture lookup with explicit LOD. See `textureProj` and `textureLod`.\n  vec4 textureProjLod(sampler3D sampler, vec4 P, float lod);\n  // Do a projective texture lookup with explicit LOD. See `textureProj` and `textureLod`.\n  ivec4 textureProjLod(isampler3D sampler, vec4 P, float lod);\n  // Do a projective texture lookup with explicit LOD. See `textureProj` and `textureLod`.\n  uvec4 textureProjLod(usampler3D sampler, vec4 P, float lod);\n  // Do a projective texture lookup with explicit LOD. See `textureProj` and `textureLod`.\n  float textureProjLod(sampler2DShadow sampler, vec4 P, float lod);\n\n  // Do an offset projective texture lookup with explicit LOD. See `textureProj`, `textureLod`, and `textureOffset`.\n  vec4 textureProjLodOffset(sampler2D sampler, vec3 P, float lod, ivec2 offset);\n  // Do an offset projective texture lookup with explicit LOD. See `textureProj`, `textureLod`, and `textureOffset`.\n  ivec4 textureProjLodOffset(isampler2D sampler, vec3 P, float lod, ivec2 offset);\n  // Do an offset projective texture lookup with explicit LOD. See `textureProj`, `textureLod`, and `textureOffset`.\n  uvec4 textureProjLodOffset(usampler2D sampler, vec3 P, float lod, ivec2 offset);\n  // Do an offset projective texture lookup with explicit LOD. See `textureProj`, `textureLod`, and `textureOffset`.\n  vec4 textureProjLodOffset(sampler2D sampler, vec4 P, float lod, ivec2 offset);\n  // Do an offset projective texture lookup with explicit LOD. See `textureProj`, `textureLod`, and `textureOffset`.\n  ivec4 textureProjLodOffset(isampler2D sampler, vec4 P, float lod, ivec2 offset);\n  // Do an offset projective texture lookup with explicit LOD. See `textureProj`, `textureLod`, and `textureOffset`.\n  uvec4 textureProjLodOffset(usampler2D sampler, vec4 P, float lod, ivec2 offset);\n  // Do an offset projective texture lookup with explicit LOD. See `textureProj`, `textureLod`, and `textureOffset`.\n  vec4 textureProjLodOffset(sampler3D sampler, vec4 P, float lod, ivec3 offset);\n  // Do an offset projective texture lookup with explicit LOD. See `textureProj`, `textureLod`, and `textureOffset`.\n  ivec4 textureProjLodOffset(isampler3D sampler, vec4 P, float lod, ivec3 offset);\n  // Do an offset projective texture lookup with explicit LOD. See `textureProj`, `textureLod`, and `textureOffset`.\n  uvec4 textureProjLodOffset(usampler3D sampler, vec4 P, float lod, ivec3 offset);\n  // Do an offset projective texture lookup with explicit LOD. See `textureProj`, `textureLod`, and `textureOffset`.\n  float textureProjLodOffset(sampler2DShadow sampler, vec4 P, float lod, ivec2 offset);\n\n  // Do a texture lookup as in texture but with explicit gradients. The partial derivatives of `P` are with respect to window `x` and window `y`.\n  vec4 textureGrad(sampler2D sampler, vec2 P, vec2 dPdx, vec2 dPdy);\n  // Do a texture lookup as in texture but with explicit gradients. The partial derivatives of `P` are with respect to window `x` and window `y`.\n  ivec4 textureGrad(isampler2D sampler, vec2 P, vec2 dPdx, vec2 dPdy);\n  // Do a texture lookup as in texture but with explicit gradients. The partial derivatives of `P` are with respect to window `x` and window `y`.\n  uvec4 textureGrad(usampler2D sampler, vec2 P, vec2 dPdx, vec2 dPdy);\n  // Do a texture lookup as in texture but with explicit gradients. The partial derivatives of `P` are with respect to window `x` and window `y`.\n  vec4 textureGrad(sampler3D sampler, vec3 P, vec3 dPdx, vec3 dPdy);\n  // Do a texture lookup as in texture but with explicit gradients. The partial derivatives of `P` are with respect to window `x` and window `y`.\n  ivec4 textureGrad(isampler3D sampler, vec3 P, vec3 dPdx, vec3 dPdy);\n  // Do a texture lookup as in texture but with explicit gradients. The partial derivatives of `P` are with respect to window `x` and window `y`.\n  uvec4 textureGrad(usampler3D sampler, vec3 P, vec3 dPdx, vec3 dPdy);\n  // Do a texture lookup as in texture but with explicit gradients. The partial derivatives of `P` are with respect to window `x` and window `y`.\n  vec4 textureGrad(samplerCube sampler, vec3 P, vec3 dPdx, vec3 dPdy);\n  // Do a texture lookup as in texture but with explicit gradients. The partial derivatives of `P` are with respect to window `x` and window `y`.\n  ivec4 textureGrad(isamplerCube sampler, vec3 P, vec3 dPdx, vec3 dPdy);\n  // Do a texture lookup as in texture but with explicit gradients. The partial derivatives of `P` are with respect to window `x` and window `y`.\n  uvec4 textureGrad(usamplerCube sampler, vec3 P, vec3 dPdx, vec3 dPdy);\n  // Do a texture lookup as in texture but with explicit gradients. The partial derivatives of `P` are with respect to window `x` and window `y`.\n  float textureGrad(sampler2DShadow sampler, vec3 P, vec2 dPdx, vec2 dPdy);\n  // Do a texture lookup as in texture but with explicit gradients. The partial derivatives of `P` are with respect to window `x` and window `y`.\n  float textureGrad(samplerCubeShadow sampler, vec4 P, vec3 dPdx, vec3 dPdy);\n  // Do a texture lookup as in texture but with explicit gradients. The partial derivatives of `P` are with respect to window `x` and window `y`.\n  vec4 textureGrad(sampler2DArray sampler, vec3 P, vec2 dPdx, vec2 dPdy);\n  // Do a texture lookup as in texture but with explicit gradients. The partial derivatives of `P` are with respect to window `x` and window `y`.\n  ivec4 textureGrad(isampler2DArray sampler, vec3 P, vec2 dPdx, vec2 dPdy);\n  // Do a texture lookup as in texture but with explicit gradients. The partial derivatives of `P` are with respect to window `x` and window `y`.\n  uvec4 textureGrad(usampler2DArray sampler, vec3 P, vec2 dPdx, vec2 dPdy);\n  // Do a texture lookup as in texture but with explicit gradients. The partial derivatives of `P` are with respect to window `x` and window `y`.\n  float textureGrad(sampler2DArrayShadow sampler, vec4 P, vec2 dPdx, vec2 dPdy);\n\n  // Do a texture lookup with both explicit gradient and offset, as described in `textureGrad` and `textureOffset`.\n  vec4 textureGradOffset(sampler2D sampler, vec2 P, vec2 dPdx, vec2 dPdy, ivec2 offset);\n  // Do a texture lookup with both explicit gradient and offset, as described in `textureGrad` and `textureOffset`.\n  ivec4 textureGradOffset(isampler2D sampler, vec2 P, vec2 dPdx, vec2 dPdy, ivec2 offset);\n  // Do a texture lookup with both explicit gradient and offset, as described in `textureGrad` and `textureOffset`.\n  uvec4 textureGradOffset(usampler2D sampler, vec2 P, vec2 dPdx, vec2 dPdy, ivec2 offset);\n  // Do a texture lookup with both explicit gradient and offset, as described in `textureGrad` and `textureOffset`.\n  vec4 textureGradOffset(sampler3D sampler, vec3 P, vec3 dPdx, vec3 dPdy, ivec3 offset);\n  // Do a texture lookup with both explicit gradient and offset, as described in `textureGrad` and `textureOffset`.\n  ivec4 textureGradOffset(isampler3D sampler, vec3 P, vec3 dPdx, vec3 dPdy, ivec3 offset);\n  // Do a texture lookup with both explicit gradient and offset, as described in `textureGrad` and `textureOffset`.\n  uvec4 textureGradOffset(usampler3D sampler, vec3 P, vec3 dPdx, vec3 dPdy, ivec3 offset);\n  // Do a texture lookup with both explicit gradient and offset, as described in `textureGrad` and `textureOffset`.\n  float textureGradOffset(sampler2DShadow sampler, vec3 P, vec2 dPdx, vec2 dPdy, ivec2 offset);\n  // Do a texture lookup with both explicit gradient and offset, as described in `textureGrad` and `textureOffset`.\n  vec4 textureGradOffset(sampler2DArray sampler, vec3 P, vec2 dPdx, vec2 dPdy, ivec2 offset);\n  // Do a texture lookup with both explicit gradient and offset, as described in `textureGrad` and `textureOffset`.\n  ivec4 textureGradOffset(isampler2DArray sampler, vec3 P, vec2 dPdx, vec2 dPdy, ivec2 offset);\n  // Do a texture lookup with both explicit gradient and offset, as described in `textureGrad` and `textureOffset`.\n  uvec4 textureGradOffset(usampler2DArray sampler, vec3 P, vec2 dPdx, vec2 dPdy, ivec2 offset);\n  // Do a texture lookup with both explicit gradient and offset, as described in `textureGrad` and `textureOffset`.\n  float textureGradOffset(sampler2DArrayShadow sampler, vec4 P, vec2 dPdx, vec2 dPdy, ivec2 offset);\n\n  // Do a texture lookup both projectively, as described in `textureProj`, and with explicit gradient as\n  // described in `textureGrad`. The partial derivatives `dPdx` and `dPdy` are assumed to be already projected.\n  vec4 textureProjGrad(sampler2D sampler, vec3 P, vec2 dPdx, vec2 dPdy);\n  // Do a texture lookup both projectively, as described in `textureProj`, and with explicit gradient as\n  // described in `textureGrad`. The partial derivatives `dPdx` and `dPdy` are assumed to be already projected.\n  ivec4 textureProjGrad(isampler2D sampler, vec3 P, vec2 dPdx, vec2 dPdy);\n  // Do a texture lookup both projectively, as described in `textureProj`, and with explicit gradient as\n  // described in `textureGrad`. The partial derivatives `dPdx` and `dPdy` are assumed to be already projected.\n  uvec4 textureProjGrad(usampler2D sampler, vec3 P, vec2 dPdx, vec2 dPdy);\n  // Do a texture lookup both projectively, as described in `textureProj`, and with explicit gradient as\n  // described in `textureGrad`. The partial derivatives `dPdx` and `dPdy` are assumed to be already projected.\n  vec4 textureProjGrad(sampler2D sampler, vec4 P, vec2 dPdx, vec2 dPdy);\n  // Do a texture lookup both projectively, as described in `textureProj`, and with explicit gradient as\n  // described in `textureGrad`. The partial derivatives `dPdx` and `dPdy` are assumed to be already projected.\n  ivec4 textureProjGrad(isampler2D sampler, vec4 P, vec2 dPdx, vec2 dPdy);\n  // Do a texture lookup both projectively, as described in `textureProj`, and with explicit gradient as\n  // described in `textureGrad`. The partial derivatives `dPdx` and `dPdy` are assumed to be already projected.\n  uvec4 textureProjGrad(usampler2D sampler, vec4 P, vec2 dPdx, vec2 dPdy);\n  // Do a texture lookup both projectively, as described in `textureProj`, and with explicit gradient as\n  // described in `textureGrad`. The partial derivatives `dPdx` and `dPdy` are assumed to be already projected.\n  vec4 textureProjGrad(sampler3D sampler, vec4 P, vec3 dPdx, vec3 dPdy);\n  // Do a texture lookup both projectively, as described in `textureProj`, and with explicit gradient as\n  // described in `textureGrad`. The partial derivatives `dPdx` and `dPdy` are assumed to be already projected.\n  ivec4 textureProjGrad(isampler3D sampler, vec4 P, vec3 dPdx, vec3 dPdy);\n  // Do a texture lookup both projectively, as described in `textureProj`, and with explicit gradient as\n  // described in `textureGrad`. The partial derivatives `dPdx` and `dPdy` are assumed to be already projected.\n  uvec4 textureProjGrad(usampler3D sampler, vec4 P, vec3 dPdx, vec3 dPdy);\n  // Do a texture lookup both projectively, as described in `textureProj`, and with explicit gradient as\n  // described in `textureGrad`. The partial derivatives `dPdx` and `dPdy` are assumed to be already projected.\n  float textureProjGrad(sampler2DShadow sampler, vec4 P, vec2 dPdx, vec2 dPdy);\n\n  // Do a texture lookup projectively and with explicit gradient as described in `textureProjGrad`, as well as with `offset`, as described in `textureOffset`.\n  vec4 textureProjGradOffset(sampler2D sampler, vec3 P, vec2 dPdx, vec2 dPdy, ivec2 offset);\n  // Do a texture lookup projectively and with explicit gradient as described in `textureProjGrad`, as well as with `offset`, as described in `textureOffset`.\n  ivec4 textureProjGradOffset(isampler2D sampler, vec3 P, vec2 dPdx, vec2 dPdy, ivec2 offset);\n  // Do a texture lookup projectively and with explicit gradient as described in `textureProjGrad`, as well as with `offset`, as described in `textureOffset`.\n  uvec4 textureProjGradOffset(usampler2D sampler, vec3 P, vec2 dPdx, vec2 dPdy, ivec2 offset);\n  // Do a texture lookup projectively and with explicit gradient as described in `textureProjGrad`, as well as with `offset`, as described in `textureOffset`.\n  vec4 textureProjGradOffset(sampler2D sampler, vec4 P, vec2 dPdx, vec2 dPdy, ivec2 offset);\n  // Do a texture lookup projectively and with explicit gradient as described in `textureProjGrad`, as well as with `offset`, as described in `textureOffset`.\n  ivec4 textureProjGradOffset(isampler2D sampler, vec4 P, vec2 dPdx, vec2 dPdy, ivec2 offset);\n  // Do a texture lookup projectively and with explicit gradient as described in `textureProjGrad`, as well as with `offset`, as described in `textureOffset`.\n  uvec4 textureProjGradOffset(usampler2D sampler, vec4 P, vec2 dPdx, vec2 dPdy, ivec2 offset);\n  // Do a texture lookup projectively and with explicit gradient as described in `textureProjGrad`, as well as with `offset`, as described in `textureOffset`.\n  vec4 textureProjGradOffset(sampler3D sampler, vec4 P, vec3 dPdx, vec3 dPdy, ivec3 offset);\n  // Do a texture lookup projectively and with explicit gradient as described in `textureProjGrad`, as well as with `offset`, as described in `textureOffset`.\n  ivec4 textureProjGradOffset(isampler3D sampler, vec4 P, vec3 dPdx, vec3 dPdy, ivec3 offset);\n  // Do a texture lookup projectively and with explicit gradient as described in `textureProjGrad`, as well as with `offset`, as described in `textureOffset`.\n  uvec4 textureProjGradOffset(usampler3D sampler, vec4 P, vec3 dPdx, vec3 dPdy, ivec3 offset);\n  // Do a texture lookup projectively and with explicit gradient as described in `textureProjGrad`, as well as with `offset`, as described in `textureOffset`.\n  float textureProjGradOffset(sampler2DShadow sampler, vec4 P, vec2 dPdx, vec2 dPdy, ivec2 offset);\n\n  ////////////////////////////////////////////////////////////////////////////////\n  // Fragment Processing Functions\n\n  // Returns the derivative in x using local differencing for the input argument `p`.\n  float dFdx(float p);\n  // Returns the derivative in x using local differencing for the input argument `p`.\n  vec2 dFdx(vec2 p);\n  // Returns the derivative in x using local differencing for the input argument `p`.\n  vec3 dFdx(vec3 p);\n  // Returns the derivative in x using local differencing for the input argument `p`.\n  vec4 dFdx(vec4 p);\n\n  // Returns the derivative in y using local differencing for the input argument `p`.\n  float dFdy(float p);\n  // Returns the derivative in y using local differencing for the input argument `p`.\n  vec2 dFdy(vec2 p);\n  // Returns the derivative in y using local differencing for the input argument `p`.\n  vec3 dFdy(vec3 p);\n  // Returns the derivative in y using local differencing for the input argument `p`.\n  vec4 dFdy(vec4 p);\n\n  // Returns the sum of the absolute derivative in x and y using local differencing for the input argument `p`, i.e., `abs(dFdx(p)) + abs(dFdy(p))`.\n  float fwidth(float p);\n  // Returns the sum of the absolute derivative in x and y using local differencing for the input argument `p`, i.e., `abs(dFdx(p)) + abs(dFdy(p))`.\n  vec2 fwidth(vec2 p);\n  // Returns the sum of the absolute derivative in x and y using local differencing for the input argument `p`, i.e., `abs(dFdx(p)) + abs(dFdy(p))`.\n  vec3 fwidth(vec3 p);\n  // Returns the sum of the absolute derivative in x and y using local differencing for the input argument `p`, i.e., `abs(dFdx(p)) + abs(dFdy(p))`.\n  vec4 fwidth(vec4 p);\n}\n");
for(var l=0,g=b.length;l<g;l=l+1|0){var f=b[l];f.c.b||wg(a,f,c)}}else e=new Vi('<api>',"\nimport {\n  // The variable `gl_Position` is available only in the vertex language and is intended for writing the\n  // homogeneous vertex position. This value will be used by primitive assembly, clipping, culling, and other\n  // fixed functionality operations that operate on primitives after vertex processing has occurred.\n  //\n  // All executions of a well-formed vertex shader should write a value into this variable. It can be\n  // written at any time during shader execution. It may also be read back by the shader after being written.\n  // Compilers may generate a diagnostic message if they detect `gl_Position` is not written, or read before\n  // being written, but not all such cases are detectable. The value of `gl_Position` is undefined if a vertex\n  // shader is executed and does not write `gl_Position`.\n  highp vec4 gl_Position;\n\n  // The variable `gl_PointSize` is available only in the vertex language and is intended for\n  // a vertex shader to write the size of the point to be rasterized. It is measured in pixels.\n  mediump float gl_PointSize;\n\n  const int gl_MaxVertexAttribs;\n  const int gl_MaxVertexUniformVectors;\n  const int gl_MaxVaryingVectors;\n  const int gl_MaxVertexTextureImageUnits;\n  const int gl_MaxCombinedTextureImageUnits;\n  const int gl_MaxTextureImageUnits;\n  const int gl_MaxFragmentUniformVectors;\n  const int gl_MaxDrawBuffers;\n\n  // The fragment shader has access to the read-only built-in variable `gl_FrontFacing` whose value is `true` if\n  // the fragment belongs to a front-facing primitive. One use of this is to emulate two-sided lighting by\n  // selecting one of two colors calculated by the vertex shader.\n  const bool gl_FrontFacing;\n\n  // The fragment shader has access to the read-only built-in variable `gl_PointCoord`. The values in\n  // `gl_PointCoord` are two-dimensional coordinates indicating where within a point primitive the current\n  // fragment is located. They range from 0.0 to 1.0 across the point. If the current primitive is not a\n  // point, then the values read from `gl_PointCoord` are undefined.\n  const mediump vec2 gl_PointCoord;\n\n  // The variable `gl_FragCoord` is available as a read-only variable from within fragment shaders and it holds\n  // the window relative coordinates `x`, `y`, `z`, and `1/w` values for the fragment. This value is the result\n  // of the fixed functionality that interpolates primitives after vertex processing to generate fragments. The `z`\n  // component is the depth value that will be used for the fragment's depth.\n  const mediump vec4 gl_FragCoord;\n\n  // Writing to `gl_FragColor` specifies the fragment color that will be used by the subsequent fixed\n  // functionality pipeline.\n  //\n  // If subsequent fixed functionality consumes fragment color and an execution of a fragment shader\n  // does not write a value to `gl_FragColor` then the fragment color consumed is undefined.\n  mediump vec4 gl_FragColor;\n\n  // The variable `gl_FragData` is an array. Writing to `gl_FragData[n]` specifies the fragment data that will be\n  // used by the subsequent fixed functionality pipeline for data `n`.\n  //\n  // If subsequent fixed functionality consumes fragment data and an execution of a fragment shader does not write\n  // a value to it, then the fragment data consumed is undefined.\n  mediump vec4 gl_FragData[gl_MaxDrawBuffers];\n\n  // Depth range in window coordinates\n  struct gl_DepthRangeParameters {\n    float near;\n    float far;\n    // Equal to `far - near`\n    float diff;\n  };\n\n  uniform gl_DepthRangeParameters gl_DepthRange;\n\n  ////////////////////////////////////////////////////////////////////////////////\n  // Angle and Trigonometry Functions\n\n  // Converts `degrees` to radians, i.e. `π / 180 * degrees`\n  float radians(float degrees);\n  // Converts `degrees` to radians, i.e. `π / 180 * degrees`\n  vec2 radians(vec2 degrees);\n  // Converts `degrees` to radians, i.e. `π / 180 * degrees`\n  vec3 radians(vec3 degrees);\n  // Converts `degrees` to radians, i.e. `π / 180 * degrees`\n  vec4 radians(vec4 degrees);\n\n  // Converts `radians` to degrees, i.e. `180 / π * radians`\n  float degrees(float radians);\n  // Converts `radians` to degrees, i.e. `180 / π * radians`\n  vec2 degrees(vec2 radians);\n  // Converts `radians` to degrees, i.e. `180 / π * radians`\n  vec3 degrees(vec3 radians);\n  // Converts `radians` to degrees, i.e. `180 / π * radians`\n  vec4 degrees(vec4 radians);\n\n  // The standard trigonometric sine function.\n  float sin(float angle);\n  // The standard trigonometric sine function.\n  vec2 sin(vec2 angle);\n  // The standard trigonometric sine function.\n  vec3 sin(vec3 angle);\n  // The standard trigonometric sine function.\n  vec4 sin(vec4 angle);\n\n  // The standard trigonometric cosine function.\n  float cos(float angle);\n  // The standard trigonometric cosine function.\n  vec2 cos(vec2 angle);\n  // The standard trigonometric cosine function.\n  vec3 cos(vec3 angle);\n  // The standard trigonometric cosine function.\n  vec4 cos(vec4 angle);\n\n  // The standard trigonometric tangent.\n  float tan(float angle);\n  // The standard trigonometric tangent.\n  vec2 tan(vec2 angle);\n  // The standard trigonometric tangent.\n  vec3 tan(vec3 angle);\n  // The standard trigonometric tangent.\n  vec4 tan(vec4 angle);\n\n  // Arc sine. Returns an angle whose sine is `x`. The range of values returned by this function is `[-π/2, π/2]`. Results are undefined if `∣x∣>1`.\n  float asin(float x);\n  // Arc sine. Returns an angle whose sine is `x`. The range of values returned by this function is `[-π/2, π/2]`. Results are undefined if `∣x∣>1`.\n  vec2 asin(vec2 x);\n  // Arc sine. Returns an angle whose sine is `x`. The range of values returned by this function is `[-π/2, π/2]`. Results are undefined if `∣x∣>1`.\n  vec3 asin(vec3 x);\n  // Arc sine. Returns an angle whose sine is `x`. The range of values returned by this function is `[-π/2, π/2]`. Results are undefined if `∣x∣>1`.\n  vec4 asin(vec4 x);\n\n  // Arc cosine. Returns an angle whose cosine is `x`. The range of values returned by this function is `[0, π]`. Results are undefined if `∣x∣>1`.\n  float acos(float x);\n  // Arc cosine. Returns an angle whose cosine is `x`. The range of values returned by this function is `[0, π]`. Results are undefined if `∣x∣>1`.\n  vec2 acos(vec2 x);\n  // Arc cosine. Returns an angle whose cosine is `x`. The range of values returned by this function is `[0, π]`. Results are undefined if `∣x∣>1`.\n  vec3 acos(vec3 x);\n  // Arc cosine. Returns an angle whose cosine is `x`. The range of values returned by this function is `[0, π]`. Results are undefined if `∣x∣>1`.\n  vec4 acos(vec4 x);\n\n  // Arc tangent. Returns an angle whose tangent is `y/x`. The signs of `x` and `y` are used to determine what quadrant the\n  // angle is in. The range of values returned by this function is `[−π,π]`. Results are undefined if `x` and `y` are both 0.\n  float atan(float y, float x);\n  // Arc tangent. Returns an angle whose tangent is `y/x`. The signs of `x` and `y` are used to determine what quadrant the\n  // angle is in. The range of values returned by this function is `[−π,π]`. Results are undefined if `x` and `y` are both 0.\n  vec2 atan(vec2 y, vec2 x);\n  // Arc tangent. Returns an angle whose tangent is `y/x`. The signs of `x` and `y` are used to determine what quadrant the\n  // angle is in. The range of values returned by this function is `[−π,π]`. Results are undefined if `x` and `y` are both 0.\n  vec3 atan(vec3 y, vec3 x);\n  // Arc tangent. Returns an angle whose tangent is `y/x`. The signs of `x` and `y` are used to determine what quadrant the\n  // angle is in. The range of values returned by this function is `[−π,π]`. Results are undefined if `x` and `y` are both 0.\n  vec4 atan(vec4 y, vec4 x);\n\n  // Arc tangent. Returns an angle whose tangent is `y_over_x`. The range of values returned by this function is `[-π/2, π/2]`.\n  float atan(float y_over_x);\n  // Arc tangent. Returns an angle whose tangent is `y_over_x`. The range of values returned by this function is `[-π/2, π/2]`.\n  vec2 atan(vec2 y_over_x);\n  // Arc tangent. Returns an angle whose tangent is `y_over_x`. The range of values returned by this function is `[-π/2, π/2]`.\n  vec3 atan(vec3 y_over_x);\n  // Arc tangent. Returns an angle whose tangent is `y_over_x`. The range of values returned by this function is `[-π/2, π/2]`.\n  vec4 atan(vec4 y_over_x);\n\n  ////////////////////////////////////////////////////////////////////////////////\n  // Exponential Functions\n\n  // Returns `x` raised to the `y` power, i.e., `xʸ`. Results are undefined if `x < 0`. Results are undefined if `x = 0` and `y <= 0`.\n  float pow(float x, float y);\n  // Returns `x` raised to the `y` power, i.e., `xʸ`. Results are undefined if `x < 0`. Results are undefined if `x = 0` and `y <= 0`.\n  vec2 pow(vec2 x, vec2 y);\n  // Returns `x` raised to the `y` power, i.e., `xʸ`. Results are undefined if `x < 0`. Results are undefined if `x = 0` and `y <= 0`.\n  vec3 pow(vec3 x, vec3 y);\n  // Returns `x` raised to the `y` power, i.e., `xʸ`. Results are undefined if `x < 0`. Results are undefined if `x = 0` and `y <= 0`.\n  vec4 pow(vec4 x, vec4 y);\n\n  // Returns the natural exponentiation of `x`, i.e., `eˣ`\n  float exp(float x);\n  // Returns the natural exponentiation of `x`, i.e., `eˣ`\n  vec2 exp(vec2 x);\n  // Returns the natural exponentiation of `x`, i.e., `eˣ`\n  vec3 exp(vec3 x);\n  // Returns the natural exponentiation of `x`, i.e., `eˣ`\n  vec4 exp(vec4 x);\n\n  // Returns the natural logarithm of `x`, i.e., returns the value `y` which satisfies the equation `x = eʸ`. Results are undefined if `x <= 0`.\n  float log(float x);\n  // Returns the natural logarithm of `x`, i.e., returns the value `y` which satisfies the equation `x = eʸ`. Results are undefined if `x <= 0`.\n  vec2 log(vec2 x);\n  // Returns the natural logarithm of `x`, i.e., returns the value `y` which satisfies the equation `x = eʸ`. Results are undefined if `x <= 0`.\n  vec3 log(vec3 x);\n  // Returns the natural logarithm of `x`, i.e., returns the value `y` which satisfies the equation `x = eʸ`. Results are undefined if `x <= 0`.\n  vec4 log(vec4 x);\n\n  // Returns 2 raised to the `x` power, i.e., `2ˣ`.\n  float exp2(float x);\n  // Returns 2 raised to the `x` power, i.e., `2ˣ`.\n  vec2 exp2(vec2 x);\n  // Returns 2 raised to the `x` power, i.e., `2ˣ`.\n  vec3 exp2(vec3 x);\n  // Returns 2 raised to the `x` power, i.e., `2ˣ`.\n  vec4 exp2(vec4 x);\n\n  // Returns the base 2 logarithm of `x`, i.e., returns the value `y` which satisfies the equation `x = 2ʸ`. Results are undefined if `x <= 0`.\n  float log2(float x);\n  // Returns the base 2 logarithm of `x`, i.e., returns the value `y` which satisfies the equation `x = 2ʸ`. Results are undefined if `x <= 0`.\n  vec2 log2(vec2 x);\n  // Returns the base 2 logarithm of `x`, i.e., returns the value `y` which satisfies the equation `x = 2ʸ`. Results are undefined if `x <= 0`.\n  vec3 log2(vec3 x);\n  // Returns the base 2 logarithm of `x`, i.e., returns the value `y` which satisfies the equation `x = 2ʸ`. Results are undefined if `x <= 0`.\n  vec4 log2(vec4 x);\n\n  // Returns `√x`. Results are undefined if `x < 0`.\n  float sqrt(float x);\n  // Returns `√x`. Results are undefined if `x < 0`.\n  vec2 sqrt(vec2 x);\n  // Returns `√x`. Results are undefined if `x < 0`.\n  vec3 sqrt(vec3 x);\n  // Returns `√x`. Results are undefined if `x < 0`.\n  vec4 sqrt(vec4 x);\n\n  // Returns `1 / √x`. Results are undefined if `x <= 0`.\n  float inversesqrt(float x);\n  // Returns `1 / √x`. Results are undefined if `x <= 0`.\n  vec2 inversesqrt(vec2 x);\n  // Returns `1 / √x`. Results are undefined if `x <= 0`.\n  vec3 inversesqrt(vec3 x);\n  // Returns `1 / √x`. Results are undefined if `x <= 0`.\n  vec4 inversesqrt(vec4 x);\n\n  ////////////////////////////////////////////////////////////////////////////////\n  // Common Functions\n\n  // Returns `x` if `x >= 0`, otherwise it returns `-x`.\n  float abs(float x);\n  // Returns `x` if `x >= 0`, otherwise it returns `-x`.\n  vec2 abs(vec2 x);\n  // Returns `x` if `x >= 0`, otherwise it returns `-x`.\n  vec3 abs(vec3 x);\n  // Returns `x` if `x >= 0`, otherwise it returns `-x`.\n  vec4 abs(vec4 x);\n\n  // Returns `1.0` if `x > 0`, `0.0` if `x = 0`, or `-1.0` if `x < 0`\n  float sign(float x);\n  // Returns `1.0` if `x > 0`, `0.0` if `x = 0`, or `-1.0` if `x < 0`\n  vec2 sign(vec2 x);\n  // Returns `1.0` if `x > 0`, `0.0` if `x = 0`, or `-1.0` if `x < 0`\n  vec3 sign(vec3 x);\n  // Returns `1.0` if `x > 0`, `0.0` if `x = 0`, or `-1.0` if `x < 0`\n  vec4 sign(vec4 x);\n\n  // Returns a value equal to the nearest integer that is less than or equal to `x`\n  float floor(float x);\n  // Returns a value equal to the nearest integer that is less than or equal to `x`\n  vec2 floor(vec2 x);\n  // Returns a value equal to the nearest integer that is less than or equal to `x`\n  vec3 floor(vec3 x);\n  // Returns a value equal to the nearest integer that is less than or equal to `x`\n  vec4 floor(vec4 x);\n\n  // Returns a value equal to the nearest integer that is greater than or equal to `x`\n  float ceil(float x);\n  // Returns a value equal to the nearest integer that is greater than or equal to `x`\n  vec2 ceil(vec2 x);\n  // Returns a value equal to the nearest integer that is greater than or equal to `x`\n  vec3 ceil(vec3 x);\n  // Returns a value equal to the nearest integer that is greater than or equal to `x`\n  vec4 ceil(vec4 x);\n\n  // Returns `x - floor(x)`\n  float fract(float x);\n  // Returns `x - floor(x)`\n  vec2 fract(vec2 x);\n  // Returns `x - floor(x)`\n  vec3 fract(vec3 x);\n  // Returns `x - floor(x)`\n  vec4 fract(vec4 x);\n\n  // Modulus (modulo). Returns `x - y * floor(x/y)`\n  float mod(float x, float y);\n  // Modulus (modulo). Returns `x - y * floor(x/y)`\n  vec2 mod(vec2 x, float y);\n  // Modulus (modulo). Returns `x - y * floor(x/y)`\n  vec3 mod(vec3 x, float y);\n  // Modulus (modulo). Returns `x - y * floor(x/y)`\n  vec4 mod(vec4 x, float y);\n\n  // Modulus. Returns `x - y * floor(x/y)`\n  vec2 mod(vec2 x, vec2 y);\n  // Modulus. Returns `x - y * floor(x/y)`\n  vec3 mod(vec3 x, vec3 y);\n  // Modulus. Returns `x - y * floor(x/y)`\n  vec4 mod(vec4 x, vec4 y);\n\n  // Returns `y` if `y < x`, otherwise it returns `x`\n  float min(float x, float y);\n  // Returns `y` if `y < x`, otherwise it returns `x`\n  vec2 min(vec2 x, float y);\n  // Returns `y` if `y < x`, otherwise it returns `x`\n  vec2 min(vec2 x, vec2 y);\n  // Returns `y` if `y < x`, otherwise it returns `x`\n  vec3 min(vec3 x, float y);\n  // Returns `y` if `y < x`, otherwise it returns `x`\n  vec3 min(vec3 x, vec3 y);\n  // Returns `y` if `y < x`, otherwise it returns `x`\n  vec4 min(vec4 x, float y);\n  // Returns `y` if `y < x`, otherwise it returns `x`\n  vec4 min(vec4 x, vec4 y);\n\n  // Returns `y` if `x < y`, otherwise it returns `x`\n  float max(float x, float y);\n  // Returns `y` if `x < y`, otherwise it returns `x`\n  vec2 max(vec2 x, float y);\n  // Returns `y` if `x < y`, otherwise it returns `x`\n  vec2 max(vec2 x, vec2 y);\n  // Returns `y` if `x < y`, otherwise it returns `x`\n  vec3 max(vec3 x, float y);\n  // Returns `y` if `x < y`, otherwise it returns `x`\n  vec3 max(vec3 x, vec3 y);\n  // Returns `y` if `x < y`, otherwise it returns `x`\n  vec4 max(vec4 x, float y);\n  // Returns `y` if `x < y`, otherwise it returns `x`\n  vec4 max(vec4 x, vec4 y);\n\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  float clamp(float x, float minVal, float maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  vec2 clamp(vec2 x, float minVal, float maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  vec2 clamp(vec2 x, vec2 minVal, vec2 maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  vec3 clamp(vec3 x, float minVal, float maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  vec3 clamp(vec3 x, vec3 minVal, vec3 maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  vec4 clamp(vec4 x, float minVal, float maxVal);\n  // Returns `min(max(x, minVal), maxVal)`. Results are undefined if `minVal > maxVal`.\n  vec4 clamp(vec4 x, vec4 minVal, vec4 maxVal);\n\n  // Returns the linear blend of `x` and `y`, i.e. `x * (1-a) + y * a`\n  float mix(float x, float y, float a);\n  // Returns the linear blend of `x` and `y`, i.e. `x * (1-a) + y * a`\n  vec2 mix(vec2 x, vec2 y, float a);\n  // Returns the linear blend of `x` and `y`, i.e. `x * (1-a) + y * a`\n  vec2 mix(vec2 x, vec2 y, vec2 a);\n  // Returns the linear blend of `x` and `y`, i.e. `x * (1-a) + y * a`\n  vec3 mix(vec3 x, vec3 y, float a);\n  // Returns the linear blend of `x` and `y`, i.e. `x * (1-a) + y * a`\n  vec3 mix(vec3 x, vec3 y, vec3 a);\n  // Returns the linear blend of `x` and `y`, i.e. `x * (1-a) + y * a`\n  vec4 mix(vec4 x, vec4 y, float a);\n  // Returns the linear blend of `x` and `y`, i.e. `x * (1-a) + y * a`\n  vec4 mix(vec4 x, vec4 y, vec4 a);\n\n  // Returns `0.0` if `x < edge`, otherwise it returns `1.0`\n  float step(float edge, float x);\n  // Returns `0.0` if `x < edge`, otherwise it returns `1.0`\n  vec2 step(float edge, vec2 x);\n  // Returns `0.0` if `x < edge`, otherwise it returns `1.0`\n  vec2 step(vec2 edge, vec2 x);\n  // Returns `0.0` if `x < edge`, otherwise it returns `1.0`\n  vec3 step(float edge, vec3 x);\n  // Returns `0.0` if `x < edge`, otherwise it returns `1.0`\n  vec3 step(vec3 edge, vec3 x);\n  // Returns `0.0` if `x < edge`, otherwise it returns `1.0`\n  vec4 step(float edge, vec4 x);\n  // Returns `0.0` if `x < edge`, otherwise it returns `1.0`\n  vec4 step(vec4 edge, vec4 x);\n\n  // Returns `0.0` if `x <= edge0` and `1.0` if `x >= edge1` and performs smooth Hermite interpolation between 0 and 1 when `edge0 < x < edge1`.\n  // This is useful in cases where you would want a threshold function with a smooth transition. This is equivalent to:\n  //\n  // ```glslx\n  // float t = clamp((x - edge0) / (edge1 - edge0), 0.0, 1.0);\n  // return t * t * (3.0 - 2.0 * t);\n  // ```\n  //\n  // Results are undefined if `edge0 >= edge1`.\n  float smoothstep(float edge0, float edge1, float x);\n  // Returns `0.0` if `x <= edge0` and `1.0` if `x >= edge1` and performs smooth Hermite interpolation between 0 and 1 when `edge0 < x < edge1`.\n  // This is useful in cases where you would want a threshold function with a smooth transition. This is equivalent to:\n  //\n  // ```glslx\n  // vec2 t = clamp((x - edge0) / (edge1 - edge0), 0.0, 1.0);\n  // return t * t * (3.0 - 2.0 * t);\n  // ```\n  //\n  // Results are undefined if `edge0 >= edge1`.\n  vec2 smoothstep(float edge0, float edge1, vec2 x);\n  // Returns `0.0` if `x <= edge0` and `1.0` if `x >= edge1` and performs smooth Hermite interpolation between 0 and 1 when `edge0 < x < edge1`.\n  // This is useful in cases where you would want a threshold function with a smooth transition. This is equivalent to:\n  //\n  // ```glslx\n  // vec2 t = clamp((x - edge0) / (edge1 - edge0), 0.0, 1.0);\n  // return t * t * (3.0 - 2.0 * t);\n  // ```\n  //\n  // Results are undefined if `edge0 >= edge1`.\n  vec2 smoothstep(vec2 edge0, vec2 edge1, vec2 x);\n  // Returns `0.0` if `x <= edge0` and `1.0` if `x >= edge1` and performs smooth Hermite interpolation between 0 and 1 when `edge0 < x < edge1`.\n  // This is useful in cases where you would want a threshold function with a smooth transition. This is equivalent to:\n  //\n  // ```glslx\n  // vec3 t = clamp((x - edge0) / (edge1 - edge0), 0.0, 1.0);\n  // return t * t * (3.0 - 2.0 * t);\n  // ```\n  //\n  // Results are undefined if `edge0 >= edge1`.\n  vec3 smoothstep(float edge0, float edge1, vec3 x);\n  // Returns `0.0` if `x <= edge0` and `1.0` if `x >= edge1` and performs smooth Hermite interpolation between 0 and 1 when `edge0 < x < edge1`.\n  // This is useful in cases where you would want a threshold function with a smooth transition. This is equivalent to:\n  //\n  // ```glslx\n  // vec3 t = clamp((x - edge0) / (edge1 - edge0), 0.0, 1.0);\n  // return t * t * (3.0 - 2.0 * t);\n  // ```\n  //\n  // Results are undefined if `edge0 >= edge1`.\n  vec3 smoothstep(vec3 edge0, vec3 edge1, vec3 x);\n  // Returns `0.0` if `x <= edge0` and `1.0` if `x >= edge1` and performs smooth Hermite interpolation between 0 and 1 when `edge0 < x < edge1`.\n  // This is useful in cases where you would want a threshold function with a smooth transition. This is equivalent to:\n  //\n  // ```glslx\n  // vec4 t = clamp((x - edge0) / (edge1 - edge0), 0.0, 1.0);\n  // return t * t * (3.0 - 2.0 * t);\n  // ```\n  //\n  // Results are undefined if `edge0 >= edge1`.\n  vec4 smoothstep(float edge0, float edge1, vec4 x);\n  // Returns `0.0` if `x <= edge0` and `1.0` if `x >= edge1` and performs smooth Hermite interpolation between 0 and 1 when `edge0 < x < edge1`.\n  // This is useful in cases where you would want a threshold function with a smooth transition. This is equivalent to:\n  //\n  // ```glslx\n  // vec4 t = clamp((x - edge0) / (edge1 - edge0), 0.0, 1.0);\n  // return t * t * (3.0 - 2.0 * t);\n  // ```\n  //\n  // Results are undefined if `edge0 >= edge1`.\n  vec4 smoothstep(vec4 edge0, vec4 edge1, vec4 x);\n\n  ////////////////////////////////////////////////////////////////////////////////\n  // Geometric Functions\n\n  // Returns the length of vector `x`, i.e. `√x²`\n  float length(float x);\n  // Returns the length of vector `x`, i.e. `√x[0]² + x[1]²`\n  float length(vec2 x);\n  // Returns the length of vector `x`, i.e. `√x[0]² + x[1]² + x[2]²`\n  float length(vec3 x);\n  // Returns the length of vector `x`, i.e. `√x[0]² + x[1]² + x[2]² + x[3]²`\n  float length(vec4 x);\n\n  // Returns the distance between `p0` and `p1`, i.e. `length(p0 - p1)`\n  float distance(float p0, float p1);\n  // Returns the distance between `p0` and `p1`, i.e. `length(p0 - p1)`\n  float distance(vec2 p0, vec2 p1);\n  // Returns the distance between `p0` and `p1`, i.e. `length(p0 - p1)`\n  float distance(vec3 p0, vec3 p1);\n  // Returns the distance between `p0` and `p1`, i.e. `length(p0 - p1)`\n  float distance(vec4 p0, vec4 p1);\n\n  // Returns the dot product of `x` and `y`, i.e. `x*y`\n  float dot(float x, float y);\n  // Returns the dot product of `x` and `y`, i.e. `x[0]*y[0] + x[1]*y[1]`\n  float dot(vec2 x, vec2 y);\n  // Returns the dot product of `x` and `y`, i.e. `x[0]*y[0] + x[1]*y[1] + x[2]*y[2]`\n  float dot(vec3 x, vec3 y);\n  // Returns the dot product of `x` and `y`, i.e. `x[0]*y[0] + x[1]*y[1] + x[2]*y[2] + x[3]*y[3]`\n  float dot(vec4 x, vec4 y);\n\n  // Returns the cross product of `x` and `y`, i.e.\n  //\n  // ```glslx\n  // vec3(\n  //   x[1]*y[2] - y[1]*x[2],\n  //   x[2]*y[0] - y[2]*x[0],\n  //   x[0]*y[1] - y[0]*x[1])\n  // ```\n  vec3 cross(vec3 x, vec3 y);\n\n  // Returns a vector in the same direction as `x` but with a length of 1.\n  float normalize(float x);\n  // Returns a vector in the same direction as `x` but with a length of 1.\n  vec2 normalize(vec2 x);\n  // Returns a vector in the same direction as `x` but with a length of 1.\n  vec3 normalize(vec3 x);\n  // Returns a vector in the same direction as `x` but with a length of 1.\n  vec4 normalize(vec4 x);\n\n  // If `dot(Nref, I) < 0` return `N`, otherwise return `-N`\n  float faceforward(float N, float I, float Nref);\n  // If `dot(Nref, I) < 0` return `N`, otherwise return `-N`\n  vec2 faceforward(vec2 N, vec2 I, vec2 Nref);\n  // If `dot(Nref, I) < 0` return `N`, otherwise return `-N`\n  vec3 faceforward(vec3 N, vec3 I, vec3 Nref);\n  // If `dot(Nref, I) < 0` return `N`, otherwise return `-N`\n  vec4 faceforward(vec4 N, vec4 I, vec4 Nref);\n\n  // For the incident vector `I` and surface orientation `N`, returns the reflection direction: `I - 2 * dot(N, I) * N`.\n  // `N` must already be normalized in order to achieve the desired result.\n  float reflect(float I, float N);\n  // For the incident vector `I` and surface orientation `N`, returns the reflection direction: `I - 2 * dot(N, I) * N`.\n  // `N` must already be normalized in order to achieve the desired result.\n  vec2 reflect(vec2 I, vec2 N);\n  // For the incident vector `I` and surface orientation `N`, returns the reflection direction: `I - 2 * dot(N, I) * N`.\n  // `N` must already be normalized in order to achieve the desired result.\n  vec3 reflect(vec3 I, vec3 N);\n  // For the incident vector `I` and surface orientation `N`, returns the reflection direction: `I - 2 * dot(N, I) * N`.\n  // `N` must already be normalized in order to achieve the desired result.\n  vec4 reflect(vec4 I, vec4 N);\n\n  // For the incident vector `I` and surface normal `N`, and the ratio of indices of refraction `eta`, return the refraction vector.\n  // The result is computed by:\n  //\n  // ```glslx\n  // float k = 1.0 - eta * eta * (1.0 - dot(N, I) * dot(N, I));\n  // if (k < 0.0) return float(0.0);\n  // else return eta * I - (eta * dot(N, I) + sqrt(k)) * N;\n  // ```\n  //\n  // The input parameters for the incident vector `I` and the surface normal `N`.\n  float refract(float I, float N, float eta);\n  // For the incident vector `I` and surface normal `N`, and the ratio of indices of refraction `eta`, return the refraction vector.\n  // The result is computed by:\n  //\n  // ```glslx\n  // float k = 1.0 - eta * eta * (1.0 - dot(N, I) * dot(N, I));\n  // if (k < 0.0) return vec2(0.0);\n  // else return eta * I - (eta * dot(N, I) + sqrt(k)) * N;\n  // ```\n  //\n  // The input parameters for the incident vector `I` and the surface normal `N`.\n  vec2 refract(vec2 I, vec2 N, float eta);\n  // For the incident vector `I` and surface normal `N`, and the ratio of indices of refraction `eta`, return the refraction vector.\n  // The result is computed by:\n  //\n  // ```glslx\n  // float k = 1.0 - eta * eta * (1.0 - dot(N, I) * dot(N, I));\n  // if (k < 0.0) return vec3(0.0);\n  // else return eta * I - (eta * dot(N, I) + sqrt(k)) * N;\n  // ```\n  //\n  // The input parameters for the incident vector `I` and the surface normal `N`.\n  vec3 refract(vec3 I, vec3 N, float eta);\n  // For the incident vector `I` and surface normal `N`, and the ratio of indices of refraction `eta`, return the refraction vector.\n  // The result is computed by:\n  //\n  // ```glslx\n  // float k = 1.0 - eta * eta * (1.0 - dot(N, I) * dot(N, I));\n  // if (k < 0.0) return vec4(0.0);\n  // else return eta * I - (eta * dot(N, I) + sqrt(k)) * N;\n  // ```\n  //\n  // The input parameters for the incident vector `I` and the surface normal `N`.\n  vec4 refract(vec4 I, vec4 N, float eta);\n\n  ////////////////////////////////////////////////////////////////////////////////\n  // Matrix Functions\n\n  // Multiply matrix `x` by matrix `y` component-wise, i.e., `result[i][j]` is the scalar product of `x[i][j]` and `y[i][j]`.\n  // Note: to get linear algebraic matrix multiplication, use the multiply operator (`*`).\n  mat2 matrixCompMult(mat2 x, mat2 y);\n  // Multiply matrix `x` by matrix `y` component-wise, i.e., `result[i][j]` is the scalar product of `x[i][j]` and `y[i][j]`.\n  // Note: to get linear algebraic matrix multiplication, use the multiply operator (`*`).\n  mat3 matrixCompMult(mat3 x, mat3 y);\n  // Multiply matrix `x` by matrix `y` component-wise, i.e., `result[i][j]` is the scalar product of `x[i][j]` and `y[i][j]`.\n  // Note: to get linear algebraic matrix multiplication, use the multiply operator (`*`).\n  mat4 matrixCompMult(mat4 x, mat4 y);\n\n  ////////////////////////////////////////////////////////////////////////////////\n  // Vector Relational Functions\n\n  // Returns the component-wise compare of `x < y`.\n  bvec2 lessThan(ivec2 x, ivec2 y);\n  // Returns the component-wise compare of `x < y`.\n  bvec2 lessThan(vec2 x, vec2 y);\n  // Returns the component-wise compare of `x < y`.\n  bvec3 lessThan(ivec3 x, ivec3 y);\n  // Returns the component-wise compare of `x < y`.\n  bvec3 lessThan(vec3 x, vec3 y);\n  // Returns the component-wise compare of `x < y`.\n  bvec4 lessThan(ivec4 x, ivec4 y);\n  // Returns the component-wise compare of `x < y`.\n  bvec4 lessThan(vec4 x, vec4 y);\n\n  // Returns the component-wise compare of `x <= y`.\n  bvec2 lessThanEqual(ivec2 x, ivec2 y);\n  // Returns the component-wise compare of `x <= y`.\n  bvec2 lessThanEqual(vec2 x, vec2 y);\n  // Returns the component-wise compare of `x <= y`.\n  bvec3 lessThanEqual(ivec3 x, ivec3 y);\n  // Returns the component-wise compare of `x <= y`.\n  bvec3 lessThanEqual(vec3 x, vec3 y);\n  // Returns the component-wise compare of `x <= y`.\n  bvec4 lessThanEqual(ivec4 x, ivec4 y);\n  // Returns the component-wise compare of `x <= y`.\n  bvec4 lessThanEqual(vec4 x, vec4 y);\n\n  // Returns the component-wise compare of `x > y`.\n  bvec2 greaterThan(ivec2 x, ivec2 y);\n  // Returns the component-wise compare of `x > y`.\n  bvec2 greaterThan(vec2 x, vec2 y);\n  // Returns the component-wise compare of `x > y`.\n  bvec3 greaterThan(ivec3 x, ivec3 y);\n  // Returns the component-wise compare of `x > y`.\n  bvec3 greaterThan(vec3 x, vec3 y);\n  // Returns the component-wise compare of `x > y`.\n  bvec4 greaterThan(ivec4 x, ivec4 y);\n  // Returns the component-wise compare of `x > y`.\n  bvec4 greaterThan(vec4 x, vec4 y);\n\n  // Returns the component-wise compare of `x >= y`.\n  bvec2 greaterThanEqual(ivec2 x, ivec2 y);\n  // Returns the component-wise compare of `x >= y`.\n  bvec2 greaterThanEqual(vec2 x, vec2 y);\n  // Returns the component-wise compare of `x >= y`.\n  bvec3 greaterThanEqual(ivec3 x, ivec3 y);\n  // Returns the component-wise compare of `x >= y`.\n  bvec3 greaterThanEqual(vec3 x, vec3 y);\n  // Returns the component-wise compare of `x >= y`.\n  bvec4 greaterThanEqual(ivec4 x, ivec4 y);\n  // Returns the component-wise compare of `x >= y`.\n  bvec4 greaterThanEqual(vec4 x, vec4 y);\n\n  // Returns the component-wise compare of `x == y`.\n  bvec2 equal(bvec2 x, bvec2 y);\n  // Returns the component-wise compare of `x == y`.\n  bvec2 equal(ivec2 x, ivec2 y);\n  // Returns the component-wise compare of `x == y`.\n  bvec2 equal(vec2 x, vec2 y);\n  // Returns the component-wise compare of `x == y`.\n  bvec3 equal(bvec3 x, bvec3 y);\n  // Returns the component-wise compare of `x == y`.\n  bvec3 equal(ivec3 x, ivec3 y);\n  // Returns the component-wise compare of `x == y`.\n  bvec3 equal(vec3 x, vec3 y);\n  // Returns the component-wise compare of `x == y`.\n  bvec4 equal(bvec4 x, bvec4 y);\n  // Returns the component-wise compare of `x == y`.\n  bvec4 equal(ivec4 x, ivec4 y);\n  // Returns the component-wise compare of `x == y`.\n  bvec4 equal(vec4 x, vec4 y);\n\n  // Returns the component-wise compare of `x != y`.\n  bvec2 notEqual(bvec2 x, bvec2 y);\n  // Returns the component-wise compare of `x != y`.\n  bvec2 notEqual(ivec2 x, ivec2 y);\n  // Returns the component-wise compare of `x != y`.\n  bvec2 notEqual(vec2 x, vec2 y);\n  // Returns the component-wise compare of `x != y`.\n  bvec3 notEqual(bvec3 x, bvec3 y);\n  // Returns the component-wise compare of `x != y`.\n  bvec3 notEqual(ivec3 x, ivec3 y);\n  // Returns the component-wise compare of `x != y`.\n  bvec3 notEqual(vec3 x, vec3 y);\n  // Returns the component-wise compare of `x != y`.\n  bvec4 notEqual(bvec4 x, bvec4 y);\n  // Returns the component-wise compare of `x != y`.\n  bvec4 notEqual(ivec4 x, ivec4 y);\n  // Returns the component-wise compare of `x != y`.\n  bvec4 notEqual(vec4 x, vec4 y);\n\n  // Returns true if any component of `x` is `true`.\n  bool any(bvec2 x);\n  // Returns true if any component of `x` is `true`.\n  bool any(bvec3 x);\n  // Returns true if any component of `x` is `true`.\n  bool any(bvec4 x);\n\n  // Returns true only if all components of `x` are `true`.\n  bool all(bvec2 x);\n  // Returns true only if all components of `x` are `true`.\n  bool all(bvec3 x);\n  // Returns true only if all components of `x` are `true`.\n  bool all(bvec4 x);\n\n  // Returns the component-wise logical complement of `x`.\n  bvec2 not(bvec2 x);\n  // Returns the component-wise logical complement of `x`.\n  bvec3 not(bvec3 x);\n  // Returns the component-wise logical complement of `x`.\n  bvec4 not(bvec4 x);\n\n  ////////////////////////////////////////////////////////////////////////////////\n  // Texture Lookup Functions\n\n  // Use the texture coordinate `coord` to do a texture lookup in the 2D texture currently bound to `sampler`.\n  vec4 texture2D(sampler2D sampler, vec2 coord);\n  // Use the texture coordinate `coord` to do a texture lookup in the 2D texture currently bound to `sampler`.\n  vec4 texture2D(sampler2D sampler, vec2 coord, float bias);\n  // Use the texture coordinate `coord` to do a texture lookup in the 2D texture currently bound to `sampler`.\n  vec4 texture2DLod(sampler2D sampler, vec2 coord, float lod);\n  // Use the texture coordinate `coord` to do a texture lookup in the 2D texture currently bound to `sampler`.\n  // The texture coordinate `(coord.s, coord.t)` is divided by the last component of `coord`.\n  vec4 texture2DProj(sampler2D sampler, vec3 coord);\n  // Use the texture coordinate `coord` to do a texture lookup in the 2D texture currently bound to `sampler`.\n  // The texture coordinate `(coord.s, coord.t)` is divided by the last component of `coord`.\n  vec4 texture2DProj(sampler2D sampler, vec3 coord, float bias);\n  // Use the texture coordinate `coord` to do a texture lookup in the 2D texture currently bound to `sampler`.\n  // The texture coordinate `(coord.s, coord.t)` is divided by the last component of `coord`.\n  vec4 texture2DProjLod(sampler2D sampler, vec3 coord, float lod);\n  // Use the texture coordinate `coord` to do a texture lookup in the 2D texture currently bound to `sampler`.\n  // The texture coordinate `(coord.s, coord.t)` is divided by the last component of `coord`. The third component of `coord` is ignored.\n  vec4 texture2DProj(sampler2D sampler, vec4 coord);\n  // Use the texture coordinate `coord` to do a texture lookup in the 2D texture currently bound to `sampler`.\n  // The texture coordinate `(coord.s, coord.t)` is divided by the last component of `coord`. The third component of `coord` is ignored.\n  vec4 texture2DProj(sampler2D sampler, vec4 coord, float bias);\n  // Use the texture coordinate `coord` to do a texture lookup in the 2D texture currently bound to `sampler`.\n  // The texture coordinate `(coord.s, coord.t)` is divided by the last component of `coord`. The third component of `coord` is ignored.\n  vec4 texture2DProjLod(sampler2D sampler, vec4 coord, float lod);\n\n  // Use the texture coordinate `coord` to do a texture lookup in the cube map texture currently bound to `sampler`.\n  // The direction of `coord` is used to select which face to do a 2-dimensional texture lookup in.\n  vec4 textureCube(samplerCube sampler, vec3 coord);\n  // Use the texture coordinate `coord` to do a texture lookup in the cube map texture currently bound to `sampler`.\n  // The direction of `coord` is used to select which face to do a 2-dimensional texture lookup in.\n  vec4 textureCube(samplerCube sampler, vec3 coord, float bias);\n  // Use the texture coordinate `coord` to do a texture lookup in the cube map texture currently bound to `sampler`.\n  // The direction of `coord` is used to select which face to do a 2-dimensional texture lookup in.\n  vec4 textureCubeLod(samplerCube sampler, vec3 coord, float lod);\n\n  #extension GL_OES_standard_derivatives {\n    // Available only in the fragment shader, this function returns the partial derivative of expression `p` with respect to the window `x` coordinate.\n    //\n    // Expressions that imply higher order derivatives such as `dFdx(dFdx(n))` have undefined results, as do mixed-order derivatives such as\n    // `dFdx(dFdy(n))`. It is assumed that the expression `p` is continuous and therefore, expressions evaluated via non-uniform control flow may be undefined.\n    float dFdx(float v);\n    // Available only in the fragment shader, this function returns the partial derivative of expression `p` with respect to the window `x` coordinate.\n    //\n    // Expressions that imply higher order derivatives such as `dFdx(dFdx(n))` have undefined results, as do mixed-order derivatives such as\n    // `dFdx(dFdy(n))`. It is assumed that the expression `p` is continuous and therefore, expressions evaluated via non-uniform control flow may be undefined.\n    vec2 dFdx(vec2 v);\n    // Available only in the fragment shader, this function returns the partial derivative of expression `p` with respect to the window `x` coordinate.\n    //\n    // Expressions that imply higher order derivatives such as `dFdx(dFdx(n))` have undefined results, as do mixed-order derivatives such as\n    // `dFdx(dFdy(n))`. It is assumed that the expression `p` is continuous and therefore, expressions evaluated via non-uniform control flow may be undefined.\n    vec3 dFdx(vec3 v);\n    // Available only in the fragment shader, this function returns the partial derivative of expression `p` with respect to the window `x` coordinate.\n    //\n    // Expressions that imply higher order derivatives such as `dFdx(dFdx(n))` have undefined results, as do mixed-order derivatives such as\n    // `dFdx(dFdy(n))`. It is assumed that the expression `p` is continuous and therefore, expressions evaluated via non-uniform control flow may be undefined.\n    vec4 dFdx(vec4 v);\n\n    // Available only in the fragment shader, this function returns the partial derivative of expression `p` with respect to the window `y` coordinate.\n    //\n    // Expressions that imply higher order derivatives such as `dFdy(dFdy(n))` have undefined results, as do mixed-order derivatives such as\n    // `dFdx(dFdy(n))`. It is assumed that the expression `p` is continuous and therefore, expressions evaluated via non-uniform control flow may be undefined.\n    float dFdy(float v);\n    // Available only in the fragment shader, this function returns the partial derivative of expression `p` with respect to the window `y` coordinate.\n    //\n    // Expressions that imply higher order derivatives such as `dFdy(dFdy(n))` have undefined results, as do mixed-order derivatives such as\n    // `dFdx(dFdy(n))`. It is assumed that the expression `p` is continuous and therefore, expressions evaluated via non-uniform control flow may be undefined.\n    vec2 dFdy(vec2 v);\n    // Available only in the fragment shader, this function returns the partial derivative of expression `p` with respect to the window `y` coordinate.\n    //\n    // Expressions that imply higher order derivatives such as `dFdy(dFdy(n))` have undefined results, as do mixed-order derivatives such as\n    // `dFdx(dFdy(n))`. It is assumed that the expression `p` is continuous and therefore, expressions evaluated via non-uniform control flow may be undefined.\n    vec3 dFdy(vec3 v);\n    // Available only in the fragment shader, this function returns the partial derivative of expression `p` with respect to the window `y` coordinate.\n    //\n    // Expressions that imply higher order derivatives such as `dFdy(dFdy(n))` have undefined results, as do mixed-order derivatives such as\n    // `dFdx(dFdy(n))`. It is assumed that the expression `p` is continuous and therefore, expressions evaluated via non-uniform control flow may be undefined.\n    vec4 dFdy(vec4 v);\n\n    // Returns the sum of the absolute derivative in `x` and `y` using local differencing for the input argument `p`, i.e. `abs(dFdx(p)) + abs(dFdy(p))`\n    float fwidth(float v);\n    // Returns the sum of the absolute derivative in `x` and `y` using local differencing for the input argument `p`, i.e. `abs(dFdx(p)) + abs(dFdy(p))`\n    vec2 fwidth(vec2 v);\n    // Returns the sum of the absolute derivative in `x` and `y` using local differencing for the input argument `p`, i.e. `abs(dFdx(p)) + abs(dFdy(p))`\n    vec3 fwidth(vec3 v);\n    // Returns the sum of the absolute derivative in `x` and `y` using local differencing for the input argument `p`, i.e. `abs(dFdx(p)) + abs(dFdy(p))`\n    vec4 fwidth(vec4 v);\n  }\n\n  #extension GL_EXT_frag_depth {\n    // Available only in the fragment language, `gl_FragDepthEXT` is an output variable that is used to establish the depth value for the current fragment.\n    // If depth buffering is enabled and no shader writes to `gl_FragDepthEXT`, then the fixed function value for depth will be used (this value is contained\n    // in the `z` component of `gl_FragCoord`) otherwise, the value written to `gl_FragDepthEXT` is used.\n    //\n    // If a shader statically assigns to `gl_FragDepthEXT`, then the value of the fragment's depth may be undefined for executions of the shader that take\n    // that path. That is, if the set of linked fragment shaders statically contain a write to `gl_FragDepthEXT`, then it is responsible for always writing it.\n    float gl_FragDepthEXT;\n  }\n\n  #extension GL_EXT_shader_texture_lod {\n    vec4 texture2DGradEXT(sampler2D sampler, vec2 P, vec2 dPdx, vec2 dPdy);\n    vec4 texture2DLodEXT(sampler2D sampler, vec2 coord, float lod);\n    vec4 texture2DProjGradEXT(sampler2D sampler, vec3 P, vec2 dPdx, vec2 dPdy);\n    vec4 texture2DProjGradEXT(sampler2D sampler, vec4 P, vec2 dPdx, vec2 dPdy);\n    vec4 texture2DProjLodEXT(sampler2D sampler, vec3 coord, float lod);\n    vec4 texture2DProjLodEXT(sampler2D sampler, vec4 coord, float lod);\n    vec4 textureCubeGradEXT(samplerCube sampler, vec3 P, vec3 dPdx, vec3 dPdy);\n    vec4 textureCubeLodEXT(samplerCube sampler, vec3 coord, float lod);\n  }\n}\n");
return xc(a,e,0),b.unshift(e),c?1:0}function Lf(a,b,c){if(a.d)return null;for(var k=0,q=b.length;k<q;k=k+1|0){var d=b[k];xc(a,d,0)}for(var e=ie(a,b),f=new Gi(0),h=new Ti(1,null),m=new qi(c.e),l=new Ri(a,m,e),g=[],y=0,s=b.length;y<s;y=y+1|0){var i=b[y],o=Od(a,i.c,f,m,h,l);Kf(g,o.a)}return Td(l,f),new ti(f,g)}function je(a,b,c){if(a.d)return null;for(var T=0,fa=b.length;T<fa;T=T+1|0){var d=b[T];xc(a,d,0)}for(var e=ie(a,b),f=new Gi(0),h=new Ti(1,null),m=new qi(c.e),l=new Ri(a,m,e),ha=0,X=b.length;ha<X;ha=ha+1|0){var g=b[ha];Od(a,g.c,f,m,h,l)}if(Td(l,f),a.d)return null;for(var i=[],o=[],ia=0,ma=Mf(h),za=ma.length;ia<za;ia=ia+1|0){for(var k=ma[ia],q=new Gi(0),y=new Ti(1,null),s=new qi(c.e),z=new Fi,p=new Ri(z,s,e),ca=0,aa=b.length;ca<aa;ca=ca+1|0){var J=b[ca];Od(z,J.c,q,s,y,p)}Td(p,q),Nf(y,k),nh(q,s,c,e),o.push(q),i.push(new ri(k.c,'',rh(k.e))),e^1||Of(q,k)}for(var x=ah(new Pi(c.c),o),Q=0,ga=i.length;Q<ga;Q=Q+1|0)i[Q].b=new vi(o[Q],c).a;return new si(i,x)}function Mf(a){for(var b=[],d=0,e=Array.from(a.c.values()),f=e.length;d<f;d=d+1|0){
var c=e[d];c instanceof wc&&c.e&49152&&b.push(c)}return b}function Nf(a,b){for(var e=0,f=Array.from(a.c.values()),h=f.length;e<h;e=e+1|0){var c=f[e];if(c.a^b.a)c.e&=-49153;else{c.c='main';var d=c.s;d&&(d.c=c.c)}}}function Of(a,b){var c=(b.e&16384)!=0,d=(b.e&32768)!=0;if(c!=d)for(var e=a.g;e;e=e.k)if(e.b==17){var f=e.h|0;f&64&&Y(e,f&-65|(d?32:1024))}}function Pf(c,a){var b=a.l;c.b.push(c.b.length==0||Mc(c.b)),b&&Je(b.b)&&c.a.push(!1)}function Qf(d,a){var b=a.l,c=d.b.pop();c&&(a.q=!0),b&&Je(b.b)&&!d.a.pop()&&(b.b==19&&Ua(b.g)||b.b==7&&Ua(b.i)||b.b==10&&(!Ja(b)||Ua(Ja(b))))&&bb(d.b,!1)}function ke(e,a){if(Mc(e.b))switch(a.b){case 4:e.a.length&&bb(e.a,!0),bb(e.b,!1);break;case 15:case 6:case 5:bb(e.b,!1);break;case 12:var b=a.g,c=a.g.k,d=a.g.k.k;Ua(b)?c.q||bb(e.b,!1):Ob(b)&&d?d.q||bb(e.b,!1):c&&d&&!c.q&&!d.q&&bb(e.b,!1);break}}function Rf(a,b){return le(a)||le(b)||a.b!=b.b&&(a.b==18||a.b==14||b.b==14||a.b==9||b.b==9)}function le(a){return a.b==11&&a.e.q!=null}function me(a,b){var c=a.toString(),d=a.toExponential();
d.length<c.length&&(c=d);var e=c.indexOf('e'),f='';return ~e&&(f=c.slice(e),c=c.slice(0,e)),c=(+(+c).toFixed(6)).toString(),f==''&&c.indexOf('.')==-1&&(c+=b^1?'.0':'.'),b==1&&c.startsWith('0.')&&c!='0.'&&(c=c.slice(1)),b==1&&c.startsWith('-0.')&&c!='-0.'&&(c='-'+c.slice(2)),c+f}function Sf(b,a){switch(a.b){case 11:case 16:return (a.e.e&65536)!=0;case 17:return ((a.h|0)&65536)!=0}return!1}function ud(a){a.f||(a.b+=a.c)}function vd(a){a.f||(a.b=a.b.slice(a.c.length))}function cb(i,a){switch(a.b){case 3:if(!a.g&&a.l)i.a+=';';else{i.a+='{'+i.d,ud(i);for(var b=a.g;b;b=b.k)i.a+=i.b,cb(i,b),i.a+=i.d;vd(i),i.a+=i.b+'}'}break;case 4:i.a+='break;';break;case 5:i.a+='continue;';break;case 6:i.a+='discard;';break;case 7:i.a+='do',nc(i,a.g,0),i.a+=i.d+i.b+'while'+i.e+'(',P(i,a.i,0),i.a+=');';break;case 8:P(i,a.g,0),i.a+=';';break;case 9:ne(i),i.a+='#extension '+a.m+i.e+':'+i.e;switch(a.h|0){case 1:i.a+='disable';break;case 2:i.a+='enable';break;case 3:i.a+='require';break;case 4:i.a+='warn';break}oe(i);break;case 10:i.a+='for'+i.e+'(',
Ab(a)?Ab(a).b^17?(P(i,Ab(a),0),i.a+=';'):cb(i,Ab(a)):i.a+=';',Ja(a)&&(i.a+=i.e,P(i,Ja(a),0)),i.a+=';',Yc(a)&&(i.a+=i.e,P(i,Yc(a),0)),i.a+=')',nc(i,a.i,1);break;case 11:var c=a.e;i.a+=ib(c.e,i.e),P(i,c.k,0),i.a+=' ',i.a+=c.c,i.a+='(';for(var o=0,k=c.o,q=k.length;o<q;o=o+1|0){var d=k[o];d!=c.o[0]&&(i.a+=','+i.e),i.a+=ib(d.e,i.e),P(i,d.p,0),i.a+=' ',wd(i,d)}i.a+=')',c.q?(i.a+=i.e,cb(i,c.q)):i.a+=';';break;case 12:i.a+='if'+i.e+'(',P(i,a.g,0),i.a+=')',nc(i,a.g.k,1),a.g.k.k&&(i.a+=i.d+i.b+'else',a.g.k.k.b^12?nc(i,a.g.k.k,0):(i.a+=' ',cb(i,a.g.k.k)));break;case 14:i.a+='precision ',i.a+=ib(a.h|0,i.e),P(i,a.g,0),i.a+=';';break;case 15:var e=a.g;i.a+='return',e&&(Dg(e.b)||(i.a+=' '),P(i,e,0)),i.a+=';';break;case 16:var f=a.e;i.a+=ib(f.e,i.e),i.a+='struct '+f.c+i.e+'{'+i.d,ud(i);for(var h=a.g.g;h;h=h.k)i.a+=i.b,cb(i,h),i.a+=i.d;if(vd(i),i.a+=i.b+'}',a.g.k)for(var m=a.g.k.g.k;m;m=m.k)i.a+=m.o.o?','+i.e:i.e,wd(i,m.e);i.a+=';';break;case 17:i.a+=ib(a.h|0,i.e),P(i,a.g,0);for(var l=a.g.k;l;l=l.k){var g=l.e;i.a+=l.o.o?','+i.e:' ',
wd(i,g)}i.a+=';';break;case 18:ne(i),i.a+='#version '+a.m,oe(i);break;case 19:i.a+='while'+i.e+'(',P(i,a.g,0),i.a+=')',nc(i,a.i,1);break;default:i.a+=Rh[a.b];break}}function ne(a){a.a!=''&&!a.a.endsWith('\n')&&(a.a+='\n')}function oe(a){a.f&&(a.a+='\n')}function nc(c,a,b){a.b^3?(c.a+=c.f&&!b?' ':c.d,ud(c),c.a+=c.b,cb(c,a),vd(c)):(c.a+=c.e,cb(c,a))}function pe(c,a){for(var b=a;b;b=b.k)b!=a&&(c.a+=','+c.e),P(c,b,1)}function wd(b,a){b.a+=a.c,a.x&&(b.a+='[',P(b,a.x,0),b.a+=']'),V(a)&&(b.a+=b.e+'='+b.e,P(b,V(a),1))}function P(d,a,b){switch(a.b){case 20:P(d,a.g,15),d.a+='(',pe(d,a.g.k),d.a+=')';break;case 21:P(d,a.g,16),d.a+='.',d.a+=a.e?a.e.c:a.m;break;case 22:2<b&&(d.a+='('),P(d,a.g,3),d.a+=d.e+'?'+d.e,P(d,a.g.k,2),d.a+=d.e+':'+d.e,P(d,a.i,2),2<b&&(d.a+=')');break;case 23:d.a+=a.e.c;break;case 25:0<b&&(d.a+='('),pe(d,a.g),0<b&&(d.a+=')');break;case 26:var c=a.g;d.a+=sf(a.f).a.c,c&&(d.a+='[',P(d,c,0),d.a+=']');break;case 28:d.a+=(!!a.h).toString();break;case 29:d.a+=me(a.h,d.f?1:0);break;case 30:d.a+=(a.h|0).toString();
break;case 31:d.a+=(a.h|0)+'u';break;case 48:P(d,a.g,16),d.a+='[',P(d,a.i,0),d.a+=']';break;case 32:Lb(d,'~',a,b);break;case 33:Lb(d,'-',a,b);break;case 34:Lb(d,'!',a,b);break;case 35:Lb(d,'+',a,b);break;case 36:Lb(d,'--',a,b);break;case 37:Lb(d,'++',a,b);break;case 38:qe(d,'--',a,b);break;case 39:qe(d,'++',a,b);break;case 40:L(d,'+',a,b,12);break;case 41:L(d,'&',a,b,12);break;case 42:L(d,'|',a,b,12);break;case 43:L(d,'^',a,b,12);break;case 44:L(d,'/',a,b,13);break;case 45:L(d,'==',a,b,10);break;case 46:L(d,'>',a,b,10);break;case 47:L(d,'>=',a,b,10);break;case 49:L(d,'<',a,b,10);break;case 50:L(d,'<=',a,b,10);break;case 51:L(d,'&&',a,b,5);break;case 52:L(d,'||',a,b,3);break;case 53:L(d,'^^',a,b,4);break;case 54:L(d,'*',a,b,13);break;case 55:L(d,'!=',a,b,10);break;case 56:L(d,'%',a,b,13);break;case 57:L(d,'<<',a,b,12);break;case 58:L(d,'>>',a,b,12);break;case 59:L(d,'-',a,b,12);break;case 60:L(d,'=',a,b,2);break;case 61:L(d,'+=',a,b,2);break;case 62:L(d,'&=',a,b,2);break;case 63:L(d,'|=',a,b,2);break;case 64:L(d,
'^=',a,b,2);break;case 65:L(d,'/=',a,b,2);break;case 66:L(d,'*=',a,b,2);break;case 67:L(d,'%=',a,b,2);break;case 68:L(d,'<<=',a,b,2);break;case 69:L(d,'>>=',a,b,2);break;case 70:L(d,'-=',a,b,2);break;default:d.a+=Rh[a.b];break}}function Lb(f,a,b,c){var d=b.g,e=d.b;f.a+=a,(a.charCodeAt(0)==45&&(e==33||e==36||Jg(d))||a.charCodeAt(0)==43&&(e==35||e==37))&&(f.a+=' '),P(f,d,14)}function qe(d,a,b,c){P(d,b.g,15),d.a+=a}function L(f,a,b,c,d){var e=Uc(b.b);d<c&&(f.a+='('),P(f,b.g,d+(e|0)|0),f.a+=f.e+a+f.e,P(f,b.i,d+(!e|0)|0),d<c&&(f.a+=')')}function O(a){if(a.f==I)return null;switch(a.b){case 30:case 29:case 28:return Sa(a);case 23:return Tf(a);case 25:return Uf(a);case 22:return Vf(a);case 21:return Wf(a);case 48:return Xf(a);case 20:return Yf(a);case 33:return we(a,function(b){return-b},function(c){return -c|0});case 34:return dg(a,function(d){return!d});case 35:return we(a,function(e){return+e},function(f){return+f});case 40:return yd(a,function(h,m){return h+m},function(l,g){return l+g|0});case 59:return yd(a,function(i,o){
return i-o},function(k,q){return k-q|0});case 54:return Zf(a);case 44:return yd(a,function(y,s){return s!=0?y/s:0},function(z,p){return p?z/p|0:0});case 45:case 55:return ag(a);case 51:return xd(a,function(J,x){return J&&x});case 52:return xd(a,function(Q,T){return Q||T});case 53:return xd(a,function(fa,ha){return fa!=ha});case 46:return Oc(a,function(X,ca){return X>ca});case 47:return Oc(a,function(aa,ia){return aa>=ia});case 49:return Oc(a,function(ma,za){return ma<za});case 50:return Oc(a,function(ga,la){return ga<=la})}return null}function Tf(a){var b=a.e;if(b&&b.e&4){if(b.h)return Sa(b.h);if(b.z)return t(new Gi(27),a.f)}return null}function Uf(a){for(var b=a.g;b;b=b.k){var c=O(b);if(!c||b==a.i)return c}return null}function Vf(a){var b=O(a.g),c=O(a.g.k),d=O(a.i);return b&&b.b==28&&c&&d?!b.h?d:c:null}function Wf(a){var b=O(a.g);if(b&&b.b==20){var c=b.f,d=a.m;if(ad(c))for(var e=d.length,f=Ka(c),y=0,s=Yd(f),z=s.length;y<z;y=y+1|0){var h=s[y];if(~h.indexOf(d[0])){if(e==1)return r(qc(b,1+h.indexOf(d)|0));for(var m=Zd(S(c),
e),l=Da(m),g=0;g<e;g=g+1|0)n(l,Sa(qc(b,1+h.indexOf(d[g])|0)));return l}}else if(c.a&&c.a instanceof vc)for(var i=c.a,o=i.i,k=0,p=o.length;k<p;k=k+1|0){var q=o[k];if(q.c==d)return r(qc(b,1+k|0))}}return null}function Xf(a){var b=O(a.g),c=O(a.i);if(b&&b.b==20&&c&&c.b==30){var d=b.f;if(ad(d)){var e=W(d),f=c.h|0;if(-1<f&&f<e)return r(qc(b,f+1|0))}else if(xa(d)){var h=W(d),m=c.h|0;if(-1<m&&m<h){for(var l=wa(d),g=Da(l),i=qc(b,mi(m,h)),o=0;o<h;o=o+1|0)n(g,r(i.k));return g}}}return null}function Yf(a){var b=a.g;if(a.f==ea&&b.b==21&&b.m=='length'&&Vc(a)){var c=b.g.f;if(c.b&&c.c>0)return t(t(Y(new Gi(30),c.c),ea),ea)}if(b.b^26)return null;for(var d=b.f,e=S(d),f=0,h=0,m=[],l=0,g=b.k;g;g=g.k){var i=O(g);if(!i)return null;if(i.b==20&&e&&S(i.g.f))for(var o=i.g.k;o;o=o.k){var k=re(e,o);if(!k)return null;m.push(k)}else{if(e&&(i=re(e,i),!i))return null;m.push(i)}xa(i.f)&&(f=W(i.f),h=W(wa(i.f))),l=l+1|0}return xa(d)&&f&&h&&l^1?null:S(d)?_f(m,d,xa(d)?f:0,xa(d)?h:0):d.a&&d.a instanceof vc?$f(m,d):null}function Mb(a){for(var b=[],c=a.g.k;c;c=c.k)b.push(c.h);
return b}function Zf(a){var la,b=O(a.g),c=O(a.i),d=b&&b.f,e=c&&c.f;if(b&&c){if(d==ob&&e==ac||d==pb&&e==bc||d==qb&&e==cc){for(var f=W(d),h=Da(d),m=Mb(b),l=Mb(c),g=0;g<f;g=g+1|0){for(var i=0,o=0;o<f;o=o+1|0)i+=m[o]*l[o+mi(g,f)|0];n(h,t(sa(new Gi(29),i),ka))}return h}if(d==ac&&e==ob||d==bc&&e==pb||d==cc&&e==qb){for(var k=W(d),q=Da(e),y=Mb(b),s=Mb(c),z=0;z<k;z=z+1|0){for(var p=0,J=0;J<k;J=J+1|0)p+=y[z+mi(J,k)|0]*s[J];n(q,t(sa(new Gi(29),p),ka))}return q}if(xa(d)&&e==d){for(var x=W(d),Q=Da(d),T=Mb(b),fa=Mb(c),ha=0;ha<x;ha=ha+1|0)for(var X=0;X<x;X=X+1|0){for(var ca=0,aa=0;aa<x;aa=aa+1|0)ca+=T[X+mi(aa,x)|0]*fa[aa+mi(ha,x)|0];n(Q,t(sa(new Gi(29),ca),ka))}return Q}return (la=ue(b,c,function(ia,ma){return ia*ma}))||ve(b,c,function(za,ga){return mi(za,ga)})}return null}function re(a,b){var c=0;switch(b.b){case 28:c=+!!b.h;break;case 30:c=b.h|0;break;case 29:c=b.h;break;default:return null}switch(a){case ja:return t(Ia(new Gi(28),!!c),ja);case ea:return t(Y(new Gi(30),c|0),ea);case ka:return t(sa(new Gi(29),c),ka)}return null}
function _f(a,b,c,d){var e=Ka(b),f=S(b),h=Da(b);if(a.length^1){if(c&&d){for(var q=W(b),y=W(wa(b)),s=0;s<q;s=s+1|0)for(var z=0;z<y;z=z+1|0)n(h,s<c&&z<d?a[z+mi(s,d)|0]:t(sa(new Gi(29),s^z?0:1),ka))}else{if(a.length<e)return null;for(var p=0;p<e;p=p+1|0){var J=a[p];if(J.f!=f)return null;n(h,J)}}}else{var m=a[0];if(m.f!=f)return null;if(xa(b)){for(var l=W(b),g=W(wa(b)),i=0;i<l;i=i+1|0)for(var o=0;o<g;o=o+1|0)n(h,o^i?t(sa(new Gi(29),0),ka):Sa(m))}else for(var k=0;k<e;k=k+1|0)n(h,Sa(m))}return wa(b)?h:r(h.i)}function $f(a,b){var c=b.a.i,d=Da(b);if(a.length^c.length)return null;for(var e=0,f=a.length;e<f;e=e+1|0){if(a[e].f!=da(c[e]))return null;n(d,a[e])}return d}function ag(a){var b=O(a.g),c=O(a.i);if(b&&c){var d=ua(b,c);return t(Ia(new Gi(28),a.b^45?!d:d),ja)}return null}function se(a,b,c,d){if(a.b==20&&a.g.b==26&&S(a.g.f)==b){for(var e=Da(a.g.f),f=a.g.k;f;f=f.k){var h=O(f);if(!h||h.b^c)return null;n(e,d(h))}return e}return null}function bg(a,b){return a.b^29?se(a,ka,29,function(c){return t(sa(new Gi(29),b(c.h)),ka)}):t(sa(new Gi(29),
b(a.h)),ka)}function cg(a,b){return a.b^30?se(a,ea,30,function(c){return t(Y(new Gi(30),b(c.h|0)),ea)}):t(Y(new Gi(30),b(a.h|0)),ea)}function te(a,b,c,d,e){var f=a.b==20&&a.g.b==26&&S(a.g.f)==c,h=b.b==20&&b.g.b==26&&S(b.g.f)==c;if(f&&h&&b.f==a.f){for(var m=Da(a.f),l=a.g.k,g=b.g.k;l&&g;){var i=O(l),o=O(g);if(!i||i.b^d||!o||o.b^d)return null;n(m,e(i,o)),l=l.k,g=g.k}if(!l&&!g)return m}else if(f&&b.b==d){for(var k=Da(a.f),q=a.g.k;q;q=q.k){var y=O(q);if(!y||y.b^d)return null;n(k,e(y,b))}return k}else if(a.b==d&&h){for(var s=Da(b.f),z=b.g.k;z;z=z.k){var p=O(z);if(!p||p.b^d)return null;n(s,e(a,p))}return s}return null}function ue(a,b,c){return a.b==29&&b.b==29?t(sa(new Gi(29),c(a.h,b.h)),ka):te(a,b,ka,29,function(d,e){return t(sa(new Gi(29),c(d.h,e.h)),ka)})}function ve(a,b,c){return a.b==30&&b.b==30?t(Y(new Gi(30),c(a.h|0,b.h|0)),ea):te(a,b,ea,30,function(d,e){return t(Y(new Gi(30),c(d.h|0,e.h|0)),ea)})}function dg(a,b){var c=O(a.g);return c&&c.b==28?t(Ia(new Gi(28),b(!!c.h)),ja):null}function we(a,b,c){var e,d=O(a.g);
return d&&((e=bg(d,b))||cg(d,c))}function xd(a,b){var c=O(a.g),d=O(a.i);return c&&d&&c.b==28&&d.b==28?t(Ia(new Gi(28),b(!!c.h,!!d.h)),ja):null}function yd(a,b,c){var f,d=O(a.g),e=O(a.i);return d&&e?(f=ue(d,e,b))||ve(d,e,c):null}function Oc(a,b){var c=O(a.g),d=O(a.i);if(c&&d){if(c.b==29&&d.b==29)return t(Ia(new Gi(28),b(c.h,d.h)),ja);if(c.b==30&&d.b==30)return t(Ia(new Gi(28),b(c.h|0,d.h|0)),ja)}return null}function xe(a,b,c){for(var d=0,e=0,f=a.length;e<f;){var h=a.charCodeAt(e);e=e+1|0,Pc(h)&&(d=d+1|0,h==13&&e<f&&a.charCodeAt(e)==10&&(e=e+1|0))}return d>2&&(d=2),mc(c,d)}function eg(a,b,c){switch(c){case 78:switch(b){case 95:return!0;case 126:return!1}break;case 77:switch(b){case 92:return!0;case 126:return!1}break;case 95:if(b==95)return!0;break;case 92:if(b==92)return!0;break;case 115:case 114:if(rf(b)||b==118||b==50)return!1;break;case 111:case 120:case 119:case 118:case 112:return!1;case 117:if(b==113)return!1;break}switch(b){case 115:case 114:case 112:case 79:case 76:return!1;case 95:case 92:switch(a){case 130:case 115:case 114:case 111:case 110:case 120:case 79:case 76:case 32:case 12:
return!1}if(th(a))return!1;break;case 78:case 77:if(c==126)return!1;break}return!0}function Pc(a){return a==10||a==13}function fg(a){for(var b=0,c=a.length;b<c;b=b+1|0)if(Pc(a.charCodeAt(b)))return!0;return!1}function ye(a){return a==32||a==9}function Qc(a){for(var b=0,c=a.length;b<c;b=b+1|0)if(!ye(a.charCodeAt(b)))return!1;return!0}function gg(a){for(var b=a.length;ye(a.charCodeAt(b-1|0));)b=b-1|0;return a.slice(0,b)}function hg(a,b,c,d){for(var e=[],f=0,h=0,m=a.length;h<m;h=h+1|0){var l=a.charCodeAt(h);Pc(l)&&(e.push(a.slice(f,h)),l==13&&(h+1|0)<m&&a.charCodeAt(h+1|0)==10&&(h=h+1|0),f=h+1|0)}e.push(a.slice(f));for(var g=b.length;g>0&&!Pc(b.charCodeAt(g-1|0));)g=g-1|0;for(var i=b.slice(g),o=i,p=0,J=e.slice(1),x=J.length;p<x;p=p+1|0){var k=J[p];if(!Qc(k)){for(var q=0,y=k.length;q<y&&q<o.length&&k.charCodeAt(q)==o.charCodeAt(q);)q=q+1|0;o=o.slice(0,q)}}for(var s='',Q=0,T=e.length;Q<T;Q=Q+1|0){var z=e[Q];s==''?(Qc(i)&&(s+=i.slice(o.length)),s+=z):(s+=d,Qc(z)||(s+=c+z.slice(o.length)))}return s}function ig(a,b,c,d){
var e=new Fi,f=new Vi('<stdin>',a);if(xc(e,f,1),e.d)return a;var h='',m=f.c.a,l=130,g=130,i=0,o=0,k=[],q=!0,y=0,s=null,z=!1,p=function(){o=o+1|0,k.push(1)},J=function(){o=o-k.pop()|0},x=function(){z&&k.length&&Mc(k)>0?(bb(k,0),k.push(1)):p()},Q=function(T){return!Qc(a.slice(m[T-1|0].a.c,m[T].a.b))},fa=function(ha,X){switch(ha){case 120:case 117:return!0;case 4:case 9:case 10:case 11:case 12:case 15:case 17:case 32:case 43:return!X}return!1},ca=null,aa=function(){for(var ia=!0,ma=y;ma<m.length;ma=ma+1|0){switch(m[ma].b){case 0:case 1:continue;case 113:ia=!1;break}break}ia&&p(),ca(),ia&&J()},za=function(){var ga=!0;return m[y].b==115&&Q(y+1|0)&&(ga=!1),ga&&p(),s(function(la){return la==115})?(ga&&J(),!0):(ga&&J(),!1)};ca=function(){for(;s(function(kb){return kb==0||kb==1}););switch(m[y].b){case 130:case 117:return!1;case 75:case 72:case 74:case 73:case 71:s(function(ra){return!0});break;case 35:s(function(Za){return!0}),s(function(Xa){return Xa==126});break;case 17:s(function(Na){return!0}),za()&&aa(),m[y].b^12||ca();
break;case 15:case 43:s(function(lb){return!0}),za()&&aa();break;case 11:s(function(Ya){return!0}),aa(),s(function(Aa){return Aa==43})&&(za(),s(function(mb){return mb==120}));break;case 12:if(s(function(Fa){return!0}),m[y].b^17)aa();else{var Ba=Q(y);Ba&&p(),s(function(Yb){return!0}),za()&&aa(),Ba&&J()}break;case 113:case 120:s(function(rb){return!0});break;case 121:case 122:case 123:case 124:for(s(function(nb){return!0});s(function(_a){return!Q(y)}););break;default:var sb=rf(m[y].b);if(s(function(Zb){return!0}),sb&&s(function(La){return La==114}),sb&&s(function(Fc){return Fc==126})&&s(function(md){return md==115}))s(function(nd){return nd==113})||s(function(Gc){return Gc==120});else{for(var tb=!1;!tb&&Q(y)&&(tb=!0,p()),s(function(_b){return!fa(_b,!1)}););s(function($b){return $b==120}),tb&&J()}break}return!0};var ec=function(){for(var $a=[],Oa=y;Oa<m.length;){var Fb=m[Oa].b;switch(Fb){case 113:case 114:case 115:$a.push(Fb);break;case 117:if(!$a.length)return Oa;if($a.pop()^113)return-1;break;case 118:if(!$a.length||$a.pop()^114)return-1;
break;case 119:if(!$a.length||$a.pop()^115)return-1;break}Oa=Oa+1|0}return-1},fc=-1,od=function(){var pd=ec(),Hc=pd!=-1&&fg(a.slice(m[y-1|0].a.c,m[pd].a.b));for(Hc&&(fc=y),p();ca(););J(),Hc&&(fc=y),s(function(Ic){return Ic==117})},gc=function(){var hc=z;for(x(),z=!0;s(function(Gb){return Gb!=119}););J(),s(function(Ib){return Ib==119}),z=hc},ub=function(){var Jc=z;for(x(),z=!0;s(function(ic){return ic!=118}););J(),s(function(Kc){return Kc==118}),z=Jc};for(s=function(qd){var Ma=m[y];if(!qd(Ma.b)||Ma.b==130)return!1;var vb=fc^y?g^130?xe(a.slice(i,Ma.a.b),b,c):'':'\n';y=y+1|0,vb!=''&&(h+=vb,q=!0,z=!1),q?h+=mc(b,o):eg(l,g,Ma.b)&&(h+=' ');var ab=K(Ma.a);switch(Ma.b){case 0:ab=gg(ab);break;case 1:ab=hg(ab,a.slice(0,Ma.a.b),mc(b,o),c);break}h+=ab,l=g,g=Ma.b,i=Ma.a.c,q=!1;switch(Ma.b){case 113:od();break;case 115:gc();break;case 114:ub();break}return!0};ca()||s(function(Hb){return Hb!=130}););var jc=xe(a.slice(i),b,c);switch(d){case 0:jc!=''&&(h+=c);break;case 2:h!=''&&(h+=c);break}return h}function jg(a){return a.f!=null?new wi(H(a.h)+' '+a.f+';',
''):a.d&&new wi(oc(a.d),Rc(a.d.f))}function Sc(b,a){return a.a==b.a&&uc(a,b.b)}function zd(b,a){return Sc(b,a.b)?(b.c=A(a),b.d=a,b.e=a.b,!0):!1}function Ad(b,a){return zd(b,a)||Ra(b,a.p)||Ra(b,a.x)||Ra(b,V(a))}function kg(c,a){for(var d=0,e=a.o,f=e.length;d<f;d=d+1|0){var b=e[d];if(Ad(c,b))return!0}return zd(c,a)||Ra(c,a.k)||Ra(c,a.q)}function lg(c,a){for(var d=0,e=a.i,f=e.length;d<f;d=d+1|0){var b=e[d];if(Ad(c,b))return!0}return zd(c,a)}function Ra(c,a){var d;if(!a)return!1;for(var b=a.g;b;b=b.k)if(Ra(c,b))return!0;switch(a.b){case 23:if(Sc(c,a.c))return c.c=a.f,c.d=a.e,c.e=a.c,!0;break;case 26:if(Sc(c,a.c))return c.c=a.f,c.d=((d=c.c).b?d.b:c.c).a,c.e=a.c,!0;break;case 21:if(Sc(c,a.d))return c.c=a.f,ad(a.g.f)?(c.f=a.m,c.h=a.f):c.d=a.e,c.e=a.d,!0;break;case 2:return Ad(c,a.e);case 11:return kg(c,a.e);case 16:return lg(c,a.e)}return!1}function Ae(d,a){switch(a.b){case 16:case 11:Be(d,a.e);break;case 17:for(var b=a.g.k;b;b=b.k)Be(d,b.e);break;case 0:for(var c=a.g;c;c=c.k)Ae(d,c);break}}function Be(b,a){a.b&&a.b.a==b.a&&b.b.push(a)}
function mg(c,a){var b=new xi(c.a,c.b);Ra(b,a),c.e=b.d,c.e&&(xb(c,a),Ce(c.c),Ce(c.d))}function wb(d,a,b,c){b==d.e&&a&&K(a)==d.e.c&&(d.c.push(a),c^1||d.d.push(a))}function Bd(b,a){wb(b,a.b,a,1),xb(b,a.p),xb(b,a.x),xb(b,V(a))}function ng(c,a){wb(c,a.b,a,1),wb(c,a.b,a.s,1),xb(c,a.k),xb(c,a.q);for(var d=0,e=a.o,f=e.length;d<f;d=d+1|0){var b=e[d];Bd(c,b)}}function og(c,a){wb(c,a.b,a,1);for(var d=0,e=a.i,f=e.length;d<f;d=d+1|0){var b=e[d];Bd(c,b)}}function xb(c,a){if(a){for(var b=a.g;b;b=b.k)xb(c,b);switch(a.b){case 23:wb(c,a.c,a.e,0);break;case 21:wb(c,a.d,a.e,0);break;case 26:wb(c,a.c,a.f.a,0);break;case 2:Bd(c,a.e);break;case 11:ng(c,a.e);break;case 16:og(c,a.e);break}}}function pg(d,a){F(d,'keyword','false'),F(d,'keyword','true'),F(d,'keyword','void'),F(d,'keyword','const'),F(d,'keyword','lowp'),F(d,'keyword','mediump'),F(d,'keyword','highp');for(var e=0,f=Lh.length;e<f;e=e+1|0){var b=Lh[e];F(d,'struct',b.a.c).c=oc(b.a)}if(d.a.c.b==1)for(var h=0,m=Mh.length;h<m;h=h+1|0){var c=Mh[h];F(d,'struct',c.a.c).c=oc(c.a)}
Dd(d,a)}function Cd(b,a){return a!=null&&a.a==b.a&&uc(a,b.b)}function F(e,a,b){var c=null,d=a+':'+b;return e.c.has(d)?c=e.c.get(d):(c=new Ai(a,b),e.d.push(c),Pa(e.c,d,c)),c}function Nb(d,a){var b=a==$d()?'method':a instanceof wc?'function':a instanceof vc?'struct':'variable',c=F(d,b,a.c);c.c!=''?c.c+='\n':c.d=Rc(a.f),c.c+=oc(a)}function Dd(x,a){if(!a)return!1;var b=Cd(x,a.c);switch(a.b){case 0:for(var c=!1,d=a.g;d;d=d.k)if(d.b==11&&Cd(x,d.c)){c=!0;break}c||(F(x,'keyword','@fragment'),F(x,'keyword','@vertex'),F(x,'keyword','#define'),F(x,'keyword','#elif'),F(x,'keyword','#else'),F(x,'keyword','#endif'),F(x,'keyword','#error'),F(x,'keyword','#extension'),F(x,'keyword','#if'),F(x,'keyword','#ifdef'),F(x,'keyword','#ifndef'),F(x,'keyword','#include'),F(x,'keyword','#line'),F(x,'keyword','#pragma'),F(x,'keyword','#undef'),F(x,'keyword','#version 300 es'),F(x,'keyword','invariant'),F(x,'keyword','precision'),F(x,'keyword','uniform'),x.a.c.b^1?(F(x,'keyword','attribute'),F(x,'keyword','varying')):(F(x,'keyword','centroid'),
F(x,'keyword','flat'),F(x,'keyword','layout'),F(x,'keyword','location'),F(x,'keyword','smooth')));break;case 11:if(Nb(x,a.e),b){var e=a.e;F(x,'keyword','discard'),F(x,'keyword','return');for(var Q=0,T=e.o,fa=T.length;Q<fa;Q=Q+1|0){var f=T[Q];Nb(x,f)}Dd(x,e.q)}break;case 2:Nb(x,a.e);break;case 16:Nb(x,a.e);break;case 10:case 19:case 7:b&&(F(x,'keyword','break'),F(x,'keyword','continue'));break;case 21:var h=a.g;if(b&&!Cd(x,h.c)){x.d=[];var m=h.f;switch(m){case yc:case Bc:case ob:case zc:case Cc:case pb:case Ac:case Dc:case qb:for(var ha=0,X=Yd(Ka(m)),ca=X.length;ha<ca;ha=ha+1|0)for(var l=X[ha],g=1;g<5;g=g+1|0){for(var i=[],o=0;o<g;o=o+1|0)i.push(0);for(;;){for(var k='',q=0;q<g;q=q+1|0)k+=l[i[q]];var y=Zd(S(m),k.length).a;F(x,'variable',k).c=y.c+' '+k+';';for(var s=0;s<g;){var z=i[s];if((z=z+1|0)^l.length||(z=0),i[s]=z,z)break;s=s+1|0}if(s==g)break}}break;default:if(m.a&&m.a instanceof vc)for(var aa=0,ia=m.a.i,ma=ia.length;aa<ma;aa=aa+1|0){var p=ia[aa];Nb(x,p)}else m.b&&Nb(x,$d());break}return!0}break}if(!a.b||b||a.b==17)for(var J=a.g;J;J=J.k)if(Dd(x,
J))return!0;return b}function De(b,a){return a!=null&&a.a==b.a&&uc(a,b.b)}function Ed(ra,a){if(!a||a.b&&!De(ra,a.c))return!1;for(var b=a.g;b;b=b.k)if(Ed(ra,b))return!0;switch(a.b){case 11:return Ed(ra,a.e.q),!0;case 20:var c=a.g;if(!De(ra,c.c)){var d=c.k,e=c.f,f=e.a;if(f){for(var h=[],m=d;m;m=m.k)h.push(m);if(f instanceof wc){for(var l=[],g=f;g;g=g.y)~l.indexOf(g.s)||l.push(g);l.reverse();for(var Za=0,Xa=l.length;Za<Xa;Za=Za+1|0){var i=l[Za];ra.c.push(new Ci(oc(i),i.o.map(function(o){return pc(o)}),Rc(i.f)))}if(ra.c.length){ra.e=0;for(var k=[],q=0;q<l.length;q=q+1|0)k.push(q);for(var y=h.length;y>0;y=y-1|0){for(var s=[],Na=0,lb=k,Ya=lb.length;Na<Ya;Na=Na+1|0){var z=lb[Na];l[z].o.length>=y&&s.push(z)}if(s.length){k=s;break}}if(k.length>1){var p=k.slice();Kb(p,function(J){for(var x=l[J].o,Q=0,Aa=Math.min(x.length,h.length);Q<Aa;Q=Q+1|0){var T=da(x[Q]),fa=h[Q].f;if(fa!=I&&T!=fa)return!0}return!1}),p.length||(p=k.slice(),Kb(p,function(ha){for(var X=l[ha].o,ca=0,mb=Math.min(X.length,h.length);ca<mb;ca=ca+1|0){var aa=da(X[ca]),ia=h[ca].f,ma=Ka(aa),za=Ka(ia);
if(ia!=I&&aa!=ia&&(!ma||!za||ma^za))return!0}return!1})),p.length&&(k=p)}k.length&&(ra.e=k[0])}}if(f instanceof vc&&!S(e)){var ga=f.i.map(function(la){return pc(la)});ra.c.push(new Ci(f.c+'('+ga.join(', ')+');',ga,Rc(f.f))),ra.e=0}if(h.length){ra.d=0;for(var Fa=0,Ba=h.length;Fa<Ba;Fa=Fa+1|0){var kb=h[Fa];if(ra.b<=kb.c.c||!kb.k)break;ra.d=ra.d+1|0}}return!0}}break}return!1}function ze(a){switch(a.b){case 30:return (a.h|0).toString();case 28:return (!!a.h).toString();case 29:return me(a.h,0);case 20:for(var b=a.g,c=H(a.f)+'(',d=b.k;d;d=d.k)d.o!=b&&(c+=', '),c+=ze(d);return c+')'}return null}function oc(a){if(a instanceof vc){var b=a,c=ib(b.e,' ')+'struct '+a.c;if(b.a>-1){c+=' {\n';for(var i=0,o=b.i,k=o.length;i<k;i=i+1|0){var d=o[i];c+='  '+pc(d)+';\n'}c+='}'}return c+';'}if(a instanceof qf){var e=a,f=pc(e);if(e.h){var h=ze(e.h);h!=null&&(f+=' = '+h)}return f+';'}if(a instanceof wc){for(var m=a,l=ib(m.e,' ')+H(m.k.f)+' '+a.c+'(',q=0,y=m.o,s=y.length;q<s;q=q+1|0){var g=y[q];g!=m.o[0]&&(l+=', '),l+=pc(g)}return l+');'}
return null}function pc(a){var b=da(a),c=ib(a.e,' ')+H(b.b||b)+' '+a.c;return b.b&&(c+=b.c>0?'['+b.c+']':'[]'),c}function Rc(a){var b='';if(a)for(var f=0,h=a.length;f<h;f=f+1|0){var c=a[f],d=0,e=c.length;for(c.startsWith('//')?d=d+2|0:c.startsWith('/*')&&(d=d+2|0,e=e-2|0);d<e&&c.charCodeAt(d)==32;)d=d+1|0;for(;e>d&&c.charCodeAt(e-1|0)==32;)e=e-1|0;b!=''&&(b+='\n'),b+=c.slice(d,e)}return b}function Ce(a){var b=null;a.sort(function(c,d){return c.a==d.a?sd(c.b,d.b):Jf(c.a.a,d.a.a)}),Kb(a,function(e){var f=b;return b=e,f!=null&&Zg(b,f)})}function Ee(a,b,c){if(!b)return a+': '+c+'\n';var d=Qd(b,0);return Pd(b)+': '+a+': '+c+'\n'+d.a+'\n'+d.b+'\n'}function qg(c){for(var a=new ji,d=0,e=c.a,f=e.length;d<f;d=d+1|0){var b=e[d];a.a+=Ee(b.a?'warning':'error',b.b,b.c),b.d&&(a.a+=Ee('note',b.d,b.e))}return a.a}function u(c,a,b){(!c.e||c.e.b^a.b)&&(c.e=a,c.a.push(new Ei(0,a,b)),c.d=c.d+1|0)}function rg(c,a,b){c.a.push(new Ei(1,a,b)),c.c=c.c+1|0}function yb(d,a,b){var c=Mc(d.a);c.d=a,c.e=b}function sg(b,a){u(b,a,'"'+K(a)+'" is a reserved word')}
function Fe(b,a){u(b,a.a,'Unexpected '+zf[a.b])}function tg(b,a){u(b,a,'There is no symbol called "'+K(a)+'" in the current scope')}function Fd(c,a,b){u(c,a,'There is already a symbol called "'+K(a)+'" in the current scope'),yb(c,b,'The previous definition of "'+K(b)+'" is here')}function ug(f,a,b,c,d,e){u(f,a,'Cannot change the return type of "'+b+'" to type "'+H(c)+'"'),yb(f,e,'The forward declaration of "'+b+'" has a return type of "'+H(d)+'"')}function Ge(b,a){u(b,a,'The operator "'+K(a)+'" is reserved and cannot be used')}function vg(b,a){u(b,a,'Unrecognized version "'+K(a)+'"')}function wg(c,a,b){u(c,Rd(_c(a)),'Expected this file to start with "#version 300 es"'),yb(c,Wa(b.c.a[0].a,b.c.a[2].a),'This is expected due to "#version 300 es" here')}function xg(d,a,b,c){u(d,a,'Cannot include file "'+b+'" that uses a different GLSL version');switch(c.c.b){case 0:yb(d,Rd(_c(c)),'Add "#version 300 es" to the included file');break;case 1:yb(d,Rd(_c(a.a)),'Add "#version 300 es" to the including file');break}}function yg(e,a,b,c,d){
u(e,a,'The constructor for type "'+H(b)+'" only takes '+c+' argument'+(c^1?'s':'')+' and this argument would bring the total to '+d)}function He(d,a,b,c){u(d,a,'Cannot construct type "'+H(b)+'" with '+c+' argument'+(c^1?'s':''))}function zg(f,a,b,c,d,e){u(f,a,'Expected '+b+' argument'+(b^1?'s':'')+' but found '+c+' argument'+(c^1?'s':'')+' when calling function "'+d+'"'),e&&yb(f,e,'The definition of function "'+d+'" is here')}function Ag(f,a,b,c,d,e){u(f,a,'Expected '+b+' argument'+(b^1?'s':'')+' but found '+c+' argument'+(c^1?'s':'')+' when constructing type "'+d+'"'),e&&yb(f,e,'The definition of struct "'+d+'" is here')}function Bg(c,a,b){b.b?u(c,a,'Cannot use a conditional expression with array type "'+H(b)+'"'):u(c,a,'Cannot use a conditional expression with type "'+H(b)+'" because it contains an array')}function Ie(c,a,b){b.b?u(c,a,'Cannot assign to array type "'+H(b)+'"'):u(c,a,'Cannot assign to type "'+H(b)+'" because it contains an array')}function Cg(e,a,b,c,d){c==d?u(e,a,'There is no operator "'+b+'" defined for type "'+H(c)+'"'):u(e,
a,'No binary operator "'+b+'" for type "'+H(c)+'" and type "'+H(d)+'"')}function Ke(b,a){b.b=a.b,b.c=a.c,b.d=a.d,b.e=a.e,b.f=a.f,b.h=a.h,b.m=a.m}function Le(b){var a=new Gi(b.b);return Ke(a,b),a}function Jd(b){for(var a=Le(b);b.g;)n(a,r(b.g));return a}function Sa(c){for(var a=Le(c),b=c.g;b;b=b.k)n(a,Sa(b));return a}function M(b,a){a!=b&&(Ke(b,a),Hg(b),Gg(b,a))}function Vc(a){return a.g!=null&&a.g==a.i}function Fg(c){for(var a=0,b=c.g;b;b=b.k)a=a+1|0;return a}function qc(c,a){for(var b=c.g;a;)b=b.k,a=a-1|0;return b}function t(b,a){return b.f=a,b}function Wc(b,a){return b.e=a,b}function Ia(b,a){return b.h=+a,b}function Y(b,a){return b.h=a,b}function sa(b,a){return b.h=a,b}function db(b,a){return b.m=a,b}function v(b,a){return b.c=a,b}function Ta(b,a){return b.d=a,b}function n(b,a){return a?(a.l=b,b.g?(a.o=b.i,b.i.k=a,b.i=a):b.i=b.g=a,b):b}function Gg(b,a){for(;a.g;)n(b,r(a.g));return b}function r(a){return a.o?a.o.k=a.k:a.l.g=a.k,a.k?a.k.o=a.o:a.l.i=a.o,a.l=null,a.o=null,a.k=null,a}function Hg(a){for(;a.g;)r(a.g)}
function pa(b,a){return a.l=b.l,a.o=b.o,a.k=b.k,b.o?b.o.k=a:b.l.g=a,b.k?b.k.o=a:b.l.i=a,b.l=null,b.o=null,b.k=null,b}function Xc(c,a,b){return b?a?(b.l=c,b.o=a.o,b.k=a,a.o?a.o.k=b:c.g=b,a.o=b,c):n(c,b):c}function Me(a){for(;a.g;)Xc(a.l,a.k,r(a.i));r(a)}function Ua(a){return a.b==28&&!!a.h}function Ob(a){return a.b==28&&!!!a.h}function rc(b,a){return b.b==29&&b.h==a||(b.b==30||b.b==31)&&(b.h|0)==a}function Ig(a){return a.l!=null&&a.l.b==20&&a.l.g==a}function Ne(f){if(f.l){if(Id(f.l.b)||Uc(f.l.b)&&f.l.g==f)return!0;if(f.l.b==20&&f.l.f!=I){var a=f.l.g,b=a.e;if(b&&b instanceof wc)for(var c=b,d=0,e=a.k;e;e=e.k){if(e==f)return (c.o[d].e&64)!=0;d=d+1|0}}}return!1}function Oe(a){return Ne(a)?!0:a.l&&(a.l.b==21||a.l.b==48)?Oe(a.l):!1}function Pe(a){return a.b==3&&a.g==null}function zb(a){return a.b==25&&a.g==null}function Jg(a){return a.b==30&&(a.h|0)<0||a.b==29&&a.h<0}function ta(a){switch(a.b){case 28:case 29:case 30:case 23:return!0;case 22:return ta(a.g)&&ta(a.g.k)&&ta(a.i);case 21:return ta(a.g)}return Hd(a.b)?!Id(a.b)&&ta(a.g):Tc(a.b)?!Uc(a.b)&&ta(a.g)&&ta(a.i):!1}
function Pb(a){switch(a.b){case 28:Ia(a,!!!a.h);break;case 34:M(a,r(a.g));break;case 45:a.b=55;break;case 55:a.b=45;break;case 49:a.b=47;break;case 46:a.b=50;break;case 50:a.b=46;break;case 47:a.b=49;break;case 25:Pb(a.i);break;case 52:a.b=51,Pb(a.g),Pb(a.i);break;case 51:a.b=52,Pb(a.g),Pb(a.i);break;default:M(a,t(n(new Gi(34),Jd(a)),ja));break}}function ua(d,a){if(d.b==a.b){switch(d.b){case 28:return !!d.h==!!a.h;case 29:return d.h==a.h;case 30:return (d.h|0)==(a.h|0);case 23:return d.e==a.e;case 26:return d.f==a.f;case 21:return ua(d.g,a.g)&&d.e==a.e&&d.m==a.m;case 22:return ua(d.g,a.g)&&ua(d.g.k,a.g.k)&&ua(d.i,a.i);case 20:for(var b=d.g,c=a.g;b&&c;){if(!ua(b,c))return!1;b=b.k,c=c.k}return b==null&&c==null}if(Hd(d.b))return ua(d.g,a.g);if(Tc(d.b))return ua(d.g,a.g)&&ua(d.i,a.i)}return!1}function Qe(a,b,c,d){return n(n(n(n(new Gi(10),a||new Gi(25)),b||new Gi(25)),c||new Gi(25)),d)}function Da(a){return t(n(new Gi(20),t(new Gi(26),a)),a)}function Ab(a){return zb(a.g)?null:a.g}function Ja(a){return zb(a.g.k)?null:a.g.k}
function Yc(a){return zb(a.i.o)?null:a.i.o}function Re(){return Af=Af+1|0,Af}function R(a,b){va(a,b,0).b=function(c){return fb(c,1)}}function Qb(a){return function(b,c,d){return Ta(v(n(new Gi(a),d),Wa(c.a,d.c)),c.a)}}function Se(a){return function(b,c,d){return Ta(v(n(new Gi(a),c),Wa(c.c,d.a)),d.a)}}function N(a){return function(b,c,d,e){return Ta(v(n(n(new Gi(a),c),e),Wa(c.c,e.c)),d.a)}}function Kd(a){var b=a.length,c=a.charCodeAt(b-1|0);return (c==117||c==85)&&(a=a.slice(0,b-1|0)),a.length>1&&a.charCodeAt(0)==48&&a.charCodeAt(1)^120&&a.charCodeAt(1)^88?parseInt(a,8):a|0}function Kg(a){var b=a==1;if(b){if(Cf)return Cf}else if(Bf)return Bf;var c=new Mi,d=function(e,f,h){return Ge(e.a,f.a),v(t(new Gi(27),I),Wa(f.a,h.c))},m=function(l,g,i,o){return Ge(l.a,i.a),v(t(new Gi(27),I),Wa(g.c,o.c))};return tc(c,36,function(k,q){return v(t(Ia(new Gi(28),!0),ja),q.a)}),tc(c,13,function(y,s){return v(t(Ia(new Gi(28),!1),ja),s.a)}),tc(c,127,function(z,p){return v(t(Y(new Gi(30),Kd(K(p.a))),ea),p.a)}),tc(c,125,function(J,x){
return v(t(sa(new Gi(29),+K(x.a)),ka),x.a)}),tc(c,128,function(Q,T){return b?v(t(Y(new Gi(31),Kd(K(T.a))),dc),T.a):(Fe(Q.a,T),v(t(new Gi(24),I),T.a))}),R(c,3),R(c,5),R(c,6),R(c,7),R(c,14),R(c,20),R(c,22),R(c,23),R(c,24),R(c,26),R(c,27),R(c,28),R(c,39),R(c,40),R(c,41),R(c,42),R(c,51),R(c,52),R(c,53),R(c,54),R(c,55),R(c,56),R(c,63),R(c,68),R(c,69),R(c,70),Tb(c,76,14,b?Qb(32):d),Tb(c,77,14,Qb(36)),Tb(c,78,14,Qb(37)),Tb(c,92,14,Qb(33)),Tb(c,79,14,Qb(34)),Tb(c,95,14,Qb(35)),cf(c,77,15,Se(38)),cf(c,78,15,Se(39)),Z(c,83,13,N(44)),Z(c,84,10,N(45)),Z(c,85,10,N(46)),Z(c,86,10,N(47)),Z(c,87,10,N(49)),Z(c,88,10,N(50)),Z(c,92,12,N(59)),Z(c,93,13,N(54)),Z(c,94,10,N(55)),Z(c,95,12,N(40)),Z(c,96,13,b?N(56):m),Z(c,97,11,b?N(57):m),Z(c,98,11,b?N(58):m),Z(c,90,3,N(52)),Z(c,91,4,N(53)),Z(c,89,5,N(51)),Z(c,80,8,b?N(41):m),Z(c,81,6,b?N(42):m),Z(c,82,7,b?N(43):m),Ea(c,99,2,N(60)),Ea(c,100,2,N(61)),Ea(c,101,2,b?N(62):m),Ea(c,102,2,b?N(63):m),Ea(c,103,2,b?N(64):m),Ea(c,104,2,N(65)),Ea(c,105,2,N(66)),Ea(c,106,2,b?N(67):m),Ea(c,107,2,b?N(68):m),
Ea(c,108,2,b?N(69):m),Ea(c,109,2,N(70)),va(c,126,0).b=function(fa){var ha=fb(fa,0);if(ha)return ha;var X=G(fa),ca=K(X.a),aa=Wd(fa.i,ca);return aa?(aa.m!=null&&Ga(fa.d.a,aa.m,0)==1&&u(fa.a,X.a,'Cannot use "'+ca+'" from disabled extension "'+aa.m+'"'),aa.g=aa.g+1|0,v(Wc(new Gi(23),aa),X.a)):(tg(fa.a,X.a),v(t(new Gi(24),I),X.a))},Z(c,111,1,function(ia,ma,za,ga){return ma.b^25&&(ma=v(n(new Gi(25),ma),ma.c)),n(ma,ga),v(ma,D(ia,ma.c))}),va(c,112,16).c=function(la,kb){var ra=w(la).a;G(la);var Za=w(la).a;return C(la,126)?Ta(v(db(n(new Gi(21),kb),K(Za)),D(la,kb.c)),Za):Ta(v(db(n(new Gi(21),kb),''),D(la,kb.c)),ef(ra))},va(c,115,0).b=function(Xa){var Na=G(Xa),lb=ba(Xa.h,Xa,0);return !lb||!C(Xa,119)?v(t(new Gi(24),I),D(Xa,Na.a)):v(lb,D(Xa,Na.a))},va(c,115,15).c=function(Ya,Aa){var mb=G(Ya),Fa=n(new Gi(20),Aa);return Lg(Ya,Fa,119)?(Aa.b==26&&Aa.f.b&&zb(Aa.g)&&(Aa.f=$c(Aa.f.b,Fg(Fa)-1|0)),Ta(v(Fa,D(Ya,Aa.c)),D(Ya,mb.a))):v(t(new Gi(24),I),D(Ya,mb.a))},va(c,114,16).c=function(Ba,Yb){var rb=G(Ba);if(w(Ba).b==118)return Va(Ba),
G(Ba),v(t(new Gi(24),I),D(Ba,rb.a));var nb=ba(Ba.h,Ba,0);return !nb||!C(Ba,118)?v(t(new Gi(24),I),D(Ba,rb.a)):Ta(v(n(n(new Gi(48),Yb),nb),D(Ba,Yb.c)),D(Ba,rb.a))},va(c,116,2).c=function(_a,sb){var Zb=G(_a),La=ba(_a.h,_a,1);if(!La||!C(_a,110))return v(t(new Gi(24),I),D(_a,Zb.a));var Fc=ba(_a.h,_a,1);return Fc?v(n(n(n(new Gi(22),sb),La),Fc),D(_a,sb.c)):v(t(new Gi(24),I),D(_a,Zb.a))},b?Cf=c:Bf=c,c}function Lg(a,b,c){for(var d=!0;!E(a,c);){d||C(a,111);var e=w(a),f=ba(a.h,a,1);if(f)n(b,f);else if(n(b,v(t(new Gi(24),I),D(a,e.a))),w(a).b^111&&w(a).b^c)return!1;d=!1}return!0}function Mg(a){var b=G(a);a.i=new Ti(3,a.i);var c=eb(a,2);if(!c||!C(a,43)||!C(a,115))return null;var d=ba(a.h,a,0);return !d||!C(a,119)?null:(Sb(a),Rb(a,b.a,n(n(new Gi(7),c),d)))}function Ng(a){var b=G(a),c=a.l;if(a.l|=b.b^74?b.b^73?b.b^72?65536:49152:32768:16384,E(a,113)){var d=new Gi(13);return !sc(a,d,1)||!C(a,117)?(a.l=c,null):(a.l=c,v(d,D(a,b.a)))}var e=eb(a,1);return e?(a.l=c,e):(a.l=c,null)}function Pg(a){var b=G(a),c=w(a).a;if(!C(a,126))return null;
var d=K(c);if(E(a,113)){a.d.a.has(d)||Pa(a.d.a,d,0);var e=new Gi(13);if(!sc(a,e,1)||!C(a,117))return null;for(var f=e.g;f;f=f.k)if(f.b^17)f.e&&(f.e.m=d);else for(var h=f.g.k;h;h=h.k)h.e.m=d;return v(e,D(a,b.a))}if(!Yh.has(d)&&!a.d.a.has(d)&&rg(a.a,c,'The extension "'+d+'" is not in the known list of valid WebGL extensions'),!C(a,110))return null;var m=K(w(a).a);if(!Og.has(m))return Va(a),null;G(a);var l=Og.get(m);return Pa(a.d.a,d,l),Ta(v(Y(db(new Gi(9),d),l),D(a,b.a)),c)}function Qg(a){var b=G(a);if(a.i=new Ti(3,a.i),!C(a,115))return null;var c=null;if(!E(a,120)){var d=Ve(a),e=Ld(a,2),f=null;if(e){if(f=fb(a,1),!f)return null}else f=fb(a,0);if(f){if(c=Ue(a,b.a,e,f,0,d),!c)return null}else if((c=ba(a.h,a,0),!c)||!C(a,120))return null}var h=null;if(!E(a,120)&&((h=ba(a.h,a,0),!h)||!C(a,120)))return null;var m=null;if(!E(a,119)&&((m=ba(a.h,a,0),!m)||!C(a,119)))return null;var l=eb(a,2);return l&&(Sb(a),v(Qe(c,h,m,l),D(a,b.a)))}function Rg(a){var b=G(a);if(!C(a,115))return null;var c=w(a),d=ba(a.h,a,0);if(d||(d=v(t(new Gi(24),
I),D(a,c.a))),!C(a,119))return null;var e=eb(a,2);if(!e)return null;var f=null;return E(a,12)&&(f=eb(a,2),!f)?null:v(n(n(n(new Gi(12),d),e),f),D(a,b.a))}function Sg(a){a.g&&u(a.a,w(a).a,'Move "#version" to the beginning of the file');var b=G(a),c=w(a).a;if(!C(a,127))return null;if(K(c)=='100')return v(db(new Gi(18),'100'),D(a,b.a));if(K(c)=='300'){var d=w(a).a;if(!C(a,126))return null;if(K(d)=='es')return v(db(new Gi(18),'300 es'),D(a,b.a))}return vg(a.a,c),v(db(new Gi(18),K(D(a,c))),D(a,b.a))}function Tg(a){var b=G(a);if(a.i=new Ti(3,a.i),!C(a,115))return null;var c=w(a),d=ba(a.h,a,0);if(d||(d=v(t(new Gi(24),I),D(a,c.a))),!C(a,119))return null;var e=eb(a,2);return e&&(Sb(a),v(n(n(new Gi(19),d),e),D(a,b.a)))}function Ug(a){var b=G(a),c=null;if(!E(a,120)){var d=w(a);c=ba(a.h,a,0),c||(c=v(t(new Gi(24),I),D(a,d.a))),C(a,120)}return v(n(new Gi(15),c),D(a,b.a))}function Vg(a){var b=G(a),c=0;switch(w(a).b){case 25:c=256;break;case 29:c=512;break;case 16:c=16;break;default:return Va(a),null}G(a);var d=fb(a,1);return d&&Rb(a,
b.a,n(Y(new Gi(14),c),d))}function Wg(a,b,c){var d=w(a).a;if(!C(a,126))return null;var e=new vc(Nc(a.d),d,K(d),new Ti(4,a.i));if(e.e|=a.l|b,e.f=c,!Nd(a,e))return null;var f=w(a).a,h=new Gi(1),m=null;if(!C(a,113))return null;for(a.i=e.d;w(a).b^117&&w(a).b^130;){var l=eb(a,3);if(!l)return null;if(l.b^17)u(a.a,l.c,'This statement cannot be used inside a struct');else{n(h,l);for(var g=l.g.k;g;g=g.k){var i=g.e;e.i.push(i),V(i)&&u(a.a,V(i).c,'Cannot initialize struct variables')}}}if(Sb(a),!C(a,117))return null;if(v(h,D(a,f)),w(a).b^126)C(a,120);else if(m=af(0,t(new Gi(26),A(e)),G(a).a,a,c),!m)return null;return n(n(Wc(new Gi(16),e),h),m)}function Te(a,b,c){for(var d=!1,e=a.i;e;e=e.b)if(e.a==3){d=!0;break}return d||u(a.a,b,'This statement cannot be used outside a loop'),Rb(a,b,c)}function Rb(a,b,c){return C(a,120),v(c,D(a,b))}function Ue(a,b,c,d,e,f){var h=w(a).a;if(!c&&w(a).b^126){var m=bf(a.h,a,0,d);return m&&Rb(a,b,n(new Gi(8),m))}if(!C(a,126))return null;if(E(a,115))return Yg(c,d,h,a,f);var l=af(c,d,h,a,f);return l&&v(l,
D(a,b))}function Ve(a){var b=w(a),c=b.c;if(!c)return null;for(var d=b.a.b,e=null,f=c.length-1|0;f>-1;f=f-1|0){for(var h=c[f],m=h.a.b.slice(h.c,d),l=0,g=0;g<m.length;g=g+1|0){var i=m.charCodeAt(g);(i==13||i==10)&&(l=l+1|0,i==13&&(g+1|0)<m.length&&m.charCodeAt(g+1|0)==10&&(g=g+1|0))}if(l>1)break;(e||(e=[])).push(K(h)),d=h.b}return e&&e.reverse(),e}function eb(a,b){var c=w(a);switch(c.b){case 4:return Te(a,G(a).a,new Gi(4));case 9:return Te(a,G(a).a,new Gi(5));case 10:return Rb(a,G(a).a,new Gi(6));case 11:return Mg(a);case 72:case 73:case 74:case 75:return Ng(a);case 121:return Pg(a);case 15:return Qg(a);case 17:return Rg(a);case 113:return Xe(a);case 31:return Vg(a);case 32:return Ug(a);case 120:return v(new Gi(3),G(a).a);case 124:return Sg(a);case 43:return Tg(a)}var d=Ve(a),e=Ld(a,b),f=null;if(E(a,35)){var h=Wg(a,e,d);return h&&v(h,D(a,c.a))}if(e){if(f=fb(a,1),!f)return null}else f=fb(a,0);if(f)return Ue(a,c.a,e,f,1,d);var m=ba(a.h,a,0);return m&&Rb(a,c.a,n(new Gi(8),m))}function We(a,b){if(b.b^17&&b.b^16){var c=a.i.a==1||a.i.a==4,d=b.b==9||b.b==11||b.b==14||b.b==18;
d&&!c?u(a.a,b.c,'This statement cannot be used inside a function'):!d&&c&&u(a.a,b.c,'This statement cannot be used outside a function')}}function Xg(a,b){var c=w(a).a;if(!C(a,129))return!0;var d=null;try{d=JSON.parse(K(c))}catch(m){return u(a.a,c,'Invalid string literal'),!0}var e=a.d.b;if(!e)return u(a.a,c,'Cannot include files without access to a file system'),!1;var f=e(d,c.a.a);if(!f)return u(a.a,c,'Cannot read the file '+JSON.stringify(d)),!1;if(a.f.has(f.a))return!0;Pa(a.f,f.a,!0),a.m.push(new Ji(c,_c(f))),xc(a.a,f,0),a.c^f.c.b&&xg(a.a,c,d,f);var h=new Ki(a.a,f.c,a.d,a.e,a.f);return h.i=a.i,!sc(h,b,1)||!C(h,130)?!1:!0}function Xe(a){var b=w(a),c=new Gi(3);return a.i=new Ti(2,a.i),!C(a,113)||!sc(a,c,2)||!C(a,117)?null:(Sb(a),v(c,D(a,b.a)))}function Ye(a){switch(a){case 50:case 8:case 44:case 21:case 37:case 2:case 38:case 18:case 30:case 19:case 16:case 29:case 25:return!0}return!1}function Ze(a){if(E(a,50)){if(E(a,115))for(;w(a).b^130&&!E(a,119);)G(a)}else G(a)}function Ld(a,b){var c=a.c==1,d=0;if(b==1)if(c&&E(a,
50)&&C(a,115)){for(var e=-1;;){var f=w(a);if(f.b==126&&K(f.a)=='location')G(a),C(a,99)&&(w(a).b==127||w(a).b==128?(e=Kd(K(w(a).a)),G(a)):C(a,127));else{if(Va(a),f.b^126)break;G(a)}if(!E(a,111))break}~e&&(d|=e<<18|1<<17),C(a,119)}else E(a,21)&&(d|=128),c&&(E(a,45)?d|=8:E(a,62)&&(d|=2048));if(b^3&&E(a,8)?d|=4:b==1&&E(a,37)?d|=4096:b==1&&!c&&E(a,2)?d|=1:b==1&&!c&&E(a,38)?d|=8192:b==1&&c&&E(a,44)?(d|=2,E(a,18)?d|=32:E(a,30)?d|=1024:Va(a)):(!b||c&&b==1)&&E(a,18)?d|=32:(!b||c&&b==1)&&E(a,30)?d|=1024:(!b||c&&b==1)&&E(a,19)&&(d|=64),E(a,16)?d|=16:E(a,29)?d|=512:E(a,25)&&(d|=256),Ye(w(a).b))for(u(a.a,w(a).a,'Cannot use this qualifier here'),Ze(a);Ye(w(a).b);)Ze(a);return d}function fb(a,b){var c=w(a),d=null;switch(c.b){case 3:d=ja;break;case 5:d=yc;break;case 6:d=zc;break;case 7:d=Ac;break;case 14:d=ka;break;case 20:d=ea;break;case 22:d=Bc;break;case 23:d=Cc;break;case 24:d=Dc;break;case 26:d=ac;break;case 27:d=bc;break;case 28:d=cc;break;case 33:d=xh;break;case 34:d=yh;break;case 39:d=ob;break;case 40:d=pb;break;case 41:
d=qb;break;case 42:d=ae;break;case 46:d=U(a,zh);break;case 47:d=U(a,Ah);break;case 48:d=U(a,Bh);break;case 49:d=U(a,Ch);break;case 51:d=U(a,cd);break;case 52:d=U(a,dd);break;case 53:d=U(a,ed);break;case 54:d=U(a,fd);break;case 55:d=U(a,gd);break;case 56:d=U(a,hd);break;case 57:d=U(a,Dh);break;case 58:d=U(a,Eh);break;case 59:d=U(a,Fh);break;case 60:d=U(a,di);break;case 61:d=U(a,Gh);break;case 63:d=U(a,dc);break;case 64:d=U(a,Hh);break;case 65:d=U(a,Ih);break;case 66:d=U(a,Jh);break;case 67:d=U(a,Kh);break;case 68:d=U(a,id);break;case 69:d=U(a,jd);break;case 70:d=U(a,kd);break;case 126:var e=Wd(a.i,K(c.a));if(!e||!(e instanceof vc))return b^1||Va(a),null;e.g=e.g+1|0,d=A(e);break;default:return b^1||Va(a),null}if(G(a),a.c==1){var f=_e(a,!0,!0);if(f)if(f==ee)d=I;else return v(t(n(new Gi(26),f.a),$c(d,f.b)),D(a,c.a))}return v(t(new Gi(26),d),D(a,c.a))}function _e(a,b,c){if(!c)return Md(a),null;var d=w(a);if(!E(a,114))return null;var e=null,f=0;if(E(a,118)){if(!b)return u(a.a,D(a,d.a),'All array sizes must be specified'),
Md(a),ee;e=new Gi(25)}else{if(e=ba(a.h,a,0),!e||!C(a,118))return ee;if(_(a.e,e),qa(a.e,e,ea),e.f!=I){var h=O(e);h?h.b^30||(f=h.h|0,f<1&&(u(a.a,e.c,'Cannot declare an array with a size of "'+f+'"'),f=0)):u(a.a,e.c,'This value must be a compile-time constant')}if(Md(a))return ee}return new Hi(e,f)}function Md(a){if(w(a).b^114)return!1;for(;;){var b=G(a);if(w(a).b^118&&!ba(a.h,a,0)||!C(a,118)||(u(a.a,D(a,b.a),'Multidimensional arrays are not a part of the language'),w(a).b^114))return!0}}function Yg(a,b,c,d,e){var f=d.i,h=new wc(Nc(d.d),c,K(c),new Ti(0,f));if(h.e|=d.l|a|(h.c=='main'?49152:0),h.f=e,h.k=b,d.i=h.d,E(d,42)){if(!C(d,119))return null}else if(!E(d,119)){for(;;){var m=Ld(d,0),l=fb(d,1);if(!l)return null;var g=w(d).a;if(!C(d,126))return null;var i=new qf(Nc(d.d),g,K(g),d.i,0);if(i.e|=m,i.p=l,i.Q=l.f,h.o.push(i),Nd(d,i),$e(d,i),!E(d,111))break}if(!C(d,119))return null}var o=Ga(f.c,K(c),null),k=!E(d,120);if(o){if(o instanceof wc){for(var q=o;q;q=q.y)if(sh(q,h)){q.k.f!=h.k.f?ug(d.a,h.k.c,h.c,h.k.f,q.k.f,q.k.c):q.q||!k?Fd(d.a,
h.b,q.b):(q.s=h,h.s=q,h.e|=q.e,q.e=h.e);break}h.y=o,ph(f,h)}else return Fd(d.a,c,o.b),null}else nf(f,h);if(k){var y=d.l;if(d.l&=-114689,h.q=Xe(d),d.l=y,!h.q)return null}return Sb(d),v(Wc(new Gi(11),h),D(d,b.c))}function $e(a,b){var c=_e(a,b.z!=0,da(b).b==null);c==ee?b.Q=I:c&&(b.x=c.a,da(b)!=I&&(b.Q=$c(da(b),c.b)))}function af(a,b,c,d,e){for(var f=n(Y(new Gi(17),d.l|a),b);;){var h=new qf(Nc(d.d),c,K(c),d.i,d.i.a^1?d.i.a^4?2:3:1);h.e|=d.l|a,h.f=e,h.p=b,$e(d,h);var m=w(d).a,l=null;if(E(d,99)){var g=w(d);l=ba(d.h,d,1),l||(l=v(t(new Gi(24),I),D(d,g.a)))}else m=null;var i=Ta(v(n(Wc(new Gi(2),h),l),D(d,h.b)),m);if(h.J=i,h.e&4)_(d.e,i);else{_(d.e,h.p);var o=da(h);if(o.b&&l){var k=h.p.g,q=h.x;(k&&zb(k)||q&&zb(q))&&(_(d.e,l),l.f.b&&l.f.c>0&&(h.Q=$c(o.b,l.f.c)))}}if(n(f,i),Nd(d,h),!E(d,111))return C(d,120),f;if(c=w(d).a,!C(d,126))return null}}function Nd(a,b){var c=Ga(a.i.c,b.c,null);return c?(Fd(a.a,b.b,c.b),!1):(nf(a.i,b),!0)}function sc(a,b,c){for(;w(a).b^130&&w(a).b^117;){var d=w(a).a;if(E(a,122)){if(c^1)return u(a.a,
d,'"#include" statements cannot be used here'),E(a,129),!1;if(!Xg(a,b))return!1}else{var e=eb(a,c);if(!e)return!1;if(e.b^13)We(a,e),n(b,e);else for(;e.g;){var f=r(e.g);We(a,f),n(b,f)}}}return!0}function Od(a,b,c,d,e,f){var h=new Map,m=new Ki(a,b,d,f,h);return m.i=e,sc(m,c,1)&&C(m,130),new Ii(m.m)}function w(a){return a.b[a.g]}function G(b){var a=w(b);return (b.g+1|0)<b.b.length&&(b.g=b.g+1|0),a}function D(c,a){var b=c.b[c.g>0?c.g-1|0:0];return b.a.c<a.b?a:Wa(a,b.a)}function E(b,a){return w(b).b^a?!1:(G(b),!0)}function C(e,a){if(E(e,a))return!0;var b=w(e),c=b.a,d=(e.g>0?e.b[e.g-1|0]:b).a;return a==120||df(d).a^df(c).a?u(e.a,ef(d),'Expected '+zf[a]):u(e.a,c,'Expected '+zf[a]+' but found '+zf[b.b]),!1}function Va(a){Fe(a.a,w(a))}function U(b,a){return b.c^1?(Va(b),I):a}function Sb(a){a.i=a.i.b}function va(e,a,b){var c=Ca(e.a,a,null);if(c)b>c.a&&(c.a=b);else{var d=new Li(b);c=d,Ha(e.a,a,d)}return c}function ba(f,a,b){var c=w(a),d=Ca(f.a,c.b,null);if(!d||!d.b)return Va(a),null;var e=bf(f,a,b,d.b(a));return e}function bf(f,a,b,c){
for(;c;){var d=w(a).b,e=Ca(f.a,d,null);if(!e||!e.c||e.a<=b)break;c=e.c(a,c)}return c}function tc(d,a,b){va(d,a,0).b=function(c){return b(c,G(c))}}function Tb(h,a,b,c){va(h,a,0).b=function(d){var e=G(d),f=ba(h,d,b);return f&&c(d,e,f)}}function cf(f,a,b,c){va(f,a,b).c=function(d,e){return c(d,e,G(d))}}function Z(m,a,b,c){va(m,a,b).c=function(d,e){var f=G(d),h=ba(m,d,b);return h&&c(d,e,f,h)}}function Ea(m,a,b,c){va(m,a,b).c=function(d,e){var f=G(d),h=ba(m,d,b-1|0);return h&&c(d,e,f,h)}}function K(a){return a.a.b.slice(a.b,a.c)}function Pd(b){var a=Wb(b.a,b.b);return b.a.a+':'+(a.a+1|0)+':'+(a.b+1|0)}function Zg(b,a){return b.a==a.a&&b.b<a.c&&a.b<b.c}function uc(b,a){return b.b<=a&&a<=b.c}function Qd(p,a){for(var b=Wb(p.a,p.b),c=Wb(p.a,p.c),d=qh(p.a,b.a),e=b.b,f=c.a^b.a?d.length:c.b,h=Ff(Wh,d,0),m=[],l=0,g=0;;){h.b^e||(l=m.length),h.b^f||(g=m.length);var i=Gf(h);if(i<0)break;if(i^9)m.push(i);else for(var o=0,J=8-m.length%8|0;o<J;o=o+1|0)m.push(32)}var k=m.length;if(a>0&&k>a){var q=Math.min(g-l|0,a/2|0),y=Math.max((a-q|0)/2|0,
3);if(l<y)d=Lc(m.slice(0,a-3|0))+'...',g>(a-3|0)&&(g=a-3|0);else if((k-l|0)<(a-y|0)){var s=k-a|0;d='...'+Lc(m.slice(s+3|0,k)),l=l-s|0,g=g-s|0}else{var z=l-y|0;d='...'+Lc(m.slice(z+3|0,(z+a|0)-3|0))+'...',l=l-z|0,g=g-z|0,g>(a-3|0)&&(g=a-3|0)}}else d=Lc(m);return new Ni(d,mc(' ',l)+((g-l|0)<2?'^':mc('~',g-l|0)))}function _g(c,a,b){return new Oi(c.a,c.b+a|0,c.b+b|0)}function df(a){return Wb(a.a,a.b)}function Rd(a){return new Oi(a.a,a.b,a.b)}function ef(a){return new Oi(a.a,a.c,a.c)}function Wa(a,b){return new Oi(a.a,a.b,b.c)}function $g(a){var b='abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ_'[a%53];for(a=a/53|0;a>0;)a=a-1|0,b+='abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ_0123456789'[a%63],a=a/63|0;return b}function ah(o,a){for(var b=0,k=a.length;b<k;b=b+1|0)o.h=b,Bb(o,a[b]);ch(o);var c=new Map,d=ff(o,o.f,null);d.sort(function(e,f){return sd(gf(o,f),gf(o,e))});for(var J=0,x=d.length;J<x;J=J+1|0)for(var h=d[J],m=null,z=0,p=h.length;z<p;z=z+1|0)for(var l=h[z],q=0,y=Array.from(l.c.values()),s=y.length;q<s;q=q+1|0){
var g=y[q],i=g.c;!(g.e&7<<14)&&(!o.a||o.a==1&&!of(g))&&(m==null&&(m=bh(o)),g.c=m),!(g.e&65536)&&of(g)&&Pa(c,i,g.c)}return c}function Bb(f,a){var m,h;a.e&&((h=Sd(f,a.e)).d=h.d+1|0);for(var b=a.g;b;b=b.k)Bb(f,b);switch(a.b){case 2:var c=a.e;Bb(f,c.p),V(c)&&Bb(f,V(c));break;case 11:var d=a.e;f.l=f.d.get(a.e.a).b,d.s&&ce(f.f,f.l,Sd(f,d.s).b),Bb(f,d.k);for(var l=0,g=d.o,i=g.length;l<i;l=l+1|0){var e=g[l];(m=Sd(f,e)).d=m.d+1|0,Bb(f,e.p)}d.q&&Bb(f,d.q),f.l=-1;break}}function Sd(c,a){var b=Ca(c.d,a.a,null);return b||(b=new Qi(a.c,c.c.length),b.e=_d(a),c.c.push(b),Ha(c.d,a.a,b),uf(c.e),uf(c.f)),b.c.has(c.h)||Ha(b.c,c.h,a),_d(a)&&ce(c.e,c.l,b.b),b}function bh(b){for(;;){var a=$g(b.m);if(b.m=b.m+1|0,!uh.has(a)&&!vh.has(a)&&!a.startsWith('gl_'))return a}}function ff(h,a,b){for(var c=new Map,m=0,l=h.c,g=l.length;m<g;m=m+1|0){var d=l[m];if(!b||b(d)){var e=ld(a,d.b),f=Ca(c,e,null);f||(f=[],Ha(c,e,f)),f.push(d)}}return Array.from(c.values())}function ch(b){dh(b,ff(b,b.e,function(a){return a.e}))}function dh(m,a){for(var b=[],g=0,i=a.length;g<i;g=g+1|0){
var c=a[g];c.sort(function(d,e){return sd(e.d,d.d)});for(var f=0,l=c.length;f<l;f=f+1|0){var h=c[f];f<b.length?ce(m.f,h.b,b[f]):b.push(h.b)}}}function gf(d,a){for(var b=0,e=0,f=a.length;e<f;e=e+1|0){var c=a[e];b=b+c.d|0}return b}function Td(f,a){_(f,a);for(var h=0,m=f.e,l=m.length;h<l;h=h+1|0){var b=m[h];r(b)}var c=a.g;f.e.length&&Xc(a,c,f.e[0]);for(var g=0,i=Array.from(f.f.values()),o=i.length;g<o;g=g+1|0){var d=i[g];Xc(a,c,d)}f.a.b=f.a.b.filter(function(e){return e.g==0})}function Ud(b,a){a.b.a.a!='<api>'&&!a.g&&!(a.e&49152)&&b.a.b.push(a)}function _(p,a){if(!a.f){a.f=I;var b=a.b;switch(b){case 0:case 1:Vd(p,a);break;case 2:var c=a.e;Ud(p,c),_(p,c.p);var d=da(c);if(d==ae&&(u(p.a,c.p.c,'Cannot create a variable of type "'+H(d)+'"'),d=I),c.x&&!zb(c.x)&&(na(p,c.x),qa(p,c.x,ea)),V(c)&&(na(p,V(c)),qa(p,V(c),d),d.d&&!p.c&&Ie(p.a,a.d,d)),c.e&4)if(V(c)){if(V(c).f!=I){var e=O(V(c));e?c.h=e:u(p.a,V(c).c,'This value must be a compile-time constant')}}else c.z^2||u(p.a,c.b,'Constants must be initialized');break;case 3:Cb(p,
a);break;case 4:case 5:case 6:break;case 7:Cb(p,a.g),_(p,a.i),qa(p,a.i,ja);break;case 8:_(p,a.g);break;case 9:break;case 10:Ab(a)&&na(p,Ab(a)),Ja(a)&&(na(p,Ja(a)),qa(p,Ja(a),ja)),Yc(a)&&na(p,Yc(a)),Cb(p,a.i);break;case 11:var f=a.e;Ud(p,f);for(var J=0,x=f.o,Q=x.length;J<Q;J=J+1|0){var h=x[J];_(p,h.p)}_(p,f.k),f.q&&(p.h=f.k.f,Cb(p,f.q),p.h&&p.h!=ae&&f.q.q&&u(p.a,f.b,'All control paths for "'+f.c+'" must return a value of type "'+H(p.h)+'"'),p.h=null);break;case 12:_(p,a.g),qa(p,a.g,ja),Cb(p,a.g.k),a.g.k.k&&Cb(p,a.g.k.k);break;case 14:break;case 15:a.g?(_(p,a.g),qa(p,a.g,p.h||I)):(a.f=ae,qa(p,a,p.h||I));break;case 16:var m=a.e;Ud(p,m),Vd(p,a);for(var l=A(m),T=0,fa=m.i,ha=fa.length;T<ha;T=T+1|0){var g=fa[T],i=da(g);i.d&&(l.d=!0),i.e&&(l.e=!0)}break;case 17:Vd(p,a);break;case 18:p.e.push(a);break;case 19:_(p,a.g),qa(p,a.g,ja),Cb(p,a.i);break;case 20:hh(p,a);break;case 21:ih(p,a);break;case 22:var o=a.g,k=a.i,q=a.g.k;na(p,o),qa(p,o,ja),na(p,q),na(p,k),q.f==I||k.f==I?a.f=I:q.f!=k.f?u(p.a,Wa(q.c,k.c),'Cannot merge type "'+H(q.f)+'" and type "'+H(k.f)+'"'):q.f.d?Bg(p.a,
Wa(q.c,k.c),q.f):a.f=q.f;break;case 23:var y=a.e;y instanceof qf?(_(p,y.p),a.f=da(y)):y instanceof wc&&!Ig(a)?u(p.a,a.c,'The function "'+y.c+'" must be called'):a.f=A(y);var s=y.m;s!=null&&!p.f.has(s)&&!Ga(p.b.a,s,0)&&Pa(p.f,s,Y(db(new Gi(9),s),2));break;case 25:for(var z=a.g;z;z=z.k)na(p,z);a.f=a.i.f;break;default:Hd(b)?eh(p,a):Tc(b)&&gh(p,a);break}}}function Cb(c,a){if(Pf(c.d,a),a.b^3)_(c,a),ke(c.d,a);else for(var b=a.g;b;b=b.k)_(c,b),ke(c.d,b);Qf(c.d,a)}function eh(d,a){var b=a.g;na(d,b),Id(a.b)&&hf(d,b);var c=b.f;switch(a.b){case 33:case 35:case 36:case 37:case 38:case 39:a.f=bd(c)?c:I;break;case 32:a.f=jb(c)?c:I;break;case 34:a.f=c==ja?ja:I;break}a.f==I&&c!=I&&u(d.a,a.d,'No unary operator "'+K(a.d)+'" for type "'+H(c)+'"')}function fh(c,a,b){var f,e,d;if(xa(a)){if(xa(b)){if(W(a)==W(wa(b)))return (d=be(W(b),W(wa(a))))||I}else if(wa(b)==ka&&W(a)==W(b))return (e=be(1,W(wa(a))))||I}else if(wa(a)==ka&&xa(b)&&W(a)==W(wa(b)))return (f=be(W(b),1))||I;return I}function gh(i,a){var b=a.g,c=a.i;na(i,b),na(i,c),Uc(a.b)&&hf(i,
b);var d=b.f,e=c.f,f=d==e;switch(a.b){case 40:case 59:case 54:case 44:a.f=f&&S(d)||bd(d)&&e==S(d)?d:bd(e)&&d==S(e)?e:a.b^54?I:fh(i,d,e);break;case 41:case 42:case 43:case 56:a.f=f&&jb(d)||jb(d)&&e==S(d)?d:jb(e)&&d==S(e)?e:I;break;case 57:case 58:a.f=f&&jb(d)||jb(d)&&e==S(d)?d:I;break;case 45:case 55:a.f=f&&wh(d,i.c)?ja:I;break;case 51:case 52:case 53:a.f=f&&d==ja?ja:I;break;case 49:case 50:case 46:case 47:a.f=f&&(d==ka||d==ea)?ja:I;break;case 60:a.f=d,d.d&&!i.c&&Ie(i.a,a.d,d),qa(i,c,d);return;case 61:case 70:case 66:case 65:a.f=f&&S(d)||bd(d)&&e==S(d)||a.b==66&&(d==ob&&e==ac||d==pb&&e==bc||d==qb&&e==cc)?d:I;break;case 62:case 63:case 64:case 67:case 68:case 69:a.f=f&&jb(d)||jb(d)&&e==S(d)?d:I;break;case 48:if(e==ea){var h=wa(d);h&&(a.f=h);var m=O(c);if(m&&m.b==30){var l=m.h|0,g=W(d);(l<0||g>0&&l>=g)&&u(i.a,c.c,'Index "'+l+'" is out of bounds for type "'+H(d)+'"')}}break}a.f==I&&d!=I&&e!=I&&(a.b^48?Cg(i.a,a.d,K(a.d),d,e):u(i.a,a.d,'No index operator for type "'+H(d)+'" and type "'+H(e)+'"'))}function hh(i,a){var b=a.g;
if(i.c==1&&b.b==21&&b.m=='length'){var c=b.g;na(i,c);var d=c.f;if(d.b){var e=$d();b.f=A(e),a.e=e}else u(i.a,b.d,'Cannot find "'+b.m+'" on type "'+H(d)+'"'),b.f=I}else _(i,b);for(var f=b.f,h=f.a,m=[],l=!1,g=b.k;g;g=g.k)na(i,g),m.push(g),g.f==I&&(l=!0);if(!l){if(h){if(h instanceof wc){jh(i,h,a,m);return}if(h instanceof vc){lh(i,f,a,m);return}}b.b==26&&f.b?kh(i,f,a,m):f!=I&&u(i.a,b.c,'Cannot call type "'+H(f)+'"')}}function ih(m,a){var b=a.g,c=a.m,d=a.d;if(na(m,b),c!=''){var e=b.f,f=Ne(a);if(ad(e))a.f=mh(m,d,e,c,f);else if(e==I);else{if(e.a&&e.a instanceof vc)for(var l=0,g=e.a.i,i=g.length;l<i;l=l+1|0){var h=g[l];if(h.c==c){h.g=h.g+1|0,a.e=h,_(m,h.p),a.f=da(h);break}}a.e||u(m.a,d,'Cannot find "'+c+'" on type "'+H(e)+'"')}}}function jh(p,a,b,c){for(var d=[],e=a;e;e=e.y)~d.indexOf(e.s)||d.push(e);if(d.length^1&&(d=d.slice(),Kb(d,function(f){return f.o.length!=c.length}),d.length^1)){var h=d.slice();Kb(d,function(m){for(var l=0,J=c.length;l<J;l=l+1|0)if(da(m.o[l])!=c[l].f)return!0;return!1}),d.length^1&&(d=h,Kb(d,function(g){
for(var i=0,x=c.length;i<x;i=i+1|0){var o=da(g.o[i]),k=c[i].f,q=Ka(o),y=Ka(k);if(o!=k&&(!q||!y||q^y))return!0}return!1}))}if(d.length^1)u(p.a,b.g.c,'No matching overload for function "'+a.c+'"');else{var s=d[0];if(s.o.length^c.length)zg(p.a,b.d,s.o.length,c.length,s.c,s.b);else for(var z=0,Q=c.length;z<Q;z=z+1|0)qa(p,c[z],da(s.o[z]));b.g.e=s,b.f=s.k.f}}function kh(e,a,b,c){b.f=a,a.c>0&&a.c^c.length&&He(e.a,b.d,a,c.length);for(var f=0,h=c.length;f<h;f=f+1|0){var d=c[f];qa(e,d,a.b)}}function lh(s,a,b,c){if(b.f=a,a!=I)if(S(a)){for(var d=Ka(a),e=!1,f=0,z=0,p=c.length;z<p;z=z+1|0){var h=c[z],m=h.f,l=Ka(m);if(!S(m)){m!=I&&u(s.a,h.c,'Cannot use value of type "'+H(m)+'" when constructing type "'+H(a)+'"');return}f>=d&&yg(s.a,h.c,a,d,f+l|0),xa(m)&&(e=!0),f=f+l|0}var g=xa(a)&&e;g&&c.length^1?u(s.a,b.d,'If a matrix argument is given to a matrix constructor, it is an error to have any other arguments'):f<d&&f^1&&!g&&He(s.a,b.d,a,f)}else{var i=a.a,o=i.i,k=o.length,q=c.length;if(k^q)Ag(s.a,b.d,k,q,i.c,i.b);else for(var y=0;y<k;y=y+1|0)qa(s,
c[y],da(o[y]))}}function mh(l,a,b,c,d){var e=c.length;if(e<1||e>4)return u(l.a,a,'Invalid swizzle "'+c+'" on type "'+H(b)+'"'),I;for(var f=Ka(b),g=0,i=Yd(f),o=i.length;g<o;g=g+1|0){var h=i[g];if(~h.indexOf(c[0])){for(var m=1;m<e;m=m+1|0){if(h.indexOf(c[m])==-1)return u(l.a,a,'Invalid swizzle "'+c+'" on type "'+H(b)+'"'),I;if(d&&~c.slice(0,m).indexOf(c[m]))return u(l.a,_g(a,m,m+1|0),'The field "'+c[m]+'" cannot be specified multiple times when used as a storage location'),I}return Zd(S(b),e)}}return u(l.a,a,'Invalid swizzle "'+c+'" on type "'+H(b)+'"'),I}function na(b,a){_(b,a),a.b==26&&a.f!=I&&(u(b.a,a.c,'Unexpected type "'+H(a.f)+'"'),a.f=I)}function Vd(c,a){for(var b=a.g;b;b=b.k)_(c,b)}function hf(c,a){d:for(var b=a;;){if(b.f==I)break;switch(b.b){case 23:(b.e.e&4||b.e.e&4096)&&u(c.a,a.c,'Cannot store to this location');break d;case 21:b=b.g;break;case 48:b=b.g;break;default:u(c.a,a.c,'Cannot store to this location');break d}}}function qa(c,a,b){a.f!=b&&a.f!=I&&b!=I&&u(c.a,a.c,'Cannot convert from type "'+H(a.f)+'" to type "'+H(b)+'"')}
function nh(a,b,c,d){for(;;){var e=new Si(d);if(c.a&&hb(e,a),c.d&&(gb(e,a),Eb(e,a),oh(e,a,b)),!e.b)break}c.a&&Db(a)}function Db(a){switch(a.b){case 12:if(!a.g.k.k)return!0;var b=a.g.k,c=Db(b);return c&&b.b^3&&pa(b,n(new Gi(3),Jd(b))),Db(a.g.k.k);case 10:return Db(a.i);case 11:var d=a.e;d.q&&Db(d.q);break;case 19:return Db(a.i);default:for(var e=a.g;e;e=e.k)Db(e);return!1}return!1}function gb(l,a){for(var b=a.g;b;b=b.k)gb(l,b);switch(a.b){case 2:var c=a.e;c.x&&gb(l,c.x),V(c)&&gb(l,V(c)),(c.z==2||c.z==1)&&(l.c.push(c),Ha(l.d,c.a,0),Ha(l.e,c.a,0));break;case 11:var d=a.e;Ha(l.d,d.a,0),gb(l,d.k);for(var g=0,i=d.o,o=i.length;g<o;g=g+1|0){var e=i[g];gb(l,e.p),e.x&&gb(l,e.x)}d.q&&gb(l,d.q);break;case 23:var f=a.e.a,h=Ca(l.d,f,-1);~h&&Ha(l.d,f,h+1|0),Oe(a)&&(h=Ca(l.e,f,-1),~h&&Ha(l.e,f,h+1|0));var m=a.e.m;m!=null&&Pa(l.f,m,0);break}}function Eb(g,a){for(var b=a.g,c=null;b;b=c)c=b.k,Eb(g,b);switch(a.b){case 2:if(lf(g,a.e)||mf(g,a.e)||kf(g,a.e)&&(!a.g||ta(a.g)||a.e.h))r(a),g.b=!0;else{a.g&&Eb(g,a.g);var d=a.e.x;if(d&&d.b==23){
var e=jf(g,d.e);e&&(a.e.x=e,g.b=!0)}}break;case 11:var f=a.e;if(kf(g,f)&&!(f.e&49152))r(a),g.b=!0;else{Eb(g,f.k);for(var i=0,o=f.o,k=o.length;i<k;i=i+1|0){var h=o[i];Eb(g,h.p),h.x&&Eb(g,h.x)}f.q&&Eb(g,f.q)}break;case 17:if(!a.g.k){var m=a.l.b;m==3||!m||m==16?r(a):m^10?M(a,new Gi(3)):M(a,new Gi(25)),g.b=!0}break;case 23:var l=jf(g,a.e);l&&(M(a,l),g.b=!0);break}}function jf(b,a){return lf(b,a)?Sa(a.h):mf(b,a)?Sa(V(a)):null}function oh(e,a,b){for(var c=a.g,d=null;c;c=d)d=c.k,c.b==9&&!e.f.has(c.m)&&!Ga(b.a,c.m,0)&&r(c)}function kf(b,a){return Ca(b.d,a.a,-1)==0&&(!(a instanceof wc)||a.s==null||Ca(b.d,a.s.a,-1)==0)}function lf(b,a){return a.h!=null&&Gd(a.h.b)}function mf(b,a){return Ca(b.e,a.a,-1)==0&&V(a)!=null&&Gd(V(a).b)}function hb(B,a){for(var b=a.g,c=null;b;b=c)c=b.k,hb(B,b);switch(a.b){case 2:var d=a.e;d.x&&hb(B,d.x),V(d)&&hb(B,V(d));break;case 3:for(var e=a.g;e;e=e.k)if(Eg(e.b)&&e.k){for(;e.k;)r(e.k);B.b=!0}if(a.l&&a.l.b==3){for(var f=!1,h=a.g;h;h=h.k)h.b^17||(f=!0);if(!f){Me(a),B.b=!0;return}}break;case 8:if(a.l.b==3){
if(ta(a.g)){r(a),B.b=!0;return}var m=a.o;if(m&&m.b==8)if(B.a==1&&(tf(m.g.f)||tf(a.g.f)));else{var l=r(a.g);n(a,t(n(n(new Gi(25),r(r(m).g)),l),l.f)),B.b=!0;return}}break;case 7:Zc(B,a.g),Ob(a.i)&&(pa(a,n(new Gi(3),r(a.g))),B.b=!0);break;case 10:if(Zc(B,a.i),!Ab(a)&&a.o&&a.o.b==8&&(pa(a.g,r(a.o.g)),r(a.o),B.b=!0),Ja(a)&&Ua(Ja(a))&&(pa(Ja(a),new Gi(25)),B.b=!0),a.i.b^5){for(;a.i.g&&a.i.i.b==5;)r(a.i.i),B.b=!0}else pa(a.i,new Gi(3)),B.b=!0;break;case 11:var g=a.e;hb(B,g.k);for(var lc=0,de=g.o,rd=de.length;lc<rd;lc=lc+1|0){var i=de[lc];hb(B,i.p),i.x&&hb(B,i.x)}g.q&&hb(B,g.q);break;case 12:if(Zc(B,a.g.k),a.g.k.k&&Zc(B,a.g.k.k),Ua(a.g)){pa(a,r(a.g.k)),B.b=!0;return}if(Ob(a.g)){a.g.k.k?pa(a,r(a.g.k.k)):r(a),B.b=!0;return}if(a.g.k.k&&a.g.k.b==15&&a.g.k.k.b==15){var o=a.g.k.g,k=a.g.k.k.g;if(o&&k){pa(a,n(new Gi(15),t(n(n(n(new Gi(22),r(a.g)),r(o)),r(k)),o.f))),B.b=!0;return}}if(a.g.k.k&&a.g.k.b==8&&a.g.k.k.b==8){var q=a.g.k.g,y=a.g.k.k.g;if(q.f==y.f)if(B.a==1&&q.f==ae);else{pa(a,n(new Gi(8),t(n(n(n(new Gi(22),r(a.g)),r(q)),
r(y)),q.f))),B.b=!0;return}}if(!a.g.k.k&&a.g.k.b==8){var s=a.g.k.g,z=s.b==60&&s.g.b==23&&_d(s.g.e);if(s.f==ea||s.f==ka||z){var p=z?Sa(s.g):s.f==ea?t(Y(new Gi(30),0),ea):t(sa(new Gi(29),0),ka);pa(a,n(new Gi(8),t(n(n(n(new Gi(22),r(a.g)),r(s)),p),s.f))),B.b=!0;return}}if(!a.g.k.k&&a.g.k.b==12&&!a.g.k.g.k.k){var J=a.g,x=a.g.k.g,Q=a.g.k.g.k;M(J,t(n(n(new Gi(51),Jd(J)),r(x)),ja)),M(a.g.k,r(Q)),B.b=!0;return}if(Pe(a.g.k)){a.g.k.k?(Pb(a.g),r(a.g.k)):pa(a,n(new Gi(8),r(a.g))),B.b=!0;return}if(a.g.k.k&&Pe(a.g.k.k)){r(a.g.k.k),B.b=!0;return}break;case 15:for(var T=a.o;T&&T.b==12&&!T.g.k.k&&T.g.k.b==15;){var fa=T.g.k.g,ha=a.g;if(!fa||!ha)break;pa(a,n(new Gi(15),t(n(n(n(new Gi(22),r(T.g)),r(fa)),r(ha)),fa.f))),r(T),T=a.o,B.b=!0}break;case 17:for(var X=a.o;X;X=X.o){if(X.b^17)break;if(X.g.f==a.g.f&&(X.h|0)==(a.h|0)){for(;X.i!=X.g;)Xc(a,a.g.k,r(X.i));r(X),B.b=!0;return}for(var ca=X.g.k;ca;ca=ca.k){var aa=ca.g;if(aa&&!Gd(aa.b))return}}break;case 19:var ia=a.g,ma=a.i;pa(a,Qe(null,r(ia),null,r(ma))),B.b=!0;break;case 22:var za=a.g,ga=a.g.k,la=a.i;
if(Ua(za)){M(a,r(ga)),B.b=!0;return}if(Ob(za)){M(a,r(la)),B.b=!0;return}if(ga.b==la.b&&Tc(ga.b)&&ua(ga.g,la.g)&&ga.b^48){var kb=ga.g,ra=ga.i,Za=la.i;if(ra.f==Za.f){var Xa=n(n(n(new Gi(22),r(za)),r(ra)),r(Za));Xa.f=ra.f,M(a,n(n(new Gi(ga.b),r(kb)),Xa)),B.b=!0;return}}if(ga.b==60&&ua(ga.g,la)&&ta(la)){var Na=ga.g,lb=ga.i,Ya=n(n(n(new Gi(22),r(za)),r(lb)),r(la));Ya.f=lb.f,M(a,n(n(new Gi(60),r(Na)),Ya)),B.b=!0;return}break;case 25:for(var Aa=a.g,mb=null;Aa!=a.i;Aa=mb)mb=Aa.k,ta(Aa)&&(r(Aa),B.b=!0);if(a.l.b==25){Me(a),B.b=!0;return}if(Vc(a)){M(a,r(a.g)),B.b=!0;return}break;case 33:var Fa=a.g;Fa.b^33?Fa.b^30?Fa.b^29||Ub(B,a,-Fa.h):Vb(B,a,-(Fa.h|0)|0):(M(a,r(Fa.g)),B.b=!0);break;case 34:var Ba=a.g;Ba.b^34?Ba.b^28||$(B,a,!!!Ba.h):(M(a,r(Ba.g)),B.b=!0);break;case 35:M(a,r(a.g));break;case 40:var Yb=a.g,rb=a.i;rc(Yb,0)?(M(a,r(rb)),B.b=!0):rc(rb,0)?(M(a,r(Yb)),B.b=!0):Yb.b==30&&rb.b==30?Vb(B,a,(Yb.h|0)+(rb.h|0)|0):Yb.b==29&&rb.b==29&&Ub(B,a,Yb.h+rb.h);break;case 20:var nb=a.g;if(nb.b^26){if(a.f==ea&&nb.b==21&&nb.m=='length'&&Vc(a)){
var nd=nb.g.f;nd.b&&nd.c>0&&(M(a,t(t(Y(new Gi(30),nd.c),ea),ea)),B.b=!0)}}else{var _a=nb.f;if(_a==ea){var sb=nb.k;sb&&!sb.k&&sb.b==30&&(M(a,r(sb)),B.b=!0)}else if(_a==ka){var Zb=nb.k;Zb&&!Zb.k&&Zb.b==30&&Ub(B,a,Zb.h|0)}else if(S(_a)==ka)for(var La=nb.k;La;La=La.k)if(La.b==29){var Fc=La.h,md=Fc|0;Fc==md&&Vb(B,La,md)}}break;case 44:var Gc=a.g,tb=a.i;rc(tb,1)?(M(a,r(Gc)),B.b=!0):Gc.b==30&&tb.b==30?Vb(B,a,tb.h|0?(Gc.h|0)/(tb.h|0)|0:0):Gc.b==29&&tb.b==29&&Ub(B,a,tb.h!=0?Gc.h/tb.h:0);break;case 45:var _b=a.g,$b=a.i;ua(_b,$b)&&ta(_b)?($(B,a,!0),B.b=!0):_b.b==30&&$b.b==30?$(B,a,(_b.h|0)==($b.h|0)):_b.b==29&&$b.b==29&&$(B,a,_b.h==$b.h);break;case 46:var ec=a.g,$a=a.i;ec.b==30&&$a.b==30?$(B,a,(ec.h|0)>($a.h|0)):ec.b==29&&$a.b==29&&$(B,a,ec.h>$a.h);break;case 47:var Oa=a.g,Fb=a.i;Oa.b^30?Fb.b^30?Oa.b==30&&Fb.b==30?$(B,a,(Oa.h|0)>=(Fb.h|0)):Oa.b==29&&Fb.b==29&&$(B,a,Oa.h>=Fb.h):(a.b=46,Y(Fb,(Fb.h|0)-1|0),B.b=!0):(a.b=46,Y(Oa,(Oa.h|0)+1|0),B.b=!0);break;case 48:var fc=a.g,od=a.i,pd=fc.f;if(od.b==30){var Hc=od.h|0,Ic=0;switch(pd){
case yc:case Bc:case ob:Ic=2;break;case zc:case Cc:case pb:Ic=3;break;case Ac:case Dc:case qb:Ic=4;break}Hc>-1&&Hc<Ic&&(M(a,t(db(n(new Gi(21),r(fc)),'xyzw'[Hc]),a.f)),B.b=!0)}break;case 49:var gc=a.g,hc=a.i;gc.b==30&&hc.b==30?$(B,a,(gc.h|0)<(hc.h|0)):gc.b==29&&hc.b==29&&$(B,a,gc.h<hc.h);break;case 50:var Gb=a.g,Ib=a.i;Gb.b^30?Ib.b^30?Gb.b==30&&Ib.b==30?$(B,a,(Gb.h|0)<=(Ib.h|0)):Gb.b==29&&Ib.b==29&&$(B,a,Gb.h<=Ib.h):(a.b=49,Y(Ib,(Ib.h|0)+1|0),B.b=!0):(a.b=49,Y(Gb,(Gb.h|0)-1|0),B.b=!0);break;case 51:var ub=a.g,Jc=a.i;ub.b==28&&Ua(ub)?(M(a,r(Jc)),B.b=!0):ub.b==28&&Ob(ub)?$(B,a,!1):ub.b==28&&Jc.b==28&&$(B,a,!!ub.h&&!!Jc.h);break;case 52:var ic=a.g,Kc=a.i;ic.b==28&&Ob(ic)?(M(a,r(Kc)),B.b=!0):ic.b==28&&Ua(ic)?$(B,a,!0):ic.b==28&&Kc.b==28&&$(B,a,!!ic.h&&!!Kc.h);break;case 53:var qd=a.g,Ma=a.i;qd.b==28&&Ma.b==28&&$(B,a,!!qd.h!=!!Ma.h);break;case 54:var vb=a.g,ab=a.i;vb.b==30&&(vb.h|0)==1?(M(a,r(ab)),B.b=!0):ab.b==30&&(ab.h|0)==1?(M(a,r(vb)),B.b=!0):vb.b==30&&ab.b==30?Vb(B,a,mi(vb.h|0,ab.h|0)):vb.b==29&&ab.b==29&&Ub(B,
a,vb.h*ab.h);break;case 55:var Hb=a.g,jc=a.i;ua(Hb,jc)&&ta(Hb)?($(B,a,!1),B.b=!0):Hb.b==30&&jc.b==30?$(B,a,(Hb.h|0)!=(jc.h|0)):Hb.b==29&&jc.b==29&&$(B,a,Hb.h!=jc.h);break;case 59:var Jb=a.g,kc=a.i;rc(Jb,0)?(M(a,t(n(new Gi(33),r(kc)),a.f)),B.b=!0):rc(kc,0)?(M(a,r(Jb)),B.b=!0):Jb.b==30&&kc.b==30?Vb(B,a,(Jb.h|0)-(kc.h|0)|0):Jb.b==29&&kc.b==29&&Ub(B,a,Jb.h-kc.h);break}}function $(c,a,b){M(a,t(t(Ia(new Gi(28),b),ja),ja)),c.b=!0}function Ub(c,a,b){M(a,t(t(sa(new Gi(29),b),ka),ka)),c.b=!0}function Vb(c,a,b){M(a,t(t(Y(new Gi(30),b),ea),ea)),c.b=!0}function Zc(b,a){a.b==3&&Vc(a)&&(pa(a,r(a.g)),b.b=!0)}function nf(b,a){Pa(b.c,a.c,a)}function ph(b,a){Pa(b.c,a.c,a)}function Wd(c,a){var b=Ga(c.c,a,null);return b||c.b&&Wd(c.b,a)}function _c(a){return new Oi(a,0,a.b.length)}function qh(d,a){if(Xd(d),a<0||a>=d.d.length)return'';var b=d.d[a],c=(a+1|0)<d.d.length?d.d[a+1|0]-1|0:d.b.length;return d.b.slice(b,c)}function Wb(h,a){Xd(h);for(var b=h.d.length,c=0;b>0;){var d=b/2|0,e=c+d|0;h.d[e]<=a?(c=e+1|0,b=(b-d|0)-1|0):b=d}var f=c>0?a-h.d[c-1|0]|0:a;
return new Ui(c-1|0,f)}function Xb(d,a,b){if(Xd(d),a>-1&&a<d.d.length){var c=d.d[a];if(b>-1&&((a+1|0)<d.d.length?(c+b|0)<d.d[a+1|0]:(c+b|0)<=d.b.length))return c+b|0}return-1}function Xd(b){if(!b.d){b.d=[0];for(var a=0,c=b.b.length;a<c;a=a+1|0)b.b.charCodeAt(a)^10||b.d.push(a+1|0)}}function Yd(a){switch(a){case 2:return Zh;case 3:return _h;case 4:return $h}return null}function Zd(a,b){switch(a){case ja:switch(b){case 1:return ja;case 2:return yc;case 3:return zc;case 4:return Ac}break;case ka:switch(b){case 1:return ka;case 2:return ob;case 3:return pb;case 4:return qb}break;case ea:switch(b){case 1:return ea;case 2:return Bc;case 3:return Cc;case 4:return Dc}break;case dc:switch(b){case 1:return dc;case 2:return id;case 3:return jd;case 4:return kd}break}return null}function of(a){return (a.e&4097)!=0||(a.e&32)!=0&&a.d.a==1}function _d(a){return a instanceof qf&&(a.z==0||a.z==2)}function A(a){return a.l||(a.l=new Zi(a,null,0)),a.l}function sh(c,a){if(c.o.length^a.o.length)return!1;for(var b=0,d=c.o.length;b<d;b=b+1|0)if(da(c.o[b])!=da(a.o[b]))return!1;
return!0}function $d(){return pf||(pf=new wc(-999,null,'length',null),pf.k=t(new Gi(26),ea)),pf}function da(a){return a.Q||a.p.f}function V(a){var b;return (b=a.J)&&b.g}function xc(a,b,c){for(var d=b.b.split(ai),e=[],f=null,h=0,m=0,l=0,ha=d.length;l<ha;l=l+1|0){var g=d[l],i=g.length,o=m+i|0,k=new Oi(b,m,o);if(l%2){var q=g.charCodeAt(0);if(q>64&&q<91||q>96&&q<123||q==95){var y=Ga(uh,g,130);y^130?e.push(new Xi(k,y,f)):vh.has(g)?sg(a,k):e.push(new Xi(k,126,f))}else if(q^64){if(q>47&&q<58||q==46&&i>1)bi.test(g)?(q=g.charCodeAt(i-1|0),e.push(new Xi(k,q==117||q==85?128:127,f))):e.push(new Xi(k,125,f));else if(q^35){if(q^34){var J=Ga(ci,g,130);J^130||(g.startsWith('//')?c^1?(f||(f=[])).push(k):J=0:g.startsWith('/*')&&(c^1?(f||(f=[])).push(k):J=1)),J^130&&e.push(new Xi(k,J,f))}else e.push(new Xi(k,129,f))}else{for(var z=123,p=1;p<g.length&&(g.charCodeAt(p)==32||g.charCodeAt(p)==9);)p=p+1|0;switch(g.slice(p)){case'extension':z=121;break;case'include':z=122;break;case'version':z=124;break}e.push(new Xi(k,z,f))}}else{var s=71;
switch(g){case'@vertex':s=74;break;case'@fragment':s=73;break}e.push(new Xi(k,s,f))}}else if(g!=''){u(a,k,'Syntax error "'+g+'"');break}e.length^h&&(f=null,h=e.length),m=o}var x=0;if(e.length>2){var Q=e[0],T=e[1],fa=e[2];Q.b==124&&T.b==127&&fa.b==126&&K(T.a)=='300'&&K(fa.a)=='es'&&(x=1)}e.push(new Xi(new Oi(b,m,m),130,f)),b.c=new Yi(e,x)}function sf(a){return a.b?sf(a.b):a}function $c(c,a){c.f||(c.f=new Map);var b=Ca(c.f,a,null);return b||(Ha(c.f,a,b=new Zi(null,c,a)),b.d=!0,b.e=c.e),b}function H(a){return a.b?a.c>0?H(a.b)+'['+a.c+']':H(a.b)+'[]':a.a.c}function W(a){switch(a){case yc:case ob:case Bc:case id:case ac:case cd:case dd:return 2;case zc:case pb:case Cc:case jd:case bc:case ed:case fd:return 3;case Ac:case qb:case Dc:case kd:case cc:case gd:case hd:return 4}return a.c}function wa(a){switch(a){case yc:case zc:case Ac:return ja;case ob:case pb:case qb:return ka;case Bc:case Cc:case Dc:return ea;case id:case jd:case kd:return dc;case ac:case ed:case gd:return ob;case bc:case cd:case hd:return pb;case cc:case dd:case fd:
return qb}return a.b}function Ka(a){switch(a){case ja:case ka:case ea:case dc:return 1;case yc:case ob:case Bc:case id:return 2;case zc:case pb:case Cc:case jd:return 3;case Ac:case qb:case Dc:case kd:case ac:return 4;case cd:case ed:return 6;case dd:case gd:return 8;case bc:return 9;case fd:case hd:return 12;case cc:return 16}return 0}function S(a){switch(a){case ja:case yc:case zc:case Ac:return ja;case ka:case ob:case pb:case qb:case ac:case bc:case cc:case cd:case dd:case ed:case fd:case gd:case hd:return ka;case ea:case Bc:case Cc:case Dc:return ea;case dc:case id:case jd:case kd:return dc}return null}function ad(a){switch(a){case yc:case zc:case Ac:case Bc:case Cc:case Dc:case id:case jd:case kd:case ob:case pb:case qb:return!0}return!1}function xa(a){switch(a){case ac:case bc:case cc:case cd:case dd:case ed:case fd:case gd:case hd:return!0}return!1}function jb(b){var a=S(b);return a==ea||a==dc}function bd(b){var a=S(b);return a==ka||a==ea||a==dc}function wh(b,a){return !b.e&&(!b.d||a==1)}function tf(a){
return a==ae||a.d}function oa(a){return a.e=!0,a}function be(a,b){switch(mi(a,10)+b|0){case 21:case 12:return ob;case 31:case 13:return pb;case 41:case 14:return qb;case 22:return ac;case 23:return cd;case 24:return dd;case 32:return ed;case 33:return bc;case 34:return fd;case 42:return gd;case 43:return hd;case 44:return cc}return null}function uf(b){var a=b.a.length;return b.a.push(a),a}function ce(c,a,b){c.a[ld(c,a)]=ld(c,b)}function ld(c,a){var b=c.a[a];return b^a&&(b=ld(c,b),c.a[a]=b),b}function ya(a,b){vf(a),process.stdout.write(b),vf(0)}function Nh(a){ya(3,'error: '),ya(1,a+'\n')}function Oh(a){ya(2,'note: '),ya(1,a+'\n')}function Ph(a){ya(7,'warning: '),ya(1,a+'\n')}function wf(a){for(var b=process.stdout.columns,l=0,g=a.a,i=g.length;l<i;l=l+1|0){var c=g[l];c.b&&ya(1,Pd(c.b)+': ');switch(c.a){case 1:Ph(c.c);break;case 0:Nh(c.c);break}if(c.b){var d=Qd(c.b,b);ya(2,d.a+'\n'),ya(4,d.b+'\n')}if(c.d){ya(1,Pd(c.d)+': '),Oh(c.e);var e=Qd(c.d,b);ya(2,e.a+'\n'),ya(4,e.b+'\n')}}var f=a.d!=0,h=a.c!=0,m='';h&&(m+=a.c+' warning'+(a.c^1?'s':''),
f&&(m+=' and ')),f&&(m+=a.d+' error'+(a.d^1?'s':'')),(h||f)&&process.stdout.write(m+' generated\n')}function xf(a){if(ni(a))return[new Vi('<stdin>',a)];if(a instanceof Array){for(var b=[],c=0,e=a.length;c<e;c=c+1|0){var d=a[c];b.push(new Vi(oi(d.name),oi(d.contents)))}return b}return[new Vi(oi(a.name),oi(a.contents))]}function yf(a){return function(b,c){var d=a(b,c);if(ni(d))return new Vi(b,d);if(!d)return null;var e=d.name,f=d.contents;if(ni(e)&&ni(f))return new Vi(e,f);throw new Error('Invalid file access result')}}function Qh(){var a=process.argv.slice(2),b=new pi,c=[],d=0,e=null,f=require('fs'),h=require('path');b.e=function(m,l){var g=h.resolve(h.dirname(l),m);try{return new Vi(g,f.readFileSync(g,'utf8'))}catch(p){}return null};for(var s=0,z=a.length;s<z;s=s+1|0){var i=a[s];if(i.startsWith('-'))switch(i){case'--disable-rewriting':b.a=!1;break;case'--pretty-print':b.b=!1;break;case'--keep-symbols':b.d=!1;break;case'--help':case'-h':console.log("\nUsage: glslx [sources] [flags]\n\n  --output=PATH\n    Set the path to the output file. Defaults to standard out.\n\n  --format=FORMAT\n    Set the output format, must be json, js, c++, rust, or skew. Defaults to json.\n\nAdvanced:\n\n  --disable-rewriting\n    Disable syntax tree rewriting, useful to check for driver bugs.\n\n  --pretty-print\n    Format the output nicely instead of minifying it.\n\n  --renaming=MODE\n    Valid modes are all, internal-only, or none. Defaults to all.\n\n  --keep-symbols\n    Don't inline constants or remove unused symbols.\n");
return;default:if(i.startsWith('--output='))e=i.slice(9);else if(i.startsWith('--format=')){var o=i.slice(9);Df.has(o)||(console.log('invalid output format "'+o+'"'),process.exit(1)),d=Df.get(o)}else if(i.startsWith('--renaming=')){var k=i.slice(11);Ef.has(k)||(console.log('invalid symbol renaming mode "'+k+'"'),process.exit(1)),b.c=Ef.get(k)}else console.log('invalid flag "'+i+'"'),process.exit(1);break}else c.push(new Vi(h.resolve(i),f.readFileSync(i,'utf8')))}if(c.length){var q=new Fi,y=je(q,c,b);y?e!=null?(f.writeFileSync(e,td(y,d)),wf(q)):process.stdout.write(td(y,d)):(wf(q),process.exit(1))}else console.log("\nUsage: glslx [sources] [flags]\n\n  --output=PATH\n    Set the path to the output file. Defaults to standard out.\n\n  --format=FORMAT\n    Set the output format, must be json, js, c++, rust, or skew. Defaults to json.\n\nAdvanced:\n\n  --disable-rewriting\n    Disable syntax tree rewriting, useful to check for driver bugs.\n\n  --pretty-print\n    Format the output nicely instead of minifying it.\n\n  --renaming=MODE\n    Valid modes are all, internal-only, or none. Defaults to all.\n\n  --keep-symbols\n    Don't inline constants or remove unused symbols.\n")}
function $i(){var a=(function(){return this})(),b=typeof exports!=='undefined'?exports:a.GLSLX={};b.compile=fi,b.compileIDE=gi,b.format=hi,typeof require!=='undefined'&&typeof module!=='undefined'&&require.main===module&&Qh()}function Gd(a){return a>27&&a<31}function Hd(a){return a>31&&a<40}function Dg(a){return a>31&&a<38}function Id(a){return a>35&&a<40}function Tc(a){return a>39&&a<71}function Uc(a){return a>59&&a<71}function Eg(a){return a==4||a==5||a==6||a==15}function Je(a){return a==7||a==10||a==19}function rh(c){var a=(c&16384)!=0,b=(c&32768)!=0;return a&&!b?1:b&&!a?2:0}function ib(d,a){var b='';if(d&128&&(b+='invariant '),d&8&&(b+='flat '),d&2048&&(b+='smooth '),d&1<<17){var c=d/(1<<18)|0;b+='layout(location'+a+'='+a+c+')'+a}return d&1&&(b+='attribute '),d&4&&(b+='const '),d&4096&&(b+='uniform '),d&8192&&(b+='varying '),d&2&&(b+='centroid '),d&32&&(b+='in '),d&64&&(b+='inout '),d&1024&&(b+='out '),d&16&&(b+='highp '),d&256&&(b+='lowp '),d&512&&(b+='mediump '),b}function th(a){return a>79&&a<110}function rf(a){
switch(a){case 126:case 42:case 33:case 57:case 58:case 59:case 60:case 34:case 61:case 46:case 47:case 48:case 49:case 64:case 65:case 66:case 67:case 14:case 39:case 40:case 41:case 20:case 22:case 23:case 24:case 63:case 68:case 69:case 70:case 3:case 5:case 6:case 7:case 26:case 27:case 28:case 51:case 52:case 53:case 54:case 55:case 56:return!0}return!1}function vf(a){process.stdout.isTTY&&process.stdout.write('\x1B[0;'+ei.get(a)+'m')}function bb(b,a){return b[b.length-1|0]=a}function Mc(a){return a[a.length-1|0]}function Kf(c,a){for(var d=0,e=a.length;d<e;d=d+1|0){var b=a[d];c.push(b)}}function Kb(d,a){for(var b=0,c=0,e=d.length;c<e;c=c+1|0)a(d[c])||(b<c&&(d[b]=d[c]),b=b+1|0);for(;b<d.length;)d.pop()}function Pa(c,a,b){return c.set(a,b),b}function j(c,a,b){return c.set(a,b),c}function Ga(d,a,b){var c=d.get(a);return c!==void 0?c:b}function Ha(c,a,b){return c.set(a,b),b}function Qa(c,a,b){return c.set(a,b),c}function Ca(d,a,b){var c=d.get(a);return c!==void 0?c:b}function sd(b,a){return (a<b|0)-(a>b|0)|0}
function Lc(a){for(var b=new ji,d=0,e=a.length;d<e;d=d+1|0){var c=a[d];b.a+=Hf(c)}return b.a}function Jf(b,a){return (a<b|0)-(a>b|0)|0}function mc(d,a){for(var b='',c=0;c<a;c=c+1|0)b+=d;return b}function Hf(a){return a<65536?String.fromCharCode(a):String.fromCharCode((a-65536>>10)+55296|0)+String.fromCharCode((a-65536&1023)+56320|0)}function Ec(a){if(!a)return null;var b=a.a,c=Wb(b,a.b),d=Wb(b,a.c);return{source:b.a,start:{line:c.a,column:c.b},end:{line:d.a,column:d.b}}}function fi(a,b){b=b||{};var c=xf(a),d=new Fi,e=new pi;e.c=Ga(Ef,b.renaming,0),b.disableRewriting&&(e.a=!1),b.prettyPrint&&(e.b=!1),b.keepSymbols&&(e.d=!1),b.fileAccess&&(e.e=yf(b.fileAccess)),e.f='indent'in b?oi(b.indent):'  ',e.h='newline'in b?oi(b.newline):'\n';var f=je(d,c,e);return{log:qg(d),output:f?td(f,Ga(Df,b.format,0)):null}}function gi(a,b){b=b||{};var c=xf(a),d=new Fi,e=new pi;b.fileAccess&&(e.e=yf(b.fileAccess));var f=Lf(d,c,e),h=function(m){for(var qd,l=m.source+'',g=m.line|0,i=m.column|0,o=!!m.ignoreDiagnostics,k=null,q=null,y=null,Hb=0,jc=c.length;Hb<jc;Hb=Hb+1|0){
var s=c[Hb];if(s.a==l){var z=Xb(s,g,i);if(~z){if(!o&&d)for(var Ma=0,vb=d.a,ab=vb.length;Ma<ab;Ma=Ma+1|0){var p=vb[Ma];if(p.b&&p.b.a==s&&uc(p.b,z)){q=new wi(p.c,''),k=p.b;break}}if(!q&&f){var J=new xi(s,z);Ra(J,f.a),q=jg(J),q&&(k=J.e,y=(qd=J.d)&&qd.c)}}break}}return{tooltip:q&&q.a,range:Ec(k),symbol:y,documentation:q&&q.b}},x=function(Q){for(var T=Q.source+'',fa=Q.line|0,ha=Q.column|0,X=null,ca=null,aa=null,Jb=0,kc=f.b,B=kc.length;Jb<B;Jb=Jb+1|0){var ia=kc[Jb];if(ia.a.a.a==T){var ma=Xb(ia.a.a,fa,ha);if(~ma&&uc(ia.a,ma))return{definition:Ec(ia.b),range:Ec(ia.a),symbol:ia.b.a.a}}}for(var lc=0,de=c.length;lc<de;lc=lc+1|0){var za=c[lc];if(za.a==T){var ga=Xb(za,fa,ha);if(~ga&&f){var la=new xi(za,ga);Ra(la,f.a),la.d&&la.d.b&&la.d.b.a.a!='<api>'&&(ca=la.d.b,X=la.e,aa=la.d.c)}break}}return{definition:Ec(ca),range:Ec(X),symbol:aa}},kb=function(ra){for(var Za=ra.source+'',Xa=null,rd=0,Sh=c.length;rd<Sh;rd=rd+1|0){var Na=c[rd];if(Na.a==Za){if(f){var lb=new yi(Na);Ae(lb,f.a),Xa=lb.b.map(function(Ya){return{name:Ya.c,kind:Ya instanceof qf?'variable':Ya instanceof wc?'function':Ya instanceof vc?'struct':null,
range:Ec(Ya.b)}})}break}}return{symbols:Xa}},Aa=function(mb){for(var Fa=mb.source+'',Ba=mb.line|0,Yb=mb.column|0,rb=null,nb=null,_a=null,fe=0,Th=c.length;fe<Th;fe=fe+1|0){var sb=c[fe];if(sb.a==Fa){var Zb=Xb(sb,Ba,Yb);if(~Zb&&f){var La=new zi(sb,Zb);mg(La,f.a),La.e&&La.e.b&&La.e.b.a.a!='<api>'&&(rb=La.c.map(Ec),nb=La.d.map(Ec),_a=La.e.c)}break}}return{ranges:rb,definitions:nb,symbol:_a}},Fc=function(md){for(var nd=md.source+'',Gc=md.line|0,tb=md.column|0,_b=[],ge=0,Uh=c.length;ge<Uh;ge=ge+1|0){var $b=c[ge];if($b.a==nd){var ec=Xb($b,Gc,tb);if(~ec&&f){var $a=new Bi($b,ec);pg($a,f.a),_b=$a.d.map(function(Oa){return{kind:Oa.a,name:Oa.b,detail:Oa.c,documentation:Oa.d}})}}}return{completions:_b}},Fb=function(fc){for(var od=fc.source+'',pd=fc.line|0,Hc=fc.column|0,Ic=[],gc=-1,hc=-1,he=0,Vh=c.length;he<Vh;he=he+1|0){var Gb=c[he];if(Gb.a==od){var Ib=Xb(Gb,pd,Hc);if(~Ib&&f){var ub=new Di(Gb,Ib);Ed(ub,f.a),gc=ub.d,hc=ub.e,Ic=ub.c.map(function(Jc){return{text:Jc.a,'arguments':Jc.b,documentation:Jc.c}})}}}return{signatures:Ic,
activeArgument:gc,activeSignature:hc}};return{unusedSymbols:d.b.map(function(ic){return{name:ic.c,range:Ec(ic.b)}}),diagnostics:d.a.map(function(Kc){return{kind:ii[Kc.a].toLowerCase(),range:Ec(Kc.b),text:Kc.c}}),tooltipQuery:h,definitionQuery:x,symbolsQuery:kb,renameQuery:Aa,completionQuery:Fc,signatureQuery:Fb}}function hi(a,b){b=b||{};var c='indent'in b?oi(b.indent):'  ',d='newline'in b?oi(b.newline):'\n',e=2;if('trailingNewline'in b){var f=oi(b.trailingNewline);switch(f){case'preserve':e=0;break;case'remove':e=1;break;case'insert':e=2;break;default:throw new Error('Invalid "trailingNewline" value: '+f)}}return ig(oi(a),c,d,e)}function ji(){this.a=''}function ki(){this.a='',this.b=0,this.c=0}function pi(){this.a=!0,this.b=!0,this.c=0,this.d=!0,this.e=null,this.f='  ',this.h='\n'}function qi(a){this.a=new Map,this.c=0,this.b=a}function ri(a,b,c){this.a=a,this.b=b,this.c=c}function si(a,b){this.a=a,this.b=b}function ti(a,b){this.a=a,this.b=b}function ui(){this.a=[],this.b=[]}function vi(a,b){this.a='',this.b='',
this.c=null,this.d=null,this.e=' ',this.f=b.b,this.f?(this.e='',this.d=''):(this.c=b.f,this.d=b.h);for(var c=null,d=a.g;d;d=d.k)Sf(this,d)||(c&&Rf(c,d)&&(this.a+=this.d),cb(this,d),this.a+=this.d,c=d)}function wi(a,b){this.a=a,this.b=b}function xi(a,b){this.a=a,this.b=b,this.c=null,this.d=null,this.e=null,this.f=null,this.h=null}function yi(a){this.a=a,this.b=[]}function zi(a,b){this.a=a,this.b=b,this.c=[],this.d=[],this.e=null}function Ai(a,b){this.a=a,this.b=b,this.c='',this.d=''}function Bi(a,b){this.a=a,this.b=b,this.c=new Map,this.d=[]}function Ci(a,b,c){this.a=a,this.b=b,this.c=c}function Di(a,b){this.a=a,this.b=b,this.c=[],this.d=-1,this.e=-1}function Ei(a,b,c){this.a=a,this.b=b,this.c=c,this.d=null,this.e=''}function Fi(){this.a=[],this.b=[],this.c=0,this.d=0,this.e=null}function Gi(a){this.a=Re(),this.b=a,this.c=null,this.d=null,this.e=null,this.f=null,this.h=0,this.m=null,this.l=null,this.g=null,this.i=null,this.o=null,this.k=null,this.q=!1}function Hi(a,b){this.a=a,this.b=b}function Ii(a){this.a=a}function Ji(a,b){
this.a=a,this.b=b}function Ki(a,b,c,d,e){this.a=null,this.b=null,this.c=0,this.d=null,this.e=null,this.f=null,this.h=null,this.m=[],this.l=0,this.g=0,this.i=null,this.a=a,this.b=b.a,this.c=b.b,this.d=c,this.e=d,this.f=e,this.h=Kg(this.c)}function Li(a){this.a=a,this.b=null,this.c=null}function Mi(){this.a=new Map}function Ni(a,b){this.a=a,this.b=b}function Oi(a,b,c){this.a=a,this.b=b,this.c=c}function Qi(a,b){this.a=a,this.b=b,this.c=new Map,this.d=0,this.e=!1}function Pi(a){this.a=a,this.b=[],this.c=[],this.d=new Map,this.e=new _i,this.f=new _i,this.h=0,this.m=0,this.l=-1}function Ri(a,b,c){this.a=a,this.b=b,this.c=c,this.d=new ui,this.e=[],this.f=new Map,this.h=null}function Si(a){this.a=a,this.b=!1,this.c=[],this.d=new Map,this.e=new Map,this.f=new Map}function Ti(a,b){this.a=a,this.b=b,this.c=new Map}function Ui(a,b){this.a=a,this.b=b}function Vi(a,b){this.a=a,this.b=b,this.c=null,this.d=null}function Wi(a,b,c,d){this.a=a,this.b=b,this.c=c,this.d=d,this.e=0,this.f=null,this.h=null,this.m=null,this.l=null,this.g=0}
function vc(a,b,c,d){Wi.call(this,a,b,c,d),this.i=[]}li(vc,Wi);function wc(a,b,c,d){Wi.call(this,a,b,c,d),this.o=[],this.k=null,this.q=null,this.y=null,this.s=null}li(wc,Wi);function qf(a,b,c,d,e){Wi.call(this,a,b,c,d),this.z=e,this.p=null,this.J=null,this.x=null,this.Q=null}li(qf,Wi);function Xi(a,b,c){this.a=a,this.b=b,this.c=c}function Yi(a,b){this.a=a,this.b=b}function Zi(a,b,c){this.a=a,this.b=b,this.c=c,this.d=!1,this.e=!1,this.f=null}function _i(){this.a=[]}var Wh=new ki,Af=0,ee=new Hi(null,-1),Bf=null,Cf=null,Og=j(j(j(j(new Map,'disable',1),'enable',2),'require',3),'warn',4),Yh=j(j(j(j(new Map,'GL_OES_standard_derivatives',0),'GL_EXT_frag_depth',0),'GL_EXT_draw_buffers',0),'GL_EXT_shader_texture_lod',0),Zh=['xy','st','rg'],_h=['xyz','stp','rgb'],$h=['xyzw','stpq','rgba'],pf=null,ai=new RegExp('(\\.[0-9]+[eE][+-]?[0-9]+\\b|\\.[0-9]+\\b|[0-9]+\\.[0-9]+[eE][+-]?[0-9]+\\b|[0-9]+\\.[0-9]+\\b|[0-9]+\\.[eE][+-]?[0-9]+\\b|[0-9]+\\.|[0-9]+[eE][+-]?[0-9]+\\b|[1-9][0-9]*[uU]?\\b|0[0-7]*[uU]?\\b|0[xX][0-9A-Fa-f]+[uU]?\\b|[ \t\r\n]|/\\*(?:.|\r\n|\n)*?\\*/|//.*|&&|\\|\\||\\^\\^|\\+\\+|--|<<=?|>>=?|[()[\\]{}\\.,?:;]|[+\\-*/%=!<>&|^~]=?|@?[A-Za-z_][A-Za-z0-9_]*\\b|#\\s*\\w+\\b|"(?:[^"\\n\\\\]|\\\\.)*"?)'),bi=new RegExp('^([1-9][0-9]*|0[0-7]*|0[xX][0-9A-Fa-f]+)[uU]?$'),uh=j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(new Map,
'attribute',2),'bool',3),'break',4),'bvec2',5),'bvec3',6),'bvec4',7),'const',8),'continue',9),'discard',10),'do',11),'else',12),'false',13),'float',14),'for',15),'highp',16),'if',17),'in',18),'inout',19),'int',20),'invariant',21),'ivec2',22),'ivec3',23),'ivec4',24),'lowp',25),'mat2',26),'mat3',27),'mat4',28),'mediump',29),'out',30),'precision',31),'return',32),'sampler2D',33),'samplerCube',34),'struct',35),'true',36),'uniform',37),'varying',38),'vec2',39),'vec3',40),'vec4',41),'void',42),'while',43),'centroid',44),'flat',45),'isampler2D',46),'isampler2DArray',47),'isampler3D',48),'isamplerCube',49),'layout',50),'mat2x2',26),'mat2x3',51),'mat2x4',52),'mat3x2',53),'mat3x3',27),'mat3x4',54),'mat4x2',55),'mat4x3',56),'mat4x4',28),'sampler2DArray',57),'sampler2DArrayShadow',58),'sampler2DShadow',59),'sampler3D',60),'samplerCubeShadow',61),'smooth',62),'uint',63),'usampler2D',64),'usampler2DArray',65),'usampler3D',66),'usamplerCube',67),'uvec2',68),'uvec3',69),'uvec4',70),'export',72),'import',75),ci=j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(new Map,
'~',76),'--',77),'++',78),'!',79),'&',80),'|',81),'^',82),'/',83),'==',84),'>',85),'>=',86),'<',87),'<=',88),'&&',89),'||',90),'^^',91),'-',92),'*',93),'!=',94),'+',95),'%',96),'<<',97),'>>',98),'=',99),'+=',100),'&=',101),'|=',102),'^=',103),'/=',104),'*=',105),'%=',106),'<<=',107),'>>=',108),'-=',109),':',110),',',111),'.',112),'{',113),'[',114),'(',115),'?',116),'}',117),']',118),')',119),';',120),vh=j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(j(new Map,'asm',0),'cast',0),'class',0),'default',0),'double',0),'dvec2',0),'dvec3',0),'dvec4',0),'enum',0),'extern',0),'external',0),'fixed',0),'fvec2',0),'fvec3',0),'fvec4',0),'goto',0),'half',0),'hvec2',0),'hvec3',0),'hvec4',0),'inline',0),'input',0),'interface',0),'long',0),'namespace',0),'noinline',0),'output',0),'packed',0),'public',0),'sampler1D',0),'sampler1DShadow',0),'sampler2DRect',0),'sampler2DRectShadow',0),'sampler3DRect',0),'short',0),'sizeof',0),'static',0),'superp',0),'switch',0),'template',0),'this',0),'typedef',
0),'union',0),'unsigned',0),'using',0),'volatile',0),ja=A(new vc(-1,null,'bool',null)),yc=A(new vc(-2,null,'bvec2',null)),zc=A(new vc(-3,null,'bvec3',null)),Ac=A(new vc(-4,null,'bvec4',null)),I=A(new vc(-5,null,'<error>',null)),ka=A(new vc(-6,null,'float',null)),ea=A(new vc(-7,null,'int',null)),Bc=A(new vc(-8,null,'ivec2',null)),Cc=A(new vc(-9,null,'ivec3',null)),Dc=A(new vc(-10,null,'ivec4',null)),ac=A(new vc(-11,null,'mat2',null)),bc=A(new vc(-12,null,'mat3',null)),cc=A(new vc(-13,null,'mat4',null)),xh=oa(A(new vc(-14,null,'sampler2D',null))),yh=oa(A(new vc(-15,null,'samplerCube',null))),ob=A(new vc(-16,null,'vec2',null)),pb=A(new vc(-17,null,'vec3',null)),qb=A(new vc(-18,null,'vec4',null)),ae=A(new vc(-19,null,'void',null)),zh=oa(A(new vc(-20,null,'isampler2D',null))),Ah=oa(A(new vc(-21,null,'isampler2DArray',null))),Bh=oa(A(new vc(-22,null,'isampler3D',null))),Ch=oa(A(new vc(-23,null,'isamplerCube',null))),cd=A(new vc(-24,null,'mat2x3',null)),dd=A(new vc(-25,null,'mat2x4',null)),ed=A(new vc(-26,null,'mat3x2',
null)),fd=A(new vc(-27,null,'mat3x4',null)),gd=A(new vc(-28,null,'mat4x2',null)),hd=A(new vc(-29,null,'mat4x3',null)),Dh=oa(A(new vc(-30,null,'sampler2DArray',null))),Eh=oa(A(new vc(-31,null,'sampler2DArrayShadow',null))),Fh=oa(A(new vc(-32,null,'sampler2DShadow',null))),di=oa(A(new vc(-33,null,'sampler3D',null))),Gh=oa(A(new vc(-34,null,'samplerCubeShadow',null))),dc=A(new vc(-35,null,'uint',null)),Hh=oa(A(new vc(-36,null,'usampler2D',null))),Ih=oa(A(new vc(-37,null,'usampler2DArray',null))),Jh=oa(A(new vc(-38,null,'usampler3D',null))),Kh=oa(A(new vc(-39,null,'usamplerCube',null))),id=A(new vc(-40,null,'uvec2',null)),jd=A(new vc(-41,null,'uvec3',null)),kd=A(new vc(-42,null,'uvec4',null)),Lh=[ja,yc,zc,Ac,ka,ea,Bc,Cc,Dc,ac,bc,cc,xh,yh,ob,pb,qb],Mh=[zh,Ah,Bh,Ch,cd,dd,ed,fd,gd,hd,Dh,Eh,Fh,Gh,dc,Hh,Ih,Jh,Kh,id,jd,kd],Df=j(j(j(j(j(new Map,'json',0),'js',1),'c++',2),'skew',3),'rust',4),Ef=j(j(j(new Map,'all',0),'internal-only',1),'none',2),ii=['ERROR','WARNING'],Rh=['GLOBAL','STRUCT_BLOCK','VARIABLE','BLOCK','BREAK',
'CONTINUE','DISCARD','DO_WHILE','EXPRESSION','EXTENSION','FOR','FUNCTION','IF','MODIFIER_BLOCK','PRECISION','RETURN','STRUCT','VARIABLES','VERSION','WHILE','CALL','DOT','HOOK','NAME','PARSE_ERROR','SEQUENCE','TYPE','UNKNOWN_CONSTANT','BOOL','FLOAT','INT','UINT','COMPLEMENT','NEGATIVE','NOT','POSITIVE','PREFIX_DECREMENT','PREFIX_INCREMENT','POSTFIX_DECREMENT','POSTFIX_INCREMENT','ADD','BITWISE_AND','BITWISE_OR','BITWISE_XOR','DIVIDE','EQUAL','GREATER_THAN','GREATER_THAN_OR_EQUAL','INDEX','LESS_THAN','LESS_THAN_OR_EQUAL','LOGICAL_AND','LOGICAL_OR','LOGICAL_XOR','MULTIPLY','NOT_EQUAL','REMAINDER','SHIFT_LEFT','SHIFT_RIGHT','SUBTRACT','ASSIGN','ASSIGN_ADD','ASSIGN_BITWISE_AND','ASSIGN_BITWISE_OR','ASSIGN_BITWISE_XOR','ASSIGN_DIVIDE','ASSIGN_MULTIPLY','ASSIGN_REMAINDER','ASSIGN_SHIFT_LEFT','ASSIGN_SHIFT_RIGHT','ASSIGN_SUBTRACT'],zf=['SINGLE_LINE_COMMENT','MULTI_LINE_COMMENT','ATTRIBUTE','BOOL','BREAK','BVEC2','BVEC3','BVEC4','CONST','CONTINUE','DISCARD','DO','ELSE','FALSE','FLOAT','FOR','HIGHP','IF','IN','INOUT','INT',
'INVARIANT','IVEC2','IVEC3','IVEC4','LOWP','MAT2','MAT3','MAT4','MEDIUMP','OUT','PRECISION','RETURN','SAMPLER2D','SAMPLERCUBE','STRUCT','TRUE','UNIFORM','VARYING','VEC2','VEC3','VEC4','VOID','WHILE','CENTROID','FLAT','ISAMPLER2D','ISAMPLER2DARRAY','ISAMPLER3D','ISAMPLERCUBE','LAYOUT','MAT2X3','MAT2X4','MAT3X2','MAT3X4','MAT4X2','MAT4X3','SAMPLER2DARRAY','SAMPLER2DARRAYSHADOW','SAMPLER2DSHADOW','SAMPLER3D','SAMPLERCUBESHADOW','SMOOTH','UINT','USAMPLER2D','USAMPLER2DARRAY','USAMPLER3D','USAMPLERCUBE','UVEC2','UVEC3','UVEC4','DECORATOR','EXPORT','EXPORT_FRAGMENT','EXPORT_VERTEX','IMPORT','COMPLEMENT','DECREMENT','INCREMENT','NOT','BITWISE_AND','BITWISE_OR','BITWISE_XOR','DIVIDE','EQUAL','GREATER_THAN','GREATER_THAN_OR_EQUAL','LESS_THAN','LESS_THAN_OR_EQUAL','LOGICAL_AND','LOGICAL_OR','LOGICAL_XOR','MINUS','MULTIPLY','NOT_EQUAL','PLUS','REMAINDER','SHIFT_LEFT','SHIFT_RIGHT','ASSIGN','ASSIGN_ADD','ASSIGN_BITWISE_AND','ASSIGN_BITWISE_OR','ASSIGN_BITWISE_XOR','ASSIGN_DIVIDE','ASSIGN_MULTIPLY','ASSIGN_REMAINDER','ASSIGN_SHIFT_LEFT',
'ASSIGN_SHIFT_RIGHT','ASSIGN_SUBTRACT','COLON','COMMA','DOT','LEFT_BRACE','LEFT_BRACKET','LEFT_PARENTHESIS','QUESTION','RIGHT_BRACE','RIGHT_BRACKET','RIGHT_PARENTHESIS','SEMICOLON','EXTENSION','INCLUDE','PREPROCESSOR_DIRECTIVE','VERSION','FLOAT_LITERAL','IDENTIFIER','INT_LITERAL','UINT_LITERAL','STRING_LITERAL','END_OF_FILE'],ei=Qa(Qa(Qa(Qa(Qa(Qa(Qa(Qa(Qa(new Map,0,0),1,1),2,90),3,31),4,32),5,33),6,34),7,35),8,36);$i()})();
